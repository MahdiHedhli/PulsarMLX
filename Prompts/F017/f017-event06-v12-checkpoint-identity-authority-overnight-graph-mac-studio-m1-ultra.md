# PulsarMLX F017 Overnight Graph Program
## Event 06 V12 Checkpoint Identity Producer Authority, Pre-Package Instantiability, and Execution Readiness
### Execution Machine: Apple Mac Studio M1 Ultra

# MISSION

Event 05 is terminal and must not be retried or resumed.

Event 05 safely reached:

- fresh operator approval;
- exact candidate construction;
- exact live authorization installation;
- package claim;
- package durable start;
- checkpoint-identity durable start.

It then failed before opening any checkpoint shard because the V11 coordinator
and the measured V10 checkpoint-identity producer used irreconcilable execution
scope vocabularies.

The V11 candidate and coordinator require:

`PRODUCTION_EVENT_05`

The measured V10 identity producer accepts only:

- `SYNTHETIC_QUALIFICATION`
- `PRODUCTION_EVENT_04`

and otherwise raises:

`ValueError("checkpoint identity producer authority")`

The one-shot Event 05 GO was consumed by this terminal package failure.

This phase must repair the checkpoint-identity producer authority boundary
before another real event.

The successor must:

1. preserve Event 05 as immutable truthful failure evidence;
2. replace event-number hardcoding with one typed production operation scope;
3. define one canonical producer-consumer authority contract;
4. pass exact candidate bytes through the real identity-producer validation
   boundary before package start;
5. revalidate exact installed authorization at runtime;
6. guarantee zero checkpoint access during validation-only instantiability;
7. model authority mismatch and producer drift as exact lifecycle outcomes;
8. eliminate generic fallback for every modeled identity-authority failure;
9. version and measure the successor implementation;
10. run an overnight synthetic six-shard and failure-outcome campaign;
11. run production-shaped no-access rehearsal;
12. run exact-head FULL_NATIVE CI;
13. obtain Gemini CHALLENGE and Opus ARBITER acceptance;
14. stop at a fresh human Event 06 GO boundary.

Do not execute Event 06.

Do not mint Event-06 live authority.

Do not access original checkpoint shard payloads.

Do not execute P1 attempt 2.

Repository evidence is authoritative.

Git outranks this prompt.

---

# A. STARTING AUTHORITY

Authoritative branch:

`feat/017-rust-native-inference-runtime`

Expected current branch head:

`ec92987e8fa1f37db661f8bcdc363217b34eda0a`

Expected current tree:

`35754b23f2b86f9afaa9708e23b0987fd981d2b2`

Expected current commit:

`docs: close Event 05 terminal failure graph`

Event 05 terminal execution failure:

`docs/architecture/reviews/evidence/f017-event05-v11-terminal-execution-failure-v2.json`

Failure declaration:

`F017_CORRECTED_ORACLE_EVENT05_EXECUTION: TERMINAL_FAILURE_NO_RETRY`

Failed node:

`E5`

Last completed transition:

`CHECKPOINT_IDENTITY_START`

Failure class:

`ValueError`

Failure message:

`checkpoint identity producer authority`

Root cause:

`V10_IDENTITY_PRODUCER_SCOPE_IRRECONCILABLE_WITH_V11_EVENT05_CANDIDATE`

Operator approval SHA-256:

`453e6fdaea11f4b576cf0b0874dc693bf55503fa245e813f4c9f87cad5693c16`

Candidate SHA-256:

`2876a55750aa894c301f54c9104d493c733b8a0d9fcf7c39241a580af631c7e3`

Installation-receipt SHA-256:

`082540ab8077f69d5a829f818858231f5994528a634f2a13e0ee42fca1db7085`

Package-terminal SHA-256:

`a9a744d8fc440e28f71cc861d480e88e3c405e94c9712780bcaa5008f89d6ed0`

Failure-terminal-capsule SHA-256:

`6c334d14c668a34b524bee8dfe351396f02de95448a2e10e778f49a4fc1b31a7`

Event 05 final no-go declaration:

`docs/architecture/reviews/evidence/f017-event05-v11-final-no-go-declaration-v2.json`

Event 05 disposition:

`TERMINAL_POST_PACKAGE_START_FAILURE`

Event 05 GO disposition:

`CONSUMED_BY_ONE_SHOT_TERMINAL_FAILURE`

Event 05 package delta:

`1`

Event 05 primary delta:

`0`

Event 05 secondary delta:

`0`

Event 05 checkpoint shard opens:

`0`

Event 05 identity hash reads:

`0`

Event 05 payload reads:

`0`

Event 05 live leases after terminal:

`0`

Event 05 Opus terminal-failure review:

`docs/architecture/reviews/evidence/f017-event05-v11-opus-terminal-failure-arbiter-cycle-01-exact-response.md`

Opus reviewed head:

`176688006b8abb6fe77979fa66e272809e60b1c9`

Opus terminal-failure evidence result:

- truthful and complete: `YES`
- accepted claims: `19`
- rejected claims: `11`
- unresolved claims: `0`
- blocking findings: `0`
- non-blocking-required findings: `0`
- execution rerun: `NO`

Event 05 terminal EVIDENCE_ONLY run:

`33071927245`

Result:

`PASS`

Native jobs launched:

`0`

Historical master ledger:

`175`

Historical master-ledger SHA-256:

`aa98f5cc7f1cfae1eb49a9bc64dbefec1d6ef9ccae1504a1aa8879a8edf22e3e`

If Git differs from this handoff, Git wins.

---

# B. IMMUTABLE EVENT HISTORY

## B1. Event 04

`EVENT_04_RETRY: NO`

`EVENT_04_RESUME: NO`

Event 04 remains immutable terminal failure evidence.

## B2. Event 05

`EVENT_05_RETRY: NO`

`EVENT_05_RESUME: NO`

`EVENT_05_PACKAGE_EXECUTED: YES`

`PRIMARY_REAL_ORACLE_EVENT05_EXECUTED: NO`

`SECONDARY_REAL_ORACLE_EVENT05_EXECUTED: NO`

Event 05 approval, authorization, package claim, durable start, journal, failure
capsule, package terminal, and no-go declaration remain immutable.

Do not create:

- new Event 05 identity receipts;
- Event 05 leases;
- Event 05 primary or secondary receipts;
- Event 05 comparison;
- Event 05 success declaration.

## B3. P1

`P1_ATTEMPT_2_EXECUTED: NO`

`LIVE_P1_ATTEMPT_2_AUTHORIZATION_CREATED: NO`

---

# C. FROZEN NUMERICAL AND RESULT AUTHORITY

Do not change:

- numerical contract V4;
- primary numerical core V3;
- secondary numerical core V3;
- numerical formulas;
- numerical operation order;
- decoder semantics;
- routing semantics;
- V11 binary result envelope;
- V11 result authority;
- independent comparison authority;
- output geometry;
- thresholds;
- tensor plan;
- context.

Frozen context:

- prompt token: `9703`
- position: `0`
- KV state: `EMPTY`
- mask: `ONE_VISIBLE_KEY_CAUSAL`
- sampling: `NONE_GREEDY_ARGMAX`
- top-N: `32`

Frozen comparison thresholds:

- maximum absolute error:
  `0.0065169706285814755`
- RMSE:
  `0.003463567697419031`
- minimum cosine similarity:
  `0.9999999985448085`

Historical observations:

- `21615`
- `17351`
- `154820`

remain quarantined from:

- expected-token fields;
- execution arguments;
- environment variables;
- success predicates;
- fixture selection;
- selected logit indexes;
- threshold selection;
- retry decisions.

If numerical or result semantics must change:

1. stop this phase;
2. classify the expanded scope;
3. do not continue under this prompt.

---

# D. VERSIONING DOCTRINE

The repair changes:

- checkpoint-identity producer authority vocabulary;
- producer capability schema;
- coordinator-to-producer interface;
- candidate validation;
- installed-authorization handshake;
- lifecycle failure outcome semantics;
- measured implementation authority;
- scientific-access authority.

This is a lifecycle-generation change.

Expected successor generation:

`V12`

Event 05 and V11 remain historical terminal authority.

Do not silently modify V11.

Expected successor surfaces include:

- checkpoint-identity producer V12;
- checkpoint-identity authority validator V12;
- checkpoint-identity capability V12;
- coordinator V12;
- authorization interface V12;
- authorizer V12;
- scientific-access contract V12;
- lifecycle outcome contract V12;
- implementation measurement V12;
- authority manifest V12;
- inert Event 06 GO template.

During design and implementation:

`ACTIVE_CORRECTED_ORACLE_GENERATION: NONE`

After final whole-domain acceptance:

`ACTIVE_CORRECTED_ORACLE_GENERATION: V12`

Bank the exact versioning decision before implementation.

---

# E. CANONICAL SCOPE MODEL

Do not branch capability on event number.

Do not introduce:

- `PRODUCTION_EVENT_05`
- `PRODUCTION_EVENT_06`
- one hardcoded branch per future event.

Use separate typed concepts.

## E1. Authority scope

Allowed:

- `SYNTHETIC`
- `PRODUCTION`

## E2. Operation class

Allowed production operation:

`CORRECTED_FULL_CHECKPOINT_ORACLE`

## E3. Generation

Required live generation:

`V12`

## E4. Event identity

The package attempt and consumer event IDs remain exact unique identities.

They do not determine producer capability.

## E5. Production identity authority

The producer validates exact:

- authorization ID;
- package-attempt ID;
- generation;
- authority scope;
- operation class;
- checkpoint-set SHA;
- checkpoint root;
- checkpoint-identity contract SHA;
- producer measurement SHA;
- expected shard count;
- expected total bytes;
- attempts;
- retries;
- resume;
- installed-authorization SHA where applicable.

A production Event 06 candidate is eligible because it is an authorized V12
corrected-oracle package, not because the producer contains an Event 06 branch.

---

# F. HARD INVARIANTS

Throughout this phase:

`EVENT_04_RETRY: NO`

`EVENT_05_RETRY: NO`

`EVENT_05_RESUME: NO`

`EVENT_06_EXECUTED: NO`

`LIVE_EVENT_06_AUTHORIZATION_CREATED: NO`

`EVENT_06_PACKAGE_STARTED: NO`

`PRIMARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0`

`SECONDARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0`

`NEW_ORIGINAL_CHECKPOINT_SHARD_OPENS: 0`

`NEW_ORIGINAL_CHECKPOINT_IDENTITY_HASH_READS: 0`

`NEW_ORIGINAL_CHECKPOINT_MMAPS: 0`

`NEW_ORIGINAL_CHECKPOINT_TENSOR_READS: 0`

`NEW_ORIGINAL_CHECKPOINT_PAYLOAD_READS: 0`

`P1_ATTEMPT_2_EXECUTED: NO`

`LIVE_P1_ATTEMPT_2_AUTHORIZATION_CREATED: NO`

`HISTORICAL_MASTER_LEDGER: 175`

Synthetic file-backed checkpoint access is permitted.

Synthetic full hashing is permitted.

Production metadata-only inspection is permitted.

Production-shaped no-access rehearsal is permitted.

Original checkpoint payload access is prohibited.

---

# G. GRAPH CONTROL PLANE

Maintain:

1. `f017-event06-identity-producer-graph-state-v1.json`
2. `f017-event06-identity-producer-claim-ledger-v1.json`
3. `f017-event06-identity-producer-challenge-ledger-v1.json`
4. `f017-event06-identity-producer-support-ledger-v1.json`
5. `f017-event06-identity-producer-arbiter-ledger-v1.json`
6. one node receipt per completed node
7. one final authority manifest

Recommended location:

`docs/architecture/reviews/evidence/`

## G1. Node statuses

Each node has one status:

- `PENDING`
- `RUNNING`
- `PASS`
- `REPAIR_REQUIRED`
- `UNRESOLVED`
- `BLOCKED`
- `INVALIDATED`

Only one node may be `RUNNING`.

## G2. Claim verdicts

Every readiness-critical claim requires:

- `ACCEPT`
- `REJECT`
- `UNRESOLVED`

Global readiness requires all readiness-critical claims to be `ACCEPT`.

## G3. Resume behavior

On restart:

1. load graph state;
2. validate completed node receipts;
3. verify output commits;
4. verify dependency SHAs;
5. resume the active frontier.

Do not replay nodes whose dependencies remain exact.

---

# H. REVIEW ROLES

Builder and integrator:

`GPT-5.6 Sol High`

Final integration:

`GPT-5.6 Sol Extra High`

CHALLENGE reviewer:

`gemini-3.1-pro-high`

ARBITER reviewer:

`claude-opus-5`

Gemini creates structured challenges.

The builder answers through:

- existing evidence;
- repair;
- concession;
- `UNRESOLVED`.

Opus decides every readiness-critical claim.

Do not silently substitute another reviewer.

---

# I. READINESS-CRITICAL CLAIMS

## Scope and interface

`C-SCOPE-001`

> Checkpoint-identity producer capability depends on typed authority scope,
> operation class, and generation, not an event-number string.

`C-SCOPE-002`

> Event identity remains exact and package-bound without becoming a capability
> branch.

`C-INTERFACE-001`

> Coordinator, authorizer, capability, producer, and lifecycle contracts use one
> canonical checkpoint-identity authority interface.

`C-VALIDATE-001`

> Exact candidate bytes pass the real producer validation-only boundary before
> package state is created.

`C-VALIDATE-002`

> Exact installed authorization passes the producer boundary again before
> package claim.

`C-VALIDATE-003`

> Validation-only producer qualification creates no state and performs zero
> checkpoint access.

## Runtime and lifecycle

`C-RUNTIME-001`

> The live identity producer accepts only an immutable validated authority
> object bound to installed authorization and measured producer bytes.

`C-RUNTIME-002`

> Six-shard identity verification begins only after package durable start.

`C-FAIL-001`

> Producer-authority mismatch before package start is a modeled pre-package
> outcome with package delta zero.

`C-FAIL-002`

> Producer-authority drift after package start is a modeled package failure with
> package delta one and exact terminalization.

`C-FAIL-003`

> Every modeled checkpoint-identity authority failure avoids generic fallback.

## Compatibility and safety

`C-HIST-001`

> V10 and V11 historical producer behavior remains immutable and cannot mint or
> execute future live authority.

`C-SYN-001`

> The V12 producer successfully hashes six synthetic shards and retains exactly
> five graph leases.

`C-NOACCESS-001`

> Production-shaped rehearsal and all pre-GO qualification perform zero original
> checkpoint access.

`C-CI-001`

> Exact-head FULL_NATIVE CI accepts V12 with zero required native skips.

`C-GO-001`

> Exact final V12 readiness bytes are instantiable by coordinator, consumers,
> and identity producer before a fresh Event 06 GO.

---

# J. GRAPH TOPOLOGY

```text
R0 EVENT05_POSTMORTEM_AND_INTERFACE_INVENTORY
  |
  v
R1 CHECKPOINT_IDENTITY_AUTHORITY_CONTRACT
  |
  v
R2 PRODUCER_CONSUMER_INSTANTIABILITY_DESIGN
  |
  v
R3 LIFECYCLE_FAILURE_OUTCOME_DESIGN
  |
  v
R4 GEMINI_DESIGN_CHALLENGE
  |
  v
R5 SUPPORT_AND_DESIGN_REPAIR
  |
  v
R6 OPUS_DESIGN_ARBITER
  |
  +-- REJECT --> earliest invalidated design node
  |
  +-- UNRESOLVED --> NO_GO_CLOSEOUT
  |
  ACCEPT
  v
R7 V12_IMPLEMENTATION_AND_HISTORICAL_SUPERSESSION
  |
  v
R8 EXACT_CANDIDATE_AND_INSTALLED_AUTH_INSTANTIABILITY
  |
  v
R9 OVERNIGHT_SYNTHETIC_IDENTITY_QUALIFICATION
  |
  v
R10 RUNTIME_FAILURE_OUTCOME_CAMPAIGN
  |
  v
R11 PRODUCTION_SHAPED_NO_ACCESS_REHEARSAL
  |
  v
R12 FULL_NATIVE_CI
  |
  v
R13 GEMINI_WHOLE_DOMAIN_CHALLENGE
  |
  v
R14 SUPPORT_AND_IMPLEMENTATION_REPAIR
  |
  v
R15 OPUS_WHOLE_DOMAIN_ARBITER
  |
  +-- REJECT --> earliest invalidated node
  |
  +-- UNRESOLVED --> NO_GO_CLOSEOUT
  |
  ACCEPT
  v
R16 FINAL_READINESS_DECLARATION
  |
  v
R17 POST_COMMIT_EXACT_INSTANTIABILITY
  |
  v
R18 EVIDENCE_ONLY_CI_AND_FINAL_RATIFICATION
  |
  v
R19 FINAL_AUTHORITY_RECONCILIATION
  |
  v
STOP_AT_FRESH_HUMAN_EVENT06_GO
```

---

# R0. EVENT 05 POSTMORTEM AND INTERFACE INVENTORY

## Work

1. Fresh-fetch all refs.
2. Verify branch, head, tree, parity, and clean worktree.
3. Verify no compatible active loop.
4. Reconstruct Event 05 operator approval.
5. Reconstruct candidate and installation.
6. Verify candidate and authorization are byte-identical.
7. Reconstruct package claim and durable start.
8. Verify package delta one.
9. Reconstruct checkpoint-identity durable start.
10. Reconstruct transition journal through sequence 3.
11. Reconstruct failure capsule and package terminal.
12. Verify checkpoint opens, hashes, mappings, and reads zero.
13. Verify no leases existed.
14. Verify primary and secondary never started.
15. Verify the producer accepted scopes from exact V10 Git bytes.
16. Verify the coordinator-required scope from exact V11 Git bytes.
17. Verify the mismatch is structurally forced.
18. Verify the failure was generic fallback with no outcome ID.
19. Enumerate every checkpoint-identity producer caller.
20. Enumerate every checkpoint-identity authority validator.
21. Enumerate every scope string in active and historical code.
22. Enumerate every lifecycle outcome related to identity authority.
23. Verify no Event 06 state or IDs exist.
24. Verify original checkpoint access remains zero.
25. Verify historical ledger remains 175.

## Postmortem artifact

Bank:

`f017-event05-checkpoint-identity-authority-postmortem-v1.json`

Include:

- immutable Event 05 authority;
- exact producer and coordinator SHAs;
- exact scope vocabularies;
- exact failing transition;
- exact accounting;
- exact no-access proof;
- exact versioning impact;
- smallest truthful repair.

## Claims

Introduce:

- `C-HIST-001`
- `C-SCOPE-001`
- `C-INTERFACE-001`

## Exit

Event 05 root cause reconstructed mechanically.

---

# R1. CHECKPOINT IDENTITY AUTHORITY CONTRACT

## Objective

Define one canonical typed authority shared by authorizer, coordinator,
capability report, producer validator, live producer, and lifecycle validator.

## R1.1 Contract fields

At minimum require exact fields for:

- schema;
- authority scope;
- operation class;
- generation;
- authorization ID;
- package-attempt ID;
- checkpoint-set SHA;
- canonical checkpoint root;
- checkpoint-identity contract path and SHA;
- producer capability path and SHA;
- measured producer path and SHA;
- expected shard count;
- expected identity-only shard count;
- expected graph-payload shard count;
- expected total bytes;
- attempts;
- retries;
- resume;
- event identity plan SHA;
- installed-authorization SHA for the live phase.

Separate candidate-phase and installed-phase schemas if required.

## R1.2 Exact types

Require:

- exact lowercase schemas;
- exact enumerations;
- strict booleans;
- non-boolean integers;
- exact SHA grammar;
- canonical paths;
- exact identity grammar;
- exact counts.

Reject aliases, coercions, missing fields, and unknown fields.

## R1.3 Capability report

The producer capability report binds:

- supported authority schema;
- supported authority scopes;
- supported operation classes;
- supported generations;
- checkpoint-identity contract SHA;
- producer measurement SHA;
- zero-access validation entry point;
- live production entry point.

It must not enumerate Event 04, Event 05, or Event 06 as capability branches.

## R1.4 Event identity

The package-attempt ID and consumer IDs remain exact.

The producer verifies package identity against authorization.

It does not branch on the event number.

## R1.5 Checkpoint census

Derive:

- shard count `6`;
- identity-only count `1`;
- graph-payload count `5`;
- total bytes `238,458,632,928`;

from accepted shard records.

Do not trust independent literals.

## Claims

Close or support:

- `C-SCOPE-001`
- `C-SCOPE-002`
- `C-INTERFACE-001`

---

# R2. PRODUCER-CONSUMER INSTANTIABILITY DESIGN

## Objective

Prove exact producer compatibility before a one-shot package starts.

## R2.1 Validation-only entry point

Create a pure authority validation entry point:

```text
validate_checkpoint_identity_candidate(candidate, expected) -> report
```

It:

- parses exact candidate bytes;
- validates scope, operation, generation, IDs, checkpoint set, contract, producer
  measurement, counts, attempts, retries, and resume;
- creates no state;
- opens no checkpoint root;
- opens no checkpoint shard;
- performs no hash read;
- returns one bounded report.

## R2.2 Candidate dual-plus-one validation

Before installation, exact candidate bytes must pass:

1. primary consumer validation;
2. secondary consumer validation;
3. checkpoint-identity producer validation.

All three reports bind the same candidate SHA.

## R2.3 Installed-auth validation

Before package claim, exact installed authorization must pass:

1. primary installed-auth validation;
2. secondary installed-auth validation;
3. checkpoint-identity installed-auth validation.

All three reports bind:

- installed authorization SHA;
- installation receipt SHA;
- measured implementation;
- generation;
- package attempt;
- checkpoint set.

## R2.4 Shared validation path

Validation-only and live production must share the same core authority validator.

Do not duplicate scope logic inside the producer's live function.

## R2.5 Validated authority object

The live producer accepts an immutable validated authority object.

It must not accept an unvalidated raw dictionary or caller-supplied scope
string.

## R2.6 Package-start eligibility

Package claim and durable start are forbidden until producer installed-auth
validation PASS is durably banked.

## Claims

Close or support:

- `C-VALIDATE-001`
- `C-VALIDATE-002`
- `C-VALIDATE-003`
- `C-RUNTIME-001`

---

# R3. LIFECYCLE FAILURE OUTCOME DESIGN

## Objective

Make every identity-authority failure an exact modeled outcome.

## R3.1 Pre-candidate failures

Examples:

- unsupported generation;
- unsupported operation class;
- wrong checkpoint set;
- wrong producer measurement;
- malformed candidate authority.

Required:

- no candidate installation;
- package delta zero;
- checkpoint access zero;
- GO expires;
- pre-mint terminal evidence.

## R3.2 Post-install pre-package failures

Examples:

- installed authorization differs;
- capability changed;
- producer measurement changed;
- installed-auth report stale.

Required:

- installed authority preserved;
- package delta zero;
- checkpoint access zero;
- exact failure capsule;
- no retry.

## R3.3 Post-package drift failures

Examples:

- producer bytes changed after handshake;
- retained validated authority identity mismatch;
- checkpoint contract changed;
- package attempt mismatch discovered at identity start.

Required:

- package delta one;
- consumer deltas zero;
- checkpoint access zero where failure precedes open;
- package terminal;
- exact outcome ID;
- no generic fallback.

## R3.4 Generic fallback

Generic fallback remains only for genuinely unclassified internal errors.

The qualification must prove every injected authority mismatch uses a modeled
outcome.

## R3.5 Exact transition IDs

Do not use exception class as `failed_transition_id`.

Freeze transition IDs such as:

- `IDENTITY_CANDIDATE_AUTHORITY_VALIDATE`
- `IDENTITY_INSTALLED_AUTHORITY_VALIDATE`
- `IDENTITY_RUNTIME_AUTHORITY_REVALIDATE`
- `CHECKPOINT_IDENTITY_START`

## Claims

Close or support:

- `C-FAIL-001`
- `C-FAIL-002`
- `C-FAIL-003`
- `C-RUNTIME-002`

---

# R4. GEMINI DESIGN CHALLENGE

Use:

`gemini-3.1-pro-high`

Effort:

`high`

Maximum cycles:

`3`

Gemini must attack:

- generic scope design;
- event-number smuggling;
- candidate producer validation;
- installed-auth validation;
- validated authority object;
- package-start eligibility;
- stale report replay;
- producer measurement drift;
- checkpoint-set substitution;
- lifecycle outcomes;
- generic fallback;
- historical supersession;
- no-access guarantees.

Gemini creates challenge rows.

Repair material findings before Opus.

---

# R5. SUPPORT AND DESIGN REPAIR

For each challenge:

1. map it to a claim;
2. answer with evidence, repair, concession, or `UNRESOLVED`;
3. invalidate the earliest node;
4. add permanent mutation coverage;
5. rerun only invalidated design nodes;
6. update challenge and support ledgers.

Maximum pushed repair heads per cycle:

`2`

Maximum local repair commits per cycle:

`10`

---

# R6. OPUS DESIGN ARBITER

Use:

`claude-opus-5`

Effort:

`high`

Fresh session each cycle.

Maximum cycles:

`6`

Maximum pushed repair heads per cycle:

`2`

Maximum local repair commits per cycle:

`12`

Opus must independently:

1. reconstruct Event 05 failure;
2. inspect V10 producer and V11 coordinator;
3. verify event-number hardcoding is removed;
4. inspect canonical authority contract;
5. inspect candidate producer validation;
6. inspect installed-auth producer validation;
7. inspect package-start gate;
8. inspect validated authority object;
9. mutate scope, operation, generation, IDs, checkpoint set, contract, and
   producer measurement;
10. inspect lifecycle failure outcomes;
11. prove modeled failures avoid generic fallback;
12. verify historical V10 and V11 evidence remains immutable;
13. verify zero original checkpoint access.

Required verdict:

`ACCEPT_F017_CHECKPOINT_IDENTITY_AUTHORITY_V12_FOR_IMPLEMENTATION`

or:

`REJECT`

A repairable rejection does not stop the graph while budget remains.

After acceptance, continue automatically.

---

# R7. V12 IMPLEMENTATION AND HISTORICAL SUPERSESSION

## R7.1 Historical files

Preserve exact historical Git objects for:

- V10 identity producer;
- V10 lease manager;
- V11 coordinator;
- Event 05 authority and failure evidence.

Retire old live execution paths.

## R7.2 Implement V12 authority validator

Create the canonical validator accepted at R6.

## R7.3 Implement V12 producer capability

Create a measured capability report and validator.

## R7.4 Implement V12 identity producer

The live producer:

- accepts only validated authority;
- rechecks installed authorization identity;
- verifies measured producer bytes;
- verifies contract and checkpoint set;
- begins shard access only after package durable start;
- produces the accepted access journal, shard receipts, lease manifest, identity
  manifest, receipt, and terminal.

## R7.5 Implement V12 coordinator

The coordinator requires producer PASS:

- at candidate validation;
- at installed-auth handshake.

It forbids package claim before those reports.

## R7.6 Implement V12 authorizer and contracts

Create:

- authorization interface V12;
- authorizer V12;
- scientific-access contract V12;
- lifecycle outcome contract V12;
- capabilities V12;
- inert Event 06 template;
- implementation measurement;
- authority manifest.

## R7.7 No numerical drift

Verify all numerical and result-authority bytes remain unchanged.

---

# R8. EXACT CANDIDATE AND INSTALLED-AUTH INSTANTIABILITY

## R8.1 Candidate round trip

Use one prepared inert V12 candidate.

Pass exact candidate bytes through:

- primary validator;
- secondary validator;
- identity-producer validator.

Require all three bind one candidate SHA.

Run at least:

`20 fresh processes`

Require deterministic reports.

## R8.2 Installed-auth round trip

Simulate exact installation under a noncanonical synthetic root.

Pass exact installed bytes through:

- primary installed-auth validation;
- secondary installed-auth validation;
- identity-producer installed-auth validation.

Require all three PASS.

## R8.3 Side effects

For every validation-only case:

- live authority installed: `0`
- package state created: `0`
- checkpoint root opens: `0`
- checkpoint shard opens: `0`
- identity hash reads: `0`
- numerical operations: `0`
- IDs consumed: `0`

## R8.4 Mutations

At least 250 substantive mutations:

- old `PRODUCTION_EVENT_04`;
- old `PRODUCTION_EVENT_05`;
- invented `PRODUCTION_EVENT_06`;
- wrong authority scope;
- wrong operation class;
- wrong generation;
- wrong authorization;
- wrong package;
- wrong checkpoint set;
- alternate checkpoint root;
- wrong contract SHA;
- wrong producer SHA;
- stale capability;
- stale candidate report;
- stale installed report;
- report from another package;
- candidate/install mismatch;
- attempts nonone;
- retries nonzero;
- resume true;
- malformed counts;
- alias fields;
- coercions.

Every mutation must fail closed.

## Claims

Close:

- `C-VALIDATE-001`
- `C-VALIDATE-002`
- `C-VALIDATE-003`

---

# R9. OVERNIGHT SYNTHETIC IDENTITY QUALIFICATION

Use six tiny file-backed synthetic shards.

Do not mock hash results.

## R9.1 Successful packages

At minimum:

- 30 successful identity stages;
- 20 complete minimal packages;
- 20 complete mixed-format packages;
- 20 packages with varied event IDs under the same typed production operation;
- 20 fresh process repetitions.

Each successful identity stage requires:

- package durable start before first open;
- six shard opens;
- six complete hashes;
- exact total bytes;
- shard 1 closed;
- five graph leases retained;
- identity terminal COMPLETE;
- deterministic core evidence.

## R9.2 Event-number independence

Use several valid synthetic package/event identities.

Prove producer behavior is invariant to event ordinal while authorization
identity remains exact.

Do not use event number as capability input.

## R9.3 Scope separation

Test:

- synthetic scope under synthetic authority;
- production scope under production rehearsal authority;
- synthetic candidate presented to production producer;
- production candidate presented to synthetic path.

Cross-scope use fails.

## R9.4 Descriptor and filesystem faults

Cover:

- root symlink;
- shard symlink;
- missing shard;
- extra shard;
- wrong size;
- short read;
- hash mismatch;
- file mutation during hash;
- duplicate inode;
- identity-only lease retention;
- graph lease omission;
- lease manifest failure;
- receipt failure;
- terminal failure;
- close failure.

## R9.5 Determinism

Separate deterministic core evidence from volatile envelope.

Require one deterministic core byte sequence per case.

## Claims

Close:

- `C-SYN-001`
- `C-RUNTIME-002`

---

# R10. RUNTIME FAILURE OUTCOME CAMPAIGN

Realize every V12 identity-authority failure through the runtime.

At minimum include:

## Pre-candidate

- unsupported schema;
- unsupported scope;
- unsupported operation;
- unsupported generation;
- wrong checkpoint set;
- wrong producer measurement.

## Post-install pre-package

- installed auth differs;
- capability SHA differs;
- report candidate SHA differs;
- stale report;
- report from another package;
- producer bytes differ from measurement.

## Post-package pre-open

- validated authority object mismatch;
- package attempt mismatch;
- checkpoint contract drift;
- retained authorization identity mismatch.

## Post-open identity failures

- shard-open failure;
- size mismatch;
- read failure;
- hash mismatch;
- descriptor identity change.

For every modeled outcome:

- exact transition ID;
- exact outcome ID;
- exact durable prefix;
- correct package and consumer deltas;
- correct access census;
- correct release obligations;
- exact terminal evidence;
- generic fallback `false`.

Run at least:

- 5 fresh processes per high-risk outcome;
- 3 fresh processes per other outcome.

Target at least:

`300 substantive failure executions`

## Claims

Close:

- `C-FAIL-001`
- `C-FAIL-002`
- `C-FAIL-003`

---

# R11. PRODUCTION-SHAPED NO-ACCESS REHEARSAL

Use:

- actual branch;
- exact V12 measurement head;
- actual checkpoint metadata;
- actual checkpoint set;
- actual tensor plan;
- actual machine identity;
- exact Event 06 identity plan.

Do not open original checkpoint shards.

Run:

1. rehearsal approval;
2. candidate construction;
3. primary candidate validation;
4. secondary candidate validation;
5. identity-producer candidate validation;
6. noncanonical install simulation;
7. all three installed-auth validations;
8. package-start eligibility;
9. identity plan generation;
10. lifecycle outcome validation;
11. no-access verification.

Require:

- state created: `NO`
- live authority created: `NO`
- package started: `NO`
- checkpoint root opens: `0`
- checkpoint shard opens: `0`
- identity hash reads: `0`
- payload reads: `0`
- numerical operations: `0`
- result: `PASS`

Prepare an inert Event 06 GO template.

Do not approve it.

## Claims

Close:

- `C-NOACCESS-001`
- `C-GO-001` provisionally, pending final committed bytes.

---

# R12. FULL_NATIVE CI

Run exact-head:

`FULL_NATIVE`

CI must cover:

- V10 and V11 historical authority validation;
- V12 authority contract;
- V12 capability;
- V12 producer validator;
- V12 producer;
- V12 coordinator;
- V12 authorizer;
- candidate triple validation;
- installed-auth triple validation;
- 250-plus authority mutations;
- synthetic six-shard qualification;
- runtime failure outcomes;
- generic-fallback prohibition;
- production-shaped no-access rehearsal;
- numerical requalification regression;
- result-authority regression;
- evidence-aware CI;
- generator `--check`.

Required native skips:

`0`

Repair and rerun any repairable failure.

## Claims

Close:

- `C-CI-001`

---

# R13. GEMINI WHOLE-DOMAIN CHALLENGE

Use:

`gemini-3.1-pro-high`

Effort:

`high`

Maximum cycles:

`4`

Gemini must attack:

- scope model;
- event-number leakage;
- producer capability;
- candidate validation;
- installed-auth validation;
- package-start gate;
- stale-report replay;
- producer drift;
- lifecycle outcomes;
- generic fallback;
- synthetic qualification;
- no-access rehearsal;
- CI;
- final readiness plan.

---

# R14. SUPPORT AND IMPLEMENTATION REPAIR

For each challenge:

1. map it to a claim;
2. answer, repair, concede, or mark unresolved;
3. invalidate the earliest node;
4. add regression coverage;
5. rerun invalidated subgraph;
6. rerun FULL_NATIVE for execution-byte changes;
7. update ledgers.

Maximum pushed repair heads per cycle:

`2`

Maximum local repair commits per cycle:

`12`

---

# R15. OPUS WHOLE-DOMAIN ARBITER

Use:

`claude-opus-5`

Effort:

`high`

Fresh session each cycle.

Maximum cycles:

`8`

Maximum pushed repair heads per cycle:

`2`

Maximum local repair commits per cycle:

`12`

Opus must independently:

1. reconstruct Event 05 terminal failure;
2. inspect V10 producer and V11 coordinator;
3. inspect V12 scope model;
4. search for event-number capability branches;
5. inspect candidate triple validation;
6. inspect installed-auth triple validation;
7. bypass package-start gate;
8. replay stale reports;
9. substitute producer measurement;
10. substitute checkpoint set;
11. mutate generation and operation;
12. inject runtime drift;
13. verify modeled outcome IDs;
14. verify generic fallback remains unused;
15. run synthetic six-shard success;
16. run authority failure mutations;
17. inspect no-access rehearsal;
18. inspect FULL_NATIVE CI;
19. verify original checkpoint access zero;
20. verify Event 06 authority absent;
21. verify P1 attempt 2 absent.

Required global verdict:

`ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06_EXECUTION_AUTHORIZATION_PREPARATION`

or:

`REJECT`

No conditional acceptance.

A repairable rejection does not stop the graph while budget remains.

---

# R16. FINAL READINESS DECLARATION

After Opus accepts, bank an append-only Event 06 readiness declaration.

Bind:

- V12 implementation measurement;
- V12 authority contract;
- producer capability;
- producer validator;
- coordinator;
- authorizer;
- scientific-access contract;
- synthetic qualification;
- failure qualification;
- no-access rehearsal;
- FULL_NATIVE CI;
- Gemini result;
- Opus result;
- zero blocking findings;
- zero non-blocking-required findings;
- zero unresolved claims;
- exact safety state.

State:

`F017_CORRECTED_ORACLE_EVENT06_EXECUTION_READINESS: ACCEPTED`

`ACTIVE_CORRECTED_ORACLE_GENERATION: V12`

`EVENT_04_RETRY: NO`

`EVENT_05_RETRY: NO`

`EVENT_06_EXECUTED: NO`

`LIVE_EVENT_06_AUTHORIZATION_CREATED: NO`

`EVENT_06_PACKAGE_STARTED: NO`

`PRIMARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0`

`SECONDARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0`

`NEW_ORIGINAL_CHECKPOINT_SHARD_OPENS: 0`

`NEW_ORIGINAL_CHECKPOINT_IDENTITY_HASH_READS: 0`

`NEW_ORIGINAL_CHECKPOINT_PAYLOAD_READS: 0`

`P1_ATTEMPT_2_EXECUTED: NO`

`HISTORICAL_MASTER_LEDGER: 175`

`READY_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06_EXECUTION_GO: YES`

`READY_TO_PREPARE_P1_ATTEMPT_2_AUTHORIZATION: NO`

Do not mint Event 06 authority.

---

# R17. POST-COMMIT EXACT INSTANTIABILITY

Use the exact committed final readiness declaration bytes.

Pass them through:

1. bounded readiness decode;
2. V12 authorizer validation;
3. shared candidate builder;
4. primary candidate validator;
5. secondary candidate validator;
6. identity-producer candidate validator;
7. noncanonical installation simulation;
8. all three installed-auth validators;
9. package-start eligibility validator.

Require:

- exact declaration SHA;
- exact measured implementation;
- deterministic candidate across 20 fresh processes;
- primary PASS;
- secondary PASS;
- identity producer PASS;
- installed-auth triple PASS;
- package-start eligibility PASS;
- state created false;
- live authority installed false;
- checkpoint root opens zero;
- checkpoint shard opens zero;
- identity hash reads zero;
- numerical operations zero;
- IDs consumed zero.

Bank:

`f017-event06-final-readiness-instantiability-v1.json`

If final bytes fail:

- do not patch in place;
- bank failure;
- return to earliest invalidated node;
- rerun review as required.

---

# R18. EVIDENCE_ONLY CI AND FINAL RATIFICATION

Commit final readiness and instantiability evidence.

Require:

- EVIDENCE_ONLY mode;
- evidence integrity PASS;
- native jobs launched `0`;
- final branch parity exact;
- worktree clean.

Then obtain final Opus ratification.

Use:

`claude-opus-5`

Effort:

`high`

Fresh detached read-only worktree.

Required verdict:

`ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06_EXECUTION_AUTHORIZATION_PREPARATION`

or:

`REJECT`

Review exact:

- final declaration;
- instantiability evidence;
- measured V12 implementation;
- candidate triple validation;
- installed-auth triple validation;
- package-start eligibility;
- final EVIDENCE_ONLY CI;
- zero side effects;
- zero checkpoint access.

---

# R19. FINAL AUTHORITY RECONCILIATION

Reconcile:

- final branch head and tree;
- V12 implementation measurement;
- scope contract;
- capability;
- producer validator;
- producer;
- coordinator;
- authorizer;
- scientific-access contract;
- lifecycle outcomes;
- final declaration;
- final instantiability;
- FULL_NATIVE CI;
- EVIDENCE_ONLY CI;
- Gemini result;
- Opus whole-domain verdict;
- Opus final ratification;
- historical ledger;
- checkpoint-access counters.

Run at least 40 coordinated mutations.

Mutate:

- generation;
- scope;
- operation;
- producer SHA;
- capability SHA;
- candidate report;
- installed report;
- package-start eligibility;
- final declaration SHA;
- measurement head;
- measurement tree;
- CI run;
- reviewer head;
- verdict;
- ledger;
- access counters.

Every mutation must fail.

Bank:

`F017_CHECKPOINT_IDENTITY_PRODUCER_AUTHORITY_V12: ACCEPTED`

`F017_EVENT06_EXECUTION_READINESS: ACCEPTED`

Then stop.

---

# K. STOP RULES

Do not stop for:

- schema defects;
- producer validation defects;
- coordinator defects;
- lifecycle outcome defects;
- mutation failures;
- synthetic qualification failures;
- CI failures;
- repairable Gemini challenges;
- repairable Opus findings;
- declaration defects;
- evidence-index defects.

Stop only if:

1. original checkpoint access is required;
2. numerical or result semantics must change;
3. one canonical scope model cannot represent synthetic and production use;
4. exact producer instantiability cannot be proven before package start;
5. package-start gating cannot be made fail closed;
6. historical authority is irreconcilably conflicting;
7. Gemini and Opus materially disagree after repair;
8. review budget is exhausted;
9. Event 06 readiness is accepted and a fresh human GO is required.

---

# L. REVIEW BUDGETS

## Gemini design

- maximum cycles: `3`
- pushed repair heads per cycle: `2`

## Opus design

- maximum cycles: `6`
- pushed repair heads per cycle: `2`
- local repair commits per cycle: `12`

## Gemini whole-domain

- maximum cycles: `4`
- pushed repair heads per cycle: `2`

## Opus whole-domain

- maximum cycles: `8`
- pushed repair heads per cycle: `2`
- local repair commits per cycle: `12`

## Opus final ratification

- maximum cycles: `3`
- pushed repair heads per cycle: `1`
- local repair commits per cycle: `6`

A repairable rejection does not terminate the graph while budget remains.

---

# M. OVERNIGHT QUALIFICATION TARGETS

Use the overnight window for substantive work.

Minimum targets:

- 20 fresh candidate triple validations;
- 20 fresh installed-auth triple validations;
- 30 successful synthetic identity stages;
- 20 minimal complete synthetic packages;
- 20 mixed-format complete synthetic packages;
- 250 authority mutations;
- 300 runtime failure executions;
- at least 50 filesystem and descriptor faults;
- at least 20 event-identity variations under the same operation class;
- exact deterministic core evidence across fresh processes.

Do not inflate counts with no-op mutations.

Every case has:

- stable case ID;
- expected result;
- observed result;
- outcome ID;
- transition ID;
- accounting;
- access census;
- generic-fallback status.

---

# N. NTFY

Use topic:

`Mahdi-Dev`

Use:

`scripts/research/ntfy_mahdidev.sh`

or the established equivalent.

Send:

## Start

`PulsarMLX | F017 | Event 06 identity authority | START | Mac Studio M1 Ultra`

## Postmortem

`PulsarMLX | F017 | Event 05 identity failure | RECONSTRUCTED | zero shard access`

## Scope

`PulsarMLX | F017 | identity producer scope | FROZEN | production operation not event number`

## Design review

`PulsarMLX | F017 | identity authority design C<n> | Opus 5 | <VERDICT>`

## Implementation

`PulsarMLX | F017 | identity producer V12 | IMPLEMENTED | synthetic only`

## Candidate validation

`PulsarMLX | F017 | identity candidate instantiability | PASS | primary secondary producer`

## Installed validation

`PulsarMLX | F017 | identity installed-auth handshake | PASS | package start gated`

## Synthetic qualification

`PulsarMLX | F017 | identity producer qualification | PASS | <case count>`

## Failure outcomes

`PulsarMLX | F017 | identity authority outcomes | PASS | modeled <count>`

## Rehearsal

`PulsarMLX | F017 | Event 06 shadow rehearsal | PASS | checkpoint opens 0`

## CI

`PulsarMLX | F017 | V12 exact-head CI | <PASS/FAIL> | <short-sha>`

## Whole-domain review

`PulsarMLX | F017 | Event 06 readiness C<n> | Opus 5 | <VERDICT>`

## Final instantiability

`PulsarMLX | F017 | Event 06 final instantiability | PASS | exact declaration`

## Ready

`PulsarMLX | F017 | Event 06 execution readiness | YES | fresh human GO required`

## Blocker

`PulsarMLX | F017 | identity authority V12 | BLOCKED | <reason>`

Do not expose credentials.

---

# O. COMMIT AND PUSH DISCIPLINE

Before every commit:

1. inspect status;
2. inspect diff;
3. stage exact paths only;
4. never use `git add .`;
5. never use `git add -A`;
6. never use `git add --all`;
7. verify no unrelated bytes.

Use local commits freely.

Push at:

- design authority;
- implementation measurement;
- synthetic qualification;
- FULL_NATIVE boundary;
- review repair boundary;
- final declaration;
- final ratification.

Target new pushes:

`12 or fewer`

Hard maximum absent a genuine blocker:

`24`

No force push.

No history rewrite.

Evidence-only pushes must launch zero native jobs.

---

# P. ACCEPTANCE CRITERIA

This phase is accepted only if:

## Scope

- event-number capability branches are absent;
- one typed production operation class exists;
- generation is exact;
- event identities remain exact but do not drive capability;
- synthetic and production scopes are separated.

## Instantiability

- exact candidate bytes pass primary, secondary, and producer validation;
- exact installed bytes pass all three installed-auth validators;
- package start is impossible before producer PASS;
- validation-only side effects are zero.

## Runtime

- live producer accepts only validated authority;
- six synthetic shards hash successfully;
- shard 1 closes;
- five graph leases remain;
- identity terminal closes;
- no path reopen occurs.

## Failure outcomes

- authority mismatches are modeled;
- exact transition IDs are used;
- exact outcome IDs are used;
- accounting is correct;
- generic fallback is false for every modeled outcome;
- terminalization is complete.

## Historical safety

- Event 05 remains terminal;
- no Event 05 retry or resume;
- V10 and V11 historical authorities remain immutable;
- no Event 06 execution;
- no live Event 06 authority;
- no P1 attempt 2;
- ledger remains 175.

## CI and review

- FULL_NATIVE passes;
- required native skips zero;
- EVIDENCE_ONLY passes;
- native jobs on evidence-only zero;
- Gemini has no unresolved material disagreement;
- Opus design accepts;
- Opus whole-domain accepts;
- Opus final ratification accepts;
- blocking findings zero;
- non-blocking-required findings zero;
- unresolved claims zero.

---

# Q. FINAL RESPONSE FORMAT

## 1. Starting Authority

- branch
- head and tree
- Event 05 terminal authority
- parity
- worktree
- historical ledger

## 2. Event 05 Postmortem

- approval
- candidate
- installation
- package start
- identity start
- failure
- scope mismatch
- accounting
- checkpoint access
- terminal disposition

## 3. Versioning Decision

- successor generation
- historical paths
- successor paths
- active generation before and after acceptance

## 4. Scope and Authority Contract

- contract path and SHA
- field census
- scope enumeration
- operation enumeration
- generation
- event identity treatment
- checkpoint census
- alias policy

## 5. Producer-Consumer Interface

- validator path and SHA
- capability path and SHA
- candidate validation
- installed-auth validation
- validated authority object
- package-start gate
- side-effect census

## 6. Lifecycle Outcomes

- modeled pre-candidate outcomes
- modeled post-install outcomes
- modeled post-package outcomes
- transition IDs
- outcome IDs
- generic fallback count
- accounting results

## 7. Design Reviews

- Gemini cycles and findings
- support dispositions
- Opus cycles and claim verdicts
- global verdict

## 8. V12 Implementation

- producer
- coordinator
- authorizer
- capability
- scientific-access contract
- lifecycle contract
- measurement head and tree
- numerical and result drift

## 9. Instantiability

- candidate repetitions
- primary validation
- secondary validation
- producer validation
- installed-auth repetitions
- package-start eligibility
- state created
- checkpoint access
- numerical operations
- result

## 10. Overnight Qualification

- successful identity stages
- complete packages
- event-ID variations
- authority mutations
- filesystem faults
- runtime failure executions
- deterministic evidence
- unexpected passes
- original checkpoint access
- result

## 11. Production-Shaped No-Access Rehearsal

- authority
- candidate
- three candidate validations
- three installed-auth validations
- package-start gate
- identity plan
- state created
- checkpoint root opens
- shard opens
- hash reads
- numerical operations
- result

## 12. Native CI

- implementation head
- tree
- FULL_NATIVE run
- jobs
- tests
- native skips
- EVIDENCE_ONLY run
- native jobs

## 13. Whole-Domain Reviews

- Gemini reviewed head, cycles, findings, verdict
- Opus reviewed head, cycles, claim verdicts, verdict

## 14. Final Readiness and Instantiability

- declaration path and SHA
- exact final declaration
- candidate SHA
- repetitions
- triple validation
- installed-auth triple validation
- package-start eligibility
- side effects
- final ratification

## 15. Safety State

Always state:

`EVENT_04_RETRY: NO`

`EVENT_05_RETRY: NO`

`EVENT_06_EXECUTED: NO`

`LIVE_EVENT_06_AUTHORIZATION_CREATED: NO`

`EVENT_06_PACKAGE_STARTED: NO`

`PRIMARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0`

`SECONDARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0`

`NEW_ORIGINAL_CHECKPOINT_SHARD_OPENS: 0`

`NEW_ORIGINAL_CHECKPOINT_IDENTITY_HASH_READS: 0`

`NEW_ORIGINAL_CHECKPOINT_PAYLOAD_READS: 0`

`P1_ATTEMPT_2_EXECUTED: NO`

`HISTORICAL_MASTER_LEDGER: 175`

## 16. Verdict

Return exactly one:

`F017_EVENT06_CHECKPOINT_IDENTITY_AUTHORITY_GRAPH: ACCEPTED`

or:

`REJECT`

or:

`UNRESOLVED`

## 17. Readiness

Return exactly:

`READY_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06_EXECUTION_GO: YES`

or:

`READY_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06_EXECUTION_GO: NO`

Independently state:

`READY_TO_PREPARE_P1_ATTEMPT_2_AUTHORIZATION: NO`

## 18. Exact Next Safe Action

If readiness is YES:

`Request a fresh human GO for exactly one corrected full-checkpoint oracle Event 06 under the accepted V12 checkpoint-identity authority. Do not mint or execute automatically.`

If readiness is NO:

State the smallest rejected or unresolved readiness-critical claim.

Then STOP.

Do not execute Event 06.

Do not execute P1 attempt 2.
