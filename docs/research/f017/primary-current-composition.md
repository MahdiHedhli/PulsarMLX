# Current primary composition: qualified prefix, not full result

This prospective test entry uses the current published primary helpers. It does
not recover absent historical helper bytes or backfill historical observations.

Run the existing `f017_primary_stdlib_doctor_v1.py --check` first with isolated,
no-site, no-bytecode Python. Only after it passes, the fixed
`f017_primary_composition_v1.py --check` entry uses the same sandbox profile,
four actual pre-import fences, closed hash-bound imports, and exact two passive
Apple startup-image identifiers. No new numerical backend is admitted.

The frozen generation lists all 30 source modules and its finite expectations.
All original production observer, descriptor, numerical, decoder, wrapper and
result-authority bytes are unchanged. The added child differs from the existing
doctor child only in the generation identity, closed modes, and dispatch module.
It exposes no arbitrary command, import, path or callback interface.

## Qualified edges

| Edge | Evidence |
| --- | --- |
| Synthetic descriptor -> actual primary decode/caller | Fixed vector, matrix-row and expert-row values; independent delegated-call transcript |
| Identity/bounds refusal before I/O | Zero calls after format probes; unchanged specific exceptions |
| Short, zero, error and malformed decode | Actual delegated outcome; unchanged exception and exact known prefix |
| Primary scalar core -> wrapper banking boundary | Seeds 18101/18102, one core call per attempt, exact output snapshots and routing captures |
| Observation intent/return -> durable records | Independently reconstructed counters and hash chain; real writer/reader |
| Receipt/terminal, overflow, owner loss, finalization faults | Incomplete live attachment, null totals, unknown suffix; no readback promotion |
| Invalid counts or attribution at consumer edge | Five named refusals for dropped/doubled/wrong-shard counts, cross-attempt identity and lost prefix |
| Active producer separation | Disabled active instrumentation is rejected; lost completion remains incomplete |

The hosted baseline `9aa28b5d42f99aac181b51597613a5d18526fcf4`
CI run `34372313022` failed. The bridge job's inline assertion requires its
qualification result to be PASS. Its frozen result-builder SHA differs from the
current file, which independently forces FAIL. The captured log does not expose
the complete qualification JSON, so other contributing predicates remain
unknown. No workflow, bridge authority or result binding is changed here.

## Measured current task

Two independent fresh roots each passed 66 fixed cases, with 573 actual
delegated synthetic reads. Pinned baseline and successor fixture paths produced
identical numerical snapshots, requests, order and stops. Cross-root comparison
is exact as well. The qualified doctor used 13 target starts; each composition
root used eight (including its supervisor and description child).

The deliberate uninstrumented-source negative control returns an invalid
complete-looking attachment; the independent oracle rejects it. That claim is
not included among qualified complete observations.

## Boundaries retained

The frozen production result envelope rejects the tiny geometry with
`ResultEnvelopeError: numerical output payload binding`. The observation
receipt and terminal close the known read prefix, including the BANK failure;
they are not a numerical result receipt, consumer terminal or package terminal.
No full-result, full-forward, secondary, native, P1 or Event 06 qualification is
claimed. This is scoped work-in-progress publication requiring planner review.

The five named counter mutations exercise the actual attachment consumer edge,
not an exhaustive arbitrary-source mutation campaign. The finalization fixture
closes an owned descriptor and then injects a reported close failure; it does
not establish behavior when the operating system leaves an FD open.

RSS is observed by a sampled direct-child limit and completed-child cohort peak,
not an instantaneous kernel cap. The four fences are not a universal IPC/fork
proof. Apple internal BLAS calls and framework image identity are not measured.
Physical disk bytes/faults remain NOT_MEASURABLE_BY_CURRENT_METHOD. Missing
historical counts remain UNKNOWN; ledger remains 175. No original checkpoint,
retained Event 06 data, live authority or unrelated machine is accessed.
