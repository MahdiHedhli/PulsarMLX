# Primary-only standard-library doctor

This is a separate, narrowly scoped **DOCTOR_ONLY** admission path. It does
not repair or replace the unqualified general NumPy doctor, qualify primary
numerical composition, admit secondary or native execution, or authorize a
live oracle event or a main-branch merge.

The fixed published entry is:

```text
python3 -I -S -B scripts/ci/f017_primary_stdlib_doctor_v1.py --check
```

Use only under an independently authorized, bounded fixture task. It creates
fresh temporary roots and retains captures, including failures. It accepts no
checkpoint, arbitrary command, package, or authorization argument. `--check`
does not run numerical fixtures. Failure never permits scientific replay.

## Measured scope

Two independent fresh roots passed `HARNESS_READY` with identical G2 helper
and fixture bytes. Each executed 12 nested children and one supervisor:

- Source-free capture: ordinary exit 134, actual signal 6, timeout/signal 9
  and reaping, working streams, and an absent-executable spawn refusal.
- Positive owned-file/network/exec controls followed by actual sealed
  out-of-fixture read, write, network and process-exec refusals.
- Actual import admission of 21 primary project modules from the fixed
  28-module source-as-data closure; no numerical operation or composition.
- Source mismatch refusal before project import and an actual attempted
  NumPy import refused by the closed post-seal import finder.
- Eleven inert image-list/source-AST negative controls per root, all refused.
  These data-level controls are not additional OS enforcement measurements.

The retained result digests are
`c19b528860f0e56b4b1c57a1116f69fa867fadca74c7ebe13ca0dacbd8c4e4ba`
and `82f05214e7137f2bb362fa7b1c637f0ab0f47887e37c28eec255918891abd6f9`.
Exact independent review and publication evidence accompany the task closeout;
these local test results alone do not constitute overall integration acceptance.

Earlier failed candidates are preserved. A and B stopped at RSS-limit setup;
C passed capture controls then refused the startup image census under G1.
G1 was not rewritten. G2 was separately authorized and privately published
before either successful root. Candidate A source was later reconstructed
with matching recorded hashes, not contemporaneously retained. Candidate B
full helper bodies remain unavailable; its hashes, corrections and raw failure
captures remain evidence. C, D and E source bodies were retained explicitly.

## Exact startup-image distinction

The following two exact raw dyld names may appear in the bound source-free
startup baseline:

```text
/System/Library/Frameworks/Accelerate.framework/Versions/A/Frameworks/vecLib.framework/Versions/A/libBLAS.dylib
/System/Library/Frameworks/Accelerate.framework/Versions/A/Frameworks/vecLib.framework/Versions/A/libSparseBLAS.dylib
```

This is a fixed startup classification, not a filesystem permission grant,
signature assertion, blanket system-library exemption, or permission to load
or call a numerical backend. Code constants and G2 data must agree exactly;
neither callers nor the environment may broaden them. Wrong paths, added
images, missing or stale baselines, changed order and extra exemptions fail.

Each child's complete pre-project baseline binds interpreter identity, helper
bytes, generation, scrubbed environment and standard-library module census.
The successful admission children each reported the same 381-image census
before and after primary imports. Introduced module origins are also checked.
The fixed project call closure contains no invoked numerical backend. The
unchanged write-once helper's `ctypes` use is limited to its hash-bound libc
`fchflags` helper, which this import-only doctor does not call.

This proof covers reviewed fixed call paths and observed boundaries, not every
transient image load or arbitrary future project code. System-internal BLAS
routine calls are **NOT_MEASURED**. Independent signatures are **NOT_MEASURED**;
the framework-image identity and transitive loading cause remain **UNKNOWN**.
The measured thin interpreter launcher is not a complete framework trust proof.

## Separation, custody and limits

The controller reuses `f017_preparation_capture_v1.py`; the child reuses the
trusted standard-library self-sealing method. Before sealing, only fixed
bootstrap, source-as-data inspection and separate owned positive controls
run. Project imports require the real four-denial fence, a read-only hash-bound
code view and closed import finder. The sandbox profile's effective permissions
are unchanged from C. Its allow-default policy with four targeted denials is
not a universal process/Mach/IPC/fork confinement claim.

Both prospective generations preserve the same 28 source hashes and unchanged
conditional numerical criteria. The mandatory predecessor archive lacks the
four required integration/wrapper helper bodies. Current public copies were
not substituted. **PRIMARY_COMPOSITION is NOT_RUN**, including observer-count,
durable-prefix, numerical-equivalence and wrapper-composition qualification.
The dormant case helpers are not reachable from the child's admitted modes.

Every nested target is reserved before launch; failed spawn probes do not count
as started children. Counts are derived from captured records and fence results.
The task capture ceiling is 16 MiB. Individual control captures use stricter
bounds (normally 32 KiB; source-free description 128 KiB; outer report 1 MiB).
Resource helpers are separately recorded and do not conceal target fan-out.

The RSS wrapper samples the direct child PID and checks Darwin's completed-child
cohort peak. An observed over-budget process group is stopped. This is a sampled
1 GiB stop plus post-reap check, not an instantaneous kernel cap or a physical
read measurement. A partial spawn-record read can conservatively fail admission;
no fallback weakens the limit. The failed kernel setters' cause is unestablished.

No existing numerical, observer, result, checkpoint, authorization, accounting,
ledger, workflow or historical digest-guard byte is changed. Historical ledger
175 is unchanged. Historical missing counts remain unknown without backfill;
physical quantities remain not measurable. Secondary NumPy admission, native
reads/maps/unmaps, full-result/full-forward integration and broader F017 work
remain open. Hosted CI is a separate PASS/FAIL/PENDING claim; the inherited
bridge assertion failure is neither repaired nor relabeled by this doctor.
