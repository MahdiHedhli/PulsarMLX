# Secondary descriptor observation: finite private WIP

This is not F017 completion, live authority, or acceptance of the primary
owner-completeness path. No checkpoint or retained Event 06 data was accessed.

The independent branch starts at `6778925041ff71dd9b9613fb3f18bbd1198f6c3e`
(tree `836c2fdb2754c93379e4149ea41a49eca4d80f5d`). The final executable/test
candidate is `02a0113f5461ad273a43a1a18698eda9b5dc8086`
(tree `4dd72789a4a5fa7391e30704eb00d140d7095074`). Later documentation does not
change those measured execution bytes.

## Qualified scope

The active secondary wrapper now constructs the fixed secondary descriptor
source through an original-owner observation prefix. Encoded reads, ownership,
bounded append-only observations, receipts, terminals and independent readback
are exercised without importing NumPy or executing a numerical graph. The
full wrapper's numerical invocation was not executed by this qualification.

The unchanged published stdlib capture doctor passed. The same trusted
bootstrap and profile builder established actual synthetic read/write/network
denials before candidate imports. The capture ceiling remains its published
12 MiB, below this task's 16 MiB maximum. Sampled direct-child RSS observation
is not an instantaneous kernel memory cap or physical I/O accounting.

The final generation passed 38 cases in each of two fresh process/roots:
25 expected COMPLETE and 13 expected INCOMPLETE per root. Twelve mutation
families have restored controls; never-bound ownership is a separate refusal.
Unknown ownership or suffixes retain null totals, not complete zero totals.
Forty actual positional reads of tiny owned synthetic files across the final
two roots compare the direct baseline with the instrumented source, exactly
matching byte requests, order and returned bytes. These are not checkpoint
reads or measurements of physical storage traffic.

The final qualification result SHA-256 is
`41dfa4f440a579d244b2291ba2c5c9f678ef34fa368562b1be6a47e36a0c310a`.
The data-only audit independently checks all 256 preserved case-prefix records
across the original, supplementary and repaired runs; they are not 256 unique
test scenarios. Actual target starts total 35, including nested children.

Generation 2 was declared after Generation 1 observations and before its own
outputs. A stricter counter-mutation test initially selected the active rather
than inactive purpose row. That failed run and its 30 passing case prefix remain
retained; the repaired test verifies the originally intended causal rejection.
No launch failure or assertion failure is counted as mutation detection.

## Source disposition and limits

The historical 43-view inventory has 27 exact matches against this current
base, not the earlier historical count of 30. Three primary files differ from
those older views and retain their current accepted base bytes. Four secondary
items have explicit prospective successor paths. Nine historical helpers have
UNKNOWN original repository placement and are retired only for this named
descriptor-prefix scope; broader historical numerical coverage is not retired.

The fixed secondary measurement census is not a live registry or package.
The primary production observer, historical V10 modules, numerical cores,
scientific/result/stage/stop/accounting contracts and CI workflows are unchanged.
Static comparison finds the numerical decode tail identical; this is not
runtime NumPy or full-result numerical equivalence.

| Finish-line item | Current disposition |
| --- | --- |
| Integrated source custody | Useful candidate committed locally; the complete bundle requires the exact public base dependency. Actual private publication and readback are bound separately by the terminal custody record. |
| Read-operation counters | Finite secondary descriptor cases pass; physical per-shard faults/bytes are not measurable here. |
| Durable receipts/terminals | Original-owner and readback agreement passes for the finite secondary prefix; no live package closure claim. |
| Independent count/numerical qualification | Count/transcript qualification passes; NumPy decoding and full numerical equivalence remain NOT_QUALIFIED. |
| Full call-path composition | Descriptor factory-to-prefix composition passes; full wrapper, result banking, forward graph and native paths remain NOT_QUALIFIED. |
| Reviewable publication | External source review and public push are held by PRIVACY_SCANNER_BINDING_UNRESOLVED. No independent code ACCEPT or green CI is claimed. |

The old primary `F017-UNBOUND-OWNER-COMPLETE-ZERO` issue remains open. The
private primary owner-fix was not imported, reviewed or published. The parked
provider-support case and its original cause remain unresolved and non-gating
for this independent work. No source reviewer or provider request was retried.
No public branch push, CI dispatch/rerun, Event 06 GO/replay or P1 authority was
performed. Historical ledger 175 is carried unchanged; missing Sequence 43
counts remain UNKNOWN_NO_BACKFILL. Parent retains integration and feature
closeout acceptance.
