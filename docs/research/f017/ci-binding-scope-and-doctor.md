# CI binding inventory and source-free doctor: unqualified WIP

Status: **HARNESS_NOT_READY**. This checkpoint is not runtime qualification,
CI acceptance, checkpoint authority, or permission to run a dependent sequence.
The existing workflow and every historical/scientific/runtime body are unchanged.

The proposed reusable entry is:

```text
python3 -I -S -B scripts/ci/f017_source_free_doctor_v1.py --check
```

Do not treat the existence of that command as admission. It must return
`HARNESS_READY` in the actual environment before dependent qualification.
This revision did not do so. No further launch is authorized by this report.

## Observed partial result

Seven fresh doctor attempts consumed six tooling corrections and 54 actual
target starts, including their supervisors. The independent capture controls
distinguished ordinary exit 134, signal 6, timeout and failed spawn. The sealed
read/write/network denial controls passed on the later attempts. The necessary
NumPy import did not complete: the last attempt reached the installed extension,
but dyld reported the OpenBLAS library blocked by the sandbox. The remaining
mechanism is unestablished. Passing denial controls do not establish dependency
admission or a ready doctor.

The observed interpreter/dependency versions were CPython 3.14.7 and installed
NumPy 2.4.5. This is a dependency-import probe, not numerical graph execution or
native instrumentation qualification. No native compile/link work was performed.
The source-free child retains fixed `S54-B01` bootstrap selectors as internal
compatibility identifiers; these attempts did not relaunch Sequence 54.

The final profile, dependency census, pre-observation candidate hashes, child
bodies, command arguments, raw streams, statuses and correction records are
retained in private evidence. Earlier launcher bodies are not all retained;
their pre-observation hashes are. Unobserved fields are not reconstructed.
The invalid-entry probe and post-success dependency recensus were not reached.
Two successful fresh roots, dependent qualifiers and mutation qualification
remain missing. The tooling budget is exhausted; no additional repair or
permission widening was performed after that stop.

## Static binding inventory

At source head `7add0b28c1d739052ffcd45821d4233f368fd6d0`, the workflow references
98 distinct F017 Python roots. Static imports and tracked source literals reach
272 source files. All 272 were inventoried as data; none of the research modules
was executed by the inventory. Its complete path/hash/dependency inventory is
retained for the next authorized task.

The 32 explicit repository-path-to-digest dictionary pairs were classified:

| Guard scope | Pairs | Static disposition |
| --- | ---: | --- |
| Historical evidence | 11 | Seven V6 historical-authority pairs and four call-path test pairs use named historical Git objects. |
| Both historical and current | 10 | Eight current tombstone-integrity checks preserve retired authority; two V12 generator checks preserve V10/V11 bodies. |
| Current qualification | 11 | Six bridge pins and five frozen-authority amendment pins are checked in the current checkout. |

Of four raw current-tree differences, three are correct historical-context
bindings. Their named historical object bytes match the expected digests.
The remaining bridge builder difference is:

- Consumer: `scripts/research/qualify_f017_event06_numerical_bridge_v1.py`
- Input: `scripts/research/f017_result_bundle_builder_v11.py`
- Expected: `296fc64befb92fc47db4458d19df444a645bfd62e269d911e9c4a46b9773d145`
- Current: `8853623824f4546e419c1b51766cf1cb39f2b8a2b61e59e25f25dba89c01c70b`

This establishes an inherited static mismatch, not a newly reproduced runtime
failure or recovery of the redirected result JSON from CI run 34339744391.
That historical JSON value remains **UNKNOWN**. The old builder must not be
restored: later accounting changes are outside this task's repair scope.

This is not a complete semantic coverage proof. Lexical discovery identified
201 potential guard-bearing files and 361 artifact-reference edges whose full
guard contexts remain unresolved. Symbolic/indirect paths and dynamic consumers
are not waived. No inventory omission gate, current-scope adapter, historical
closure execution, or current binding mutation campaign was implemented.

## Boundaries

No checkpoint or retained Event 06 data was accessed. No Event 06 replay, live
GO, P1 authority, registry/package write, local native build, or other-machine
activity occurred. Historical ledger remains 175. Missing Sequence 43
observations remain `UNKNOWN_NO_BACKFILL`; per-shard physical counts remain
`NOT_MEASURABLE` where unsupported.

Independent review of this checkpoint is solely for safe WIP source publication,
not functional admission. Hosted CI, if triggered by publication, remains a
separate ordinary observation; no earlier failed run is retried. The minimum
missing claim is successful source-free dependency admission under the confined
doctor before any dependent runtime test. Return to planner control; do not
launch an automatic successor.
