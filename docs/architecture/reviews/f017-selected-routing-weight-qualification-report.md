# F017 Selected-Routing-Weight Qualification Report

The frozen acceptance rule was applied without modification to the eight
ID-keyed intervals banked by the route-ambiguity evaluation. The selected set
remains mathematically invariant: all 1,984 membership inequalities pass.

Joint normalization passes with the denominator floor inactive over the whole
box. Its outward weight-sum enclosure is
`[2.4999999999999996, 2.5000000000000004]` and contains the nominal sum.

Every selected expert fails the frozen per-ID mathematical budget
`rho_i <= 1e-5`; consequently every expert also fails engineering H=2
`rho_i <= 5e-6`. The largest radius is
`0.0002725883543786001` for expert `250`.

The resulting disposition is:

`ROUTE NOT PROVEN INVARIANT`

This result does not invalidate the selected-set membership theorem. It means
the accepted route contract's required routing-weight qualification is not
established under its pre-frozen budget. No threshold, v3.1 theorem, ambiguity
box, historical evidence, checkpoint state, or payload ledger was changed.
