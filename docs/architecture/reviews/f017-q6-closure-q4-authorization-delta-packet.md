# F017 Q6_K Closure + Q4_K Authorization Delta Packet

This narrow packet reviews only the Q6_K decoder-lineage closure and the prepared Q4_K one-payload gate. It does not reopen the accepted dense-prefix planning package and does not authorize real access.

Please verify:

1. The minimized fixture proves the old Q6_K lane interpretation wrong.
2. The corrected indexing matches pinned `dequantize_row_q6_K` semantics.
3. Upstream commit/tree/file/function identities are complete.
4. A/B/C implementations are sufficiently independent and share no generated truth.
5. Accepted F017 evidence has no defective-Q6 numerical dependency.
6. The F016 annotation distinguishes self-consistency from absolute decoder truth.
7. Historical artifacts and dispositions remain immutable.
8. `59d97b1` is correctly marked absent/non-authoritative and `304c651e1dbf2b6d94937f4e9fd6f8a24d2c620a` is the reviewed starting head.
9. The future Q6_K package binds the corrected lineage and remains non-authorized.
10. The Q4_K target and three-way exact-byte package are sufficient for exactly one real payload.
11. Q4_K execution cannot invoke embedding, MLX, or model compute.
12. The exact next ledger transition is `57 -> 58` only after a successful future event.
13. Q6_K and dense-prefix automatic continuation are impossible.
14. Dense-prefix numerical semantics did not change.

Requested verdict: `GO FOR ONE Q4_K REAL-BYTE QUALIFICATION`, `GO WITH REQUIRED FIXES`, or `NO-GO`.

The review itself does not execute or authorize the payload; the prepared binding requires a separate operator execution instruction.
