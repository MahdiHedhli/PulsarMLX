# F017 checkpoint identity lifecycle V7 — Opus design review cycle 04

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `8479b862` in a detached read-only worktree. Do not modify files or access original checkpoint shards.

Reconstruct cycles 1–3 and attack the repaired failure-specific evidence, identity cleanup, artifact back-reference closure, post-claim package failure, descriptor activation failure, all pre-install failure boundaries, exact coordinator lease ownership, unified artifact namespace, validator regression guards, freeze posture, continuity-failure evidence, exact six-shard loop guard, nested counts, and V6 revocation anchoring. Required verdict: `ACCEPT_CHECKPOINT_IDENTITY_LIFECYCLE_V7_FOR_IMPLEMENTATION` or `REJECT`. Any `BLOCKING` or `NON_BLOCKING_REQUIRED` finding prevents implementation entry.
