# F017 Event-04 runtime hardening whole-domain review — Gemini cycle 01

Use a fresh `gemini-3.1-pro-high` AGY session at high effort. Review the exact committed implementation bytes at `ee5266bfe702648f91f273ea3f59e4b6be61ae36` and its evidence descendants. The exact-head FULL_NATIVE run for that head must be terminal PASS before acceptance. Repository evidence outranks this request. Do not modify files, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Independently reconstruct the accepted V8 milestone and all twelve forward findings DID-01 through DID-12. Attack idempotent release—including close-event banking failure after a successful close, partial release, retry policy, EBADF, and double-close—dynamic durable-start accounting, exact transition-derived failure classification, every post-install/post-start terminalization edge, emergency banking authority, stale authorizer documentation, structural synthetic-root isolation, generator `--check`, deterministic core/envelope separation, enforced memory gates, import-separated malformed-descriptor checks, historical worktree cleanup, and all 47 runtime failure outcomes.

Treat execution instantiability as load-bearing. Inspect actual code rather than the qualification counters. Prove whether the installed candidate model can represent a future operator-authorized live production Event 04; whether the authorizer can perform candidate validation and canonical no-replace live installation under a future GO; whether the coordinator has a production target entry point; and whether primary and secondary production target sources can stream the accepted 238 GB production checkpoint exclusively through all five inherited descriptors without graph-path reopen. The current authorizer describes itself as rehearsal-only, the parser admits rehearsal/shadow postures, the coordinator exposes `execute_synthetic`, and the target sources accept a synthetic descriptor catalog and eagerly materialize tensors. Determine whether those facts contradict `EXECUTION_AUTHORIZATION` readiness even though synthetic five-shard consumption and the metadata-only 1,410/399 plan pass.

Also determine whether `f017_runtime_outcome_realizer_v9.py` actually drives runtime transitions and production operations or merely re-banks the symbolic DAG's declared required set, and whether the active-generation registry truthfully remains `NONE` before whole-domain acceptance. Recompute both consumers' shard-use census, zero path reopen, all 1,410 graph tensor mappings, all 399 non-access denials, all 11 formats, 50 success packages, 201 failure executions, accounting/release/multishard mutations, numerical V3 invariants, production-shaped no-access rehearsal, exact Git measurement, and CI worktree cleanup.

List every material finding exactly once as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`, with a stable ID and smallest correct repair. State material disagreement explicitly. End with exactly one unconditional verdict: `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION` or `REJECT`.
