# F017 Event-04 runtime hardening whole-domain review — Gemini cycle 02

Use a fresh `gemini-3.1-pro-high` AGY session at high effort. Review the exact pushed bytes at evidence head `05876a3bdfec625ec9c72d771e26679bf6a1af45`; the load-bearing implementation is measured at `fd517315f3e289d7abceb9ef73a793074d879fdf`, tree `afb208452ebe35eceba7100899454ec1354b9f36`. Exact-head FULL_NATIVE run `32809028957` passed both workspace and pinned native MLX jobs with required skips 0. Repository evidence outranks this request. Do not modify files, access original checkpoint payloads, mint or execute Event 04, or execute P1 attempt 2.

Reconstruct cycle-01 response SHA-256 `4863a70c93f7b999b369238e40936f7769fde270e895dfae737f22635d70b24b` and independently retest every cycle-01 finding. In particular:

1. `ACTIVE_GENERATION_NOT_NONE`: confirm the committed registry is `NONE`, names V9 only as activation candidate, and cannot activate before final whole-domain acceptance plus a fresh operator GO.
2. `TEST_ONLY_REALIZER_SPOOFING`: inspect `f017_runtime_outcome_realizer_v9.py` and prove whether it now drives actual rehearsal authorizer/install/coordinator transitions, interrupts real append-only banks at the exact causal rank, and validates actual required/forbidden artifacts across 47 outcomes and 201 executions, rather than constructing the declared required set.
3. `AUTHORIZER_LIVE_REJECTION`: inspect the separate future-live candidate posture, fresh enforced mint memory gate, operator-approval/readiness bindings, dual validation, canonical no-replace installation, exact readback, and installation receipt. Confirm candidate bytes remain non-authoritative and no Event-04 authority was created in qualification.
4. `MISSING_PRODUCTION_COORDINATOR_ENTRY`: inspect `execute_event04`, installed-authority revalidation, enforced package-start memory gate, production identity acquisition, and prohibition of test fault controls.
5. `EAGER_TENSOR_MATERIALIZATION_OOM`: inspect both V9 target sources. Prove that vectors and one matrix row are descriptor-relative `pread` decoded on demand; complete model tensors are not materialized; primary and secondary run in independent subprocesses with all five inherited file descriptors; neither target source has a graph-path open primitive; and both synthetic executions materially consume shards 2–6.
6. `IDEMPOTENT_RELEASE_BANKING_FAILURE`: rerun the close-success/evidence-bank-failure regression and confirm recovery banks pending evidence without closing again, all unaffected leases continue, and final pending/live censuses are exact.
7. `INEXACT_FAILURE_CLASSIFICATION`: verify `ModeledTransitionFailure`, exact outcome/transition lookup, durable-start-derived accounting, causal-rank injection during identity acquisition, and zero generic fallbacks for modeled failures.
8. `STALE_AUTHORIZER_DOCUMENTATION`: verify the current two-phase authorizer documentation and behavior agree.

Also attack new implementation risks: descriptor inheritance across subprocess `pass_fds`; descriptor identity revalidation before every `pread`; production tensor offset+length bounds against committed shard sizes; synthetic/production scope separation; no live identity markers; candidate/install exact bytes; emergency terminalization beginning at installed-auth revalidation; package terminal evidence semantics; generator `--check`; deterministic core/envelope split; C7-N2 independent validation; guaranteed historical worktree cleanup; numerical core and decoder SHAs unchanged; and zero original checkpoint access.

Do not recommend retries, resume, multi-attempt behavior, or dynamically scaled retries: the frozen authority requires attempts 1, retries 0, resume false. List every material finding exactly once as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`, with stable ID and smallest correct repair. State material disagreement explicitly. End with exactly one unconditional verdict: `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION` or `REJECT`.
