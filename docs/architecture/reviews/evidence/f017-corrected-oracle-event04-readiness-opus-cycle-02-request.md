# F017 Event-04 Readiness Whole-Domain Opus Review Cycle 02

Use a fresh `claude-opus-5` session at high effort. Review exact committed bytes at evidence head `0c09e8ecc9f4002d5a29b4bc7765441ede87de9b`, with measured implementation head `12d2b91671368af89ca1367bd959b3f142cc58c1`, in a clean detached read-only worktree. Git and committed evidence outrank this request. Do not modify the repository, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Cycle 01 returned one BLOCKING finding (`C1-B-1`) and one defense-in-depth finding (`C1-D-1`). Reproduce both and attack the repairs. Production authority must pin measurement manifest v3, whose SHA-256 is `88c4d91cc134d39ff2c6fc32d79bbb1da5d17809e7c94a929c28c048a60895ed`, and the manifest must attest implementation head `12d2b91671368af89ca1367bd959b3f142cc58c1`. Verify production validation parses the exact manifest, freezes its complete key and 61-entry censuses, requires branch/head/result/schema, rejects duplicates/path escape, compares every current load-bearing file to both SHA-256 and Git-blob identity, checks the model's exact required-entry census, and requires the authorization's `implementation_measurement_head` to equal the manifest. Attempt stale-v1/v2 substitution, arbitrary declared head, missing entry, changed entry, changed bytes, forged blob, path escape, duplicate entry, and an unmeasured current source edit. Verify future production authority can bind v3 and cannot bind v1/v2.

Verify `execute_f017_corrected_oracle_event_v6.py` now calls `require_active` unconditionally, so both production and synthetic qualification obey the active-generation registry. Disable the synthetic generation in a controlled copy and confirm execution fails before state creation or checkpoint access.

Verify exact-head FULL_NATIVE run `32689603653` at `12d2b91671368af89ca1367bd959b3f142cc58c1` passed both native jobs with required native skips 0. Verify evidence packet run `32690346149` at the reviewed head passed as EVIDENCE_ONLY with native jobs 0. Verify rehearsal v5 is bound to the new implementation head, uses manifest v3, and reports state created false, checkpoint opens 0, payload reads 0, numerical operations 0, and no Event-04 authority/execution.

Then re-audit the whole accepted numerical-capability and lifecycle closure from Cycle 01: representation-independent capability policy and 187 mutations; unchanged pure cores/formulas/thresholds; numerical contract and requalification v3; historical tombstones; V6 states/transitions/outcomes/accounting/paths/serialization; exact registry/matrix/model anchors; independent validator; operator-GO gate; two-phase install and handshake; receipts/terminals; conditional unstarted consumers; active generation V6; synthetic qualification; ledger 175; Event 02/03 immutability; no original checkpoint access; no Event-04 approval/authorization/execution; and no P1 attempt 2.

Use `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required prevent acceptance. Return exactly `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_AUTHORIZATION_PREPARATION` or `REJECT`; no conditional acceptance.
