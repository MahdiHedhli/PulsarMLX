# F017 lifecycle V8 causal design — Opus design review cycle 03

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `559fec05` in a detached read-only worktree. Repository evidence outranks this request. Do not modify files, access original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Reconstruct cycles 01 and 02 and Gemini cycle 04. Cycle 02 found missing payload semantics and recursive multi-file failure tails. The repair replaces every failure tail with one atomic terminal capsule whose partial write is non-authoritative and whose recursion terminator is process-exit descriptor closure; all 47 success-prefix failure variants now add exactly one durable artifact. Lease inception is the successful graph-payload access event, and retained ordinals are independently derived from actual DAG ranks. Every payload key has an independently validated rule; critical constants, result fields, shard metadata, observed/expected equality, lease-manifest equality, accounting, active generation, Event-04 status, and original-access status are enforced by the verifier. Cleanup duplicate/unknown counts are nonnegative recordable observations with closure accounting, not fixed zero constants.

Run validator, six tests, 48-outcome constructor, and consistently rehashed negative probes. Attack atomicity semantics, tail insertion, all critical payload constants, dynamic payload rules, shard receipts, lease inception/rank movement, cleanup anomalies, actor/root/key drift, result fields, and root-authority removal. Report `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH`. Required verdict: `ACCEPT_CHECKPOINT_IDENTITY_CAUSAL_DESIGN_V8_FOR_IMPLEMENTATION` or `REJECT`.
