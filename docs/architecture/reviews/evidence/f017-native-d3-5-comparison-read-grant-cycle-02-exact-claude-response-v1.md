Review complete. The cycle-1 BLOCKING defect is genuinely closed and nearly everything holds, but one cycle-1 required repair (the D0 vocabulary fix, finding 003) is incomplete: the grader now emits exact registry **oracle** labels, yet on required ordinals 20/21 it emits the metric label `operand_conditioned_matvec_all_eight_experts`, which is not defined in the frozen D0 `metric_profiles` (only `operand_conditioned_matvec` is) — and the new mutation test enshrines the non-registry label. Under the acceptance rule this forces REJECT.

```json
{
  "reviewer_model": "claude-fable-5",
  "reviewer_track": "INDEPENDENT_ADVERSARIAL_COMPARISON_AUTHORITY_REVIEWER",
  "reviewed_branch": "feat/017-rust-native-inference-runtime",
  "reviewed_head": "d11054eb67a8e193a9d84dd663f90aa028e97131",
  "detached_read_only_worktree": "/private/tmp/f017-grant-review-c2.zhA3vn/repo (detached HEAD at d11054eb, clean before and after review; directory mounted read-only; no repository bytes modified; all cargo build output redirected to /tmp/f017-c2-target outside the worktree)",
  "reviewed_artifact_hashes": {
    "grant": {"path": "specs/017-rust-native-inference-runtime/contracts/f017-native-d3-5-comparison-read-grant-v1.json", "sha256": "bcf136d13dc702b08b40eb44b92222e83d956ba8520b7c069aa927ba2abdeedc", "recomputed_match": true},
    "stage_mapping": {"path": "specs/017-rust-native-inference-runtime/contracts/f017-native-d3-5-canonical-stage-mapping-v1.json", "sha256": "9f8bb8b0b65188fd2377521c79655c82842063f870a30aeeaea97e0483cd74c5", "recomputed_match": true},
    "consumer": {"path": "crates/f017-native/src/bin/d35_grader.rs", "sha256": "e446ab7edea7299e9e92e94791d08bd318577a588af7b9eb55045b8adef868f7", "recomputed_match": true},
    "builder": {"path": "scripts/research/build_f017_native_d35_comparison_grant_v1.py", "sha256": "8cf25967c094eb438935e7bc79905cc5a34d8d65960d7360e034a3abe73b8a96", "recomputed_match": true},
    "validator": {"path": "scripts/research/validate_f017_native_d35_comparison_grant_v1.py", "sha256": "25d637cb669a3154b2d4766cae36c6c10a470d2c33d555a9a0cc2dffbc976de0", "recomputed_match": true},
    "mutation_tests": {"path": "scripts/research/tests/test_f017_native_d35_comparison_grant_v1.py", "sha256": "c87fb710aef352d8adbe52223241e407c6b9708735a43770e7a1f8a7c6d467a4", "recomputed_match": true},
    "d0": {"path": "specs/017-rust-native-inference-runtime/contracts/f017-native-bounded-p1-numeric-acceptance-contract-v2.json", "sha256": "cc62cdc7550e3a25f55de783e9eb7c68f6cf03d0eafb944a86dc8a2a60007fb9", "recomputed_match": true},
    "d3_5_evidence": {"path": "docs/architecture/reviews/evidence/f017-native-retained-qualification-execution-evidence-v1.json", "sha256": "13b1a3a653cf0325f59b0b3b035b7804439a19c000ef8ddf19dad9ecb8316ac8", "recomputed_match": true},
    "cycle_01_response": {"path": "docs/architecture/reviews/evidence/f017-native-d3-5-comparison-read-grant-cycle-01-exact-claude-response-v1.md", "sha256": "400c32bdf3365aa34d6d1a646a90acbf0e3340b38724e35cd8763fb8e3964460", "recomputed_match": true}
  },
  "tests_performed": [
    "Recomputed SHA-256 of all 9 request-listed artifacts at HEAD d11054eb; all match; HEAD is the tip of feat/017-rust-native-inference-runtime (local and origin).",
    "Cycle-1 finding rerun F017-D35-GRANT-001 (BLOCKING): builder constant at build_f017_native_d35_comparison_grant_v1.py:156 now reads f38dc275b86799712725765cec489089a4a4db50; git cat-file confirms that commit exists and the old f38dc2756bd4... does not; all 34 regenerated capture rows cite the real banking commit with the banked evidence file as source authority; the validator now resolves every (source_commit, source_authority_path) of all 89 rows via git show and hash-compares (I confirmed a nonexistent commit now raises 'unresolved source authority'); mutation class 'capture_commit' pins the exact cycle-1 wrong hash. CLOSED.",
    "Cycle-1 rerun F017-D35-VALIDATOR-002: validator lines 100-113 recompute the machine-local capture-manifest sha and require equality with both the evidence pin and the stage-mapping pin (5ad1e8f4...), and compare per-row dtype/shape/bytes/sha against the pinned manifest; new test_capture_manifest_hash_is_committed_authority mutation passes. CLOSED.",
    "Cycle-1 rerun F017-D35-GRADER-003: oracle labels now resolve exactly (RETAINED_CANONICAL_S0/S1, RETAINED_CANONICAL_ROUTER_NORMALIZED, ACCEPTED_ROUTE_AUTHORITY, INDEPENDENT_COMPLETE_EXPERT, RETAINED_PROOF_REFERENCE_*, RETAINED_CANONICAL_SHARED_F32, and the four *_BOUNDARY_CORRECTNESS labels) against the D0-v2 oracle_registry — but the metric label on required ordinals 20/21 is 'operand_conditioned_matvec_all_eight_experts', which exists nowhere in the frozen contracts (grep over the whole tree matches only the grader and its own test). PARTIALLY CLOSED — see finding 008.",
    "Cycle-1 rerun F017-D35-GRADER-004: explicit interval gate on captured routing weights (finite, [0,1]) is implemented and conjoined with membership (selected_ids byte equality), order (ranking sha equality), and tie-semantics gates before the row can pass. CLOSED.",
    "Cycle-1 rerun F017-D35-GRADER-005: dead 'tail*0.0' term removed; effective cap is exactly 2*gamma(2n)*sum|f64(w)*f64(x)| + 4n*2^-149; signed_zero_compatible bit check implemented and enforced in metric_row for all tier_b rows. CLOSED as specified (OCB-row signed-zero residual noted in finding 011).",
    "Cycle-1 rerun F017-D35-GRADER-006: grader now pins GRANT_ID, MAPPING_SHA256, all five route-authority constants (which I verified equal the D0-v2 ACCEPTED_ROUTE_AUTHORITY registry entry), and OUTPUT_ROOT as compile-time constants, and resolves ${REPOSITORY_ROOT} structurally from current_exe ancestors containing .git instead of cwd. CLOSED as specified (read-budget residual noted in finding 010).",
    "Cycle-1 rerun F017-D35-VALIDATOR-007: retention-reuse grant sha pinned (b22a11c8...), allowed_output_root exact-value checked, per-row source authorities verified via git show. MOSTLY CLOSED (serialization vocabulary still unnormalized; noted in finding 012).",
    "Grant structural audit: 15+40+34=89 reads, contiguous ordinals 0..88, unique roles, no checkpoint substring in any path, all paths under ${HOME}/.local/share/pulsarmlx/f017/, counts exact; the only duplicated path is the s0 bytes appearing as expected.input_hidden and operand.s0, both receipted.",
    "Registry binding audit: the grant's expected_reads sha256s for input_hidden/post_attention_residual/router_normalized/routed_aggregate/shared_expert_output/production_ffn/production_s2 are byte-equal to the seven D0-v2 oracle_registry expected_sha256 pins; capture.ranking and capture.selected_ids sha256s equal the ACCEPTED_ROUTE_AUTHORITY registry hashes; the 8 remaining expected reads are the disclosed per-expert down references, all bound to the hash-pinned disclosure by the validator.",
    "34-stage census attack: the 34 capture rows and the 34 stage-mapping rows align 1:1 in D0 v1 stage order; only ordinal 0 is RETAINED_DIRECT_VALUE; the grader emits exactly 34 rows with contiguous ordinals, classes drawn from the frozen v1 class list, and the required set {0,12,13,17,18,19,20,21,25,26,27,28,31,32,33} exactly equals the effective v1+ordinal-13-override BYTE_EXACT/NUMERICALLY_BOUNDED/STRUCTURAL_EXACT/INTENTIONALLY_DISTINCT rows; the 19 IMPLEMENTATION_SPECIFIC_REPRODUCIBILITY rows are covered by the banked 20/20 byte-identical determinism evidence and are not reinterpreted as correctness.",
    "Eight-expert OCB coverage attack: capture.routed_gate and capture.routed_up are [8,2048] (65536 bytes) and capture.routed_down_outputs is [8,6144] (196608 bytes); the grader decodes all eight per-expert gate/up/down operand matrices (IQ2_XXS/IQ3_XXS), computes f64 references and per-coordinate caps for all 8*2048 gate/up coordinates and all 8*6144 down coordinates from decoded operands and the retained canonical input only (never from captured native output or observed error), and enforces every coordinate against its cap; per-coordinate enforcement mathematically implies the profile's RMSE cap and cosine minimum.",
    "Consumer enforcement audit (not validator prose): grant policy predicate, executable path+sha self-check, source sha check, local authority hashes, historical ledger via git show at the pinned commit, per-row committed source-authority resolution, secure_read with lstat regular-file/non-symlink/nlink==1/read-only/size gates, O_NOFOLLOW, dev/ino identity across lstat->open->after, and EXPECTED=BEFORE=CONSUMED=AFTER sha equality on a single descriptor with byte-level readback comparison; single pass over exactly the grant's rows; output confined to the pinned root via create_dir + O_CREAT|O_EXCL 0400 with fsync, giving exactly one grading-result.json plus one terminal.json per event and no retry/resume path; no ledger writes and no checkpoint path reachable ('checkpoint' substring rejected).",
    "parse_json_no_duplicates review: rejects duplicate keys at any depth, trailing data, and non-finite numbers; unit test present and passing.",
    "python3 validate_f017_native_d35_comparison_grant_v1.py (module API, resolve_executable=False) => PASS {read_census:89, payload_reads_during_validation:0, original_checkpoint_reads:0, grant_sha256:bcf136d1...}; this run machine-verified the manifest pin, all 89 git-show source authorities, disclosure/operand/capture bindings, output-root exact value, and output-root absence (grading event not yet executed); the full default run fails only on the absent uncommitted target/release binary in this read-only worktree (environmental).",
    "python3 -m unittest of the committed suite: all 20 mutation classes rejected (ok), capture-manifest pin attack ok, grader vocabulary source attack ok; test_baseline errors only on the absent built binary (environmental).",
    "14 additional live mutations beyond the committed suite: stage-mapping sha, output-root value, expected sha, unbound path, capture byte_count/shape, operand dtype, s0 swap, census undercount, event regeneration/terminal-semantics/branch mutations all rejected; route_authority.ranking_sha256 mutation is not caught by the validator but is rejected by the grader's compile-time constant (finding 012); a wrong-but-real commit containing byte-identical authority passes (content binding preserved — inherent limit of content-addressed provenance).",
    "Reproducible-build attack: CARGO_TARGET_DIR=/tmp/f017-c2-target cargo build --release -p f017-native --bin f017-native-d35-grader produced a binary with SHA-256 bbf84851b8d4bcedd7ff526e753b113fac585621d90fe05a4660d88f5ad975cb — byte-identical to the grant's executable_sha256, closing source/executable substitution.",
    "cargo test -p f017-native --no-run => compiles; cargo test -p f017-native --lib => 3/3 pass (duplicate-key, closed-census, duplicate-read/readback enforcer tests).",
    "Route authority: decoded selected_ids (250,10,237,62,73,177,218,28) and 8 f64 weights all finite in (0.229,0.749) summing 2.5; ids/weights hex hash-match their sha256 pins and the D0-v2 registry entry; weights are not weight-descending but ordering is byte-pinned to the frozen score ranking authority, so no contradiction.",
    "Historical ledger: git show at f2a7aa38 hashes to aa98f5cc... with receipt_chain.terminal_count exactly 175; no advance. Disclosure: 14 rows of path+sha256 only, no metric values, d3_5_event_affected=false; diagnostic_metrics_reusable=false enforced by both grader and validator; the 15 expected reads are exactly the 14 disclosed paths plus the previously granted s0.",
    "Execution prohibitions honored: 0 comparison payload reads (only committed text/JSON and the metadata-only capture manifest were hashed), 0 D3.5 numerical executions, 0 original checkpoint reads, 0 real/live P1 executions or authorizations, grader never invoked, output root untouched and confirmed absent, git status clean after review."
  ],
  "findings": [
    {
      "id": "F017-D35-GRADER-008",
      "severity": "NON_BLOCKING_REQUIRED",
      "path": "crates/f017-native/src/bin/d35_grader.rs:799,817; scripts/research/tests/test_f017_native_d35_comparison_grant_v1.py:67",
      "evidence": "Required ordinals 20 (routed_gate) and 21 (routed_up) emit metric \"operand_conditioned_matvec_all_eight_experts\". The frozen D0 v1 metric_profiles registry defines only \"operand_conditioned_matvec\" (which the same grader correctly emits on ordinals 25/27/28, including ordinal 25 which also covers all eight experts), and the D0-v2 overlay adds no metric profiles. Grep over the entire tree shows the label exists only in the grader and in the mutation test, which asserts its presence — enshrining the non-registry label. Cycle-1 finding F017-D35-GRADER-003's required repair stated verbatim that these rows must emit 'INDEPENDENT_COMPLETE_EXPERT with the operand_conditioned_matvec metric'; the oracle half was repaired, the metric half was not.",
      "failure_mode": "The future acceptance artifact's metric attribution on 2 of 15 required ordinals cannot be mechanically resolved against the frozen D0 metric_profiles — the same attribution-resolution failure mode as the cycle-1 NON_BLOCKING_REQUIRED finding 003, meaning that required repair is incomplete; the consumer unilaterally extends frozen contract vocabulary, which the D0-v2 F2 repair regime exists to prevent.",
      "required_repair": "Emit metric \"operand_conditioned_matvec\" on ordinals 20 and 21 (eight-expert coverage is already evidenced structurally by the [8,2048] geometry checks and may be recorded in a non-vocabulary field), update the mutation test to assert the registry label instead of the deviant one, rebuild, regenerate the grant with the new source/executable hashes, and resubmit for a fresh independent review cycle."
    },
    {
      "id": "F017-D35-GRADER-009",
      "severity": "DEFENSE_IN_DEPTH",
      "path": "crates/f017-native/src/bin/d35_grader.rs:386-390,916,942,955,1063,1079",
      "evidence": "D0 v1 retained_qualification.expected_result_vocabulary is [BYTE_EQUIVALENT, NUMERICALLY_EQUIVALENT_WITHIN_FROZEN_TOLERANCE, INTENTIONALLY_DISTINCT, FAILED_CONTRACT]. The grader emits \"FAILED_FROZEN_CONTRACT\", \"INTENTIONALLY_DISTINCT_WITHIN_DIAGNOSTIC_BOUND\", \"STRUCTURALLY_EQUIVALENT_NUMERICALLY_BOUNDED\", \"IMPLEMENTATION_REPRODUCIBLE\", and headline \"MIXED_D0_V2_CLASS/PASS\" — none in that list. The frozen vocabulary is itself incoherent with the v1 stage table (it offers no value for the 19 IMPLEMENTATION_SPECIFIC_REPRODUCIBILITY rows or the STRUCTURAL_EXACT_NUMERIC_BOUNDED row), so full conformance is impossible without a vocabulary extension; the machine-readable pass/fail semantics live in the separate boolean fields and are unaffected.",
      "failure_mode": "Post-hoc mechanical mapping of per-row and headline result strings to the declared D0 result vocabulary fails, weakening audit of the acceptance artifact even though pass/fail outcomes are unambiguous.",
      "required_repair": "Align the avoidable values (FAILED_CONTRACT; INTENTIONALLY_DISTINCT on ordinals 26/32/33) and register the remaining result values through the append-only D0 revision mechanism rather than ad-hoc strings."
    },
    {
      "id": "F017-D35-GRADER-010",
      "severity": "DEFENSE_IN_DEPTH",
      "path": "crates/f017-native/src/bin/d35_grader.rs:527-531,574-615",
      "evidence": "The grader checks that the three count fields equal the respective array lengths but does not pin them to 15/40/34, and although it hash-pins the D0 and stage mapping it never parses them, so it does not enforce the D0-v2 registry expected_sha256 pins against the grant's expected rows nor the mapping's manifest pin against the capture rows. A substitute grant satisfying every pinned constant could add extra receipted reads (any ${HOME} file whose sha it declares, with any committed file cited as source authority), expanding the read budget; detection is only post-hoc via the recorded grant_sha256 versus the accepted grant.",
      "failure_mode": "The runtime read budget and the expected-artifact bindings rest on the procedural acceptance of the grant hash rather than on non-circular constants the binary could enforce directly.",
      "required_repair": "Pin the 15/40/34/89 counts as constants and parse the already-hash-pinned D0/mapping to enforce registry expected_sha256 values and the capture-manifest pin inside the consumer."
    },
    {
      "id": "F017-D35-GRADER-011",
      "severity": "DEFENSE_IN_DEPTH",
      "path": "crates/f017-native/src/bin/d35_grader.rs:784-793,843-847,881-885",
      "evidence": "The operand_conditioned_matvec profile declares signed_zero EXACT_BITS, but the OCB pass computations for ordinals 20/21/25/27/28 check only |actual-expected| <= cap; the signed_zero_compatible bit check is applied only in metric_row (tier_b and routing rows). A +0.0/-0.0 pair on an OCB row passes via zero magnitude difference. No effect on any magnitude-based outcome.",
      "failure_mode": "A declared structural gate of the frozen OCB profile is unenforced on the five OCB rows.",
      "required_repair": "Apply the signed-zero bit check to the OCB coordinate comparisons."
    },
    {
      "id": "F017-D35-VALIDATOR-012",
      "severity": "DEFENSE_IN_DEPTH",
      "path": "scripts/research/validate_f017_native_d35_comparison_grant_v1.py:111,134-136; specs/017-rust-native-inference-runtime/contracts/f017-native-d3-5-comparison-read-grant-v1.json",
      "evidence": "The validator never checks route_authority.ranking_sha256 (my live mutation passed validation; the grader's RANKING_SHA256 constant is the sole enforcement point); the capture dtype check uses the heuristic 'U16_LE if \"u16\" in manifest dtype else F32_LE' instead of an exact mapping table; and the cycle-1 cosmetic serialization inconsistency persists (the same s0 bytes are 'CANONICAL_LITTLE_ENDIAN_F32_CONTIGUOUS' as expected.input_hidden but 'CANONICAL_F32_LE' as operand.s0) — the one unimplemented item of cycle-1 finding 007's repair list.",
      "failure_mode": "Bindings the consumer enforces are not mirrored in the machine-verifiable validator suite, so validator PASS alone overstates coverage; the serialization vocabulary remains inconsistent for identical bytes.",
      "required_repair": "Validate ranking_sha256 against the D0-v2 registry value, replace the dtype heuristic with an exact mapping, and normalize the serialization vocabulary at the next grant regeneration."
    }
  ],
  "counts": {"BLOCKING": 0, "NON_BLOCKING_REQUIRED": 1, "DEFENSE_IN_DEPTH": 4},
  "verdict": "REJECT",
  "statement": "All nine artifact hashes recomputed and matched at the reviewed head, and the cycle-1 BLOCKING defect is verifiably closed: the builder constant now names the real banking commit f38dc275b867..., all 34 regenerated capture rows cite it, git confirms the old commit never existed, and the repaired validator resolves every one of the 89 source authorities via git show so the 'nonexistent source commit' class is now machine-detected (and covered by a pinned mutation test). The capture manifest is hash-chained to the committed evidence pin with per-row dtype/shape/byte/sha enforcement; a fresh out-of-tree release build reproduces the pinned executable hash bbf84851... exactly; the grader pins grant id, mapping sha, the full route authority (equal to the D0-v2 registry), and the output root, discovers the repository root structurally, implements the interval gate and tier_b signed-zero check, and removed the dead cap term; eight-expert OCB gate/up/down coverage is real ([8,2048]/[8,6144] geometry with per-coordinate caps derived solely from independently decoded operands and the retained canonical input, never observed native error); the 34-row result census is complete with exact-registry oracle labels and the exact effective required set; receipts enforce EXPECTED=BEFORE=CONSUMED=AFTER on a single descriptor with an exact 89-read budget, no checkpoint path reachable, no ledger advance (terminal exactly 175), disclosed diagnostic metrics unusable and unused, and a fixed one-event output root confirmed absent. The verdict is nonetheless REJECT under the acceptance rule, because cycle-1's required repair 003 is incomplete: the grader emits the metric label 'operand_conditioned_matvec_all_eight_experts' on required ordinals 20 and 21, a label defined in no frozen contract (the D0 metric_profiles registry defines only 'operand_conditioned_matvec', which cycle-1's repair instruction named verbatim), and the new mutation test asserts the deviant label rather than the registry one (NON_BLOCKING_REQUIRED). Repairing it changes the consumer source and executable hashes and therefore the grant bytes, so the corrected grant must be regenerated and resubmitted for a fresh independent review cycle before any grading read; the four DEFENSE_IN_DEPTH residues (result-string vocabulary, unpinned read counts/registry cross-checks in the binary, OCB signed-zero, and validator ranking/dtype/serialization gaps) should be folded into the same regeneration."
}
```

The one required repair is small and mechanical — change two metric strings in `d35_grader.rs` (lines 799 and 817) to `operand_conditioned_matvec` and fix the test at `test_f017_native_d35_comparison_grant_v1.py:67` that currently asserts the wrong label — but it changes the consumer hashes, so the grant must be rebuilt and re-reviewed per the standing rule. Everything else attacked this cycle, including every cycle-1 repair, the executable reproduction, the 89-read/34-stage/8-expert censuses, and 14 extra live grant mutations, held.
