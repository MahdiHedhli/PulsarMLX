# F017 Event 05 readiness interface Gemini cycle 03 repair confirmation

This is a repair confirmation within whole-domain cycle 03, not a fourth challenge cycle. Use `gemini-3.1-pro-high`, high effort, a fresh detached read-only checkout, and committed bytes only. Begin with `git rev-parse HEAD` and bind every statement to that head.

Read `docs/architecture/reviews/evidence/f017-event05-readiness-interface-whole-domain-review-manifest-v3.json` and the exact Opus implementation cycle-01 response. Re-audit the three accepted Opus findings:

- F-A: transitively consumed FULL_NATIVE, EVIDENCE_ONLY, and Gemini result evidence must use canonical bytes and must pass the real bounded decoder; the production-boundary fixture must instantiate the real committed artifacts, not only synthetic stand-ins.
- F-B: every measured implementation tree must be the exact existing 40-hex Git tree `d7a7d72ad1f7dfaefe0157b46b384e17e61f3505`, with no truncated or invented object.
- F-C: do not repeat unsupported claims. In particular, distinguish the Event-05 validation/live shared builder from the separate historical production-shadow rehearsal helper; verify the actual call graph.

Independently execute or inspect the committed production-boundary instantiability path, canonical artifact checks, 231 substantive cases, and exact-head FULL_NATIVE run 33040808487. Re-test install-time candidate rederivation and the accepted two-phase `live:false` lifecycle. Verify zero state, authorization, checkpoint access, numerical operations, and P1 attempt-2 activity.

Return structured rows and explicit dispositions for F-A, F-B, and F-C. Report open blocking, open non-blocking-required, and unresolved counts. Finish with exactly `NO_UNRESOLVED_MATERIAL_CHALLENGE` or `UNRESOLVED_MATERIAL_CHALLENGES`.
