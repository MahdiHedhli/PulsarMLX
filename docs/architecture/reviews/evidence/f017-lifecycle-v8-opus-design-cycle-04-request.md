# F017 lifecycle V8 causal design — Opus design review cycle 04

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `d834a67d` in a detached read-only worktree. Do not modify files or access original checkpoint shards.

Reconstruct cycle 03 and attack all findings. Durable prefix artifacts now serialize `outcome=PENDING` and `result=PASS` independent of their future terminal; only the final declaration or atomic failure capsule binds the terminal outcome. A byte-identity test compares all 22 prefix artifacts between failure and success. Lease and descriptor arrays have exact cardinality and descriptor-field/ordinal/lease-ID correspondence. Failure cleanup attempts must equal expected live leases while duplicate and unknown observations remain recordable. Access-event/receipt order is exact. Success results, 79-layer completion, comparison enum/equality, installation and journal digest chains, shard receipt digests, and ordered receipt SHA sequences are independently enforced. Actor and root censuses remain exact; dead cycle detectors and redundant dependencies were removed.

Run validator, eight tests, and all 48 symbolic outcomes. Attempt retroactive prefix relabelling, zero descriptors, forged descriptor fields, cleanup counts 0/42, access/receipt reordering, result/classification forgeries, digest sequence forgery, candidate/install digest mismatch, actor/root/schema drift, and terminal-capsule absence/insertion. Report `BLOCKING`, `NON_BLOCKING_REQUIRED`, `DEFENSE_IN_DEPTH`; required verdict `ACCEPT_CHECKPOINT_IDENTITY_CAUSAL_DESIGN_V8_FOR_IMPLEMENTATION` or `REJECT`.
