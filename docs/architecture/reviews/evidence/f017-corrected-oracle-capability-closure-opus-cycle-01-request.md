# F017 numerical capability closure — Opus cycle 1

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `4f84232c65c047d3da4d3337d071d28ef59ffc30` in a clean detached read-only worktree. Repository evidence outranks this request. Do not modify evidence, access original checkpoint shards, mint Event 04 authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Reconstruct the prior five-cycle numerical review and F5-R4. Independently:

1. Rerun `_backend = np; _backend.memmap(...)` and seek representation-independent variants.
2. Attack module and member values through assignments, chains, destructuring, starred targets, containers, class state, closures, defaults, comprehensions, control flow, returns, yields, decorators, and calls.
3. Attack dynamic import, reflection, dunder, loader, and multi-depth attribute surfaces.
4. Inspect semantic import resolution, fixed-point convergence, scope handling, branch unions, fail-closed unknowns, and direct-use context rules.
5. Attack receiver provenance, especially injected/protocol objects transported through aliases, containers, closures, class attributes, and returns.
6. Verify the independent checker shares no analyzer implementation or derivation.
7. Verify runtime proxy observations and bytecode names are complete, bounded, and independently useful.
8. Inspect the 170-mutation campaign for no-op cases, shallow spelling bans, untested binding forms, or false acceptance.
9. Verify contract v3 exact SHA bindings, analyzer self-binding, qualifier/validator binding, and unchanged pure-core bytes.
10. Verify complete requalification v3 and exact historical output equivalence.
11. Verify historical/current CI separation, tombstones, and exact-head FULL_NATIVE run `32673073054`.
12. Confirm no numerical formula, methodology, threshold, context, role, or independence change.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required prevent acceptance.

Required terminal verdict, exactly one:

- `ACCEPT_CORRECTED_ORACLE_NUMERICAL_AUTHORITY_SUPERSESSION`
- `REJECT`

Confirm safety: Event 04 authorization absent; Event 04 unexecuted; real primary/secondary executions zero; original checkpoint access zero; P1 attempt 2 absent; historical ledger 175.
