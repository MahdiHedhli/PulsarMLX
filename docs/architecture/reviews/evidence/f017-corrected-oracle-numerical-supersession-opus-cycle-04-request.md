# F017 corrected-oracle numerical-authority supersession — Opus cycle 4

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `d85ddbf50989b370dffa2277941a15750cfddf70` in a clean detached read-only worktree. Repository evidence outranks this request. Do not access original checkpoint shards, mint authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Reconstruct cycles 1–3. Verify that:

1. Every `numpy` and `mlx` attribute used by the secondary pure core is exact-allowlisted and any extra attribute—including load, save, fromfile, memmap, open_memmap, export, or compile/file APIs—fails closed.
2. Dangerous builtin aliases, chained aliases, decorator use, `__builtins__`, and dynamic import variants fail closed.
3. Assigned lambda/function graph names in target sources fail in addition to exact function/method census enforcement.
4. All earlier numerical equivalence, exact SHA binding, provenance, retirement, target geometry, full-CI requalification, and independence findings remain closed.
5. The complete requalification is still byte-identical and original checkpoint access is zero.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required prevent acceptance.

Required terminal verdict, exactly one:

- `ACCEPT_CORRECTED_ORACLE_NUMERICAL_AUTHORITY_SUPERSESSION`
- `REJECT`

Confirm safety: Event 04 authorization absent; Event 04 unexecuted; real executions zero; original checkpoint access zero; P1 attempt 2 absent; historical ledger 175.
