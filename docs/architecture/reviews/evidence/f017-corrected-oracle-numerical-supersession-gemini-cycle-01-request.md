# F017 corrected-oracle numerical-authority supersession — Gemini cycle 1

Use a fresh `gemini-3.1-pro-high` AGY session at high effort. Review the exact committed bytes at `b69117800eb3123cc61ef7d2a5537cea750184a9` on branch `feat/017-rust-native-inference-runtime`. Repository evidence outranks this request. Work read-only. Do not access original checkpoint shards, mint authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Independently attack:

1. The historical primary and secondary combined scripts at commit `84f0d1dc3e60a4151329ed82773880951ee3e618`, their exact Git-byte reconstruction, and their current-tree tombstones.
2. Whether `f017_corrected_oracle_primary_numerics_v2.py` and `f017_corrected_oracle_secondary_numerics_v2.py` contain only pure numerical graph logic and synthetic in-memory abstractions, with no checkpoint, authorization, access-event, lifecycle, or target CLI surface.
3. Whether the primary and secondary target-source v6 modules own checkpoint access and decoder dispatch without reimplementing graph arithmetic.
4. Whether primary/secondary numerical independence remains intact and no shared graph, routing, or result implementation was introduced.
5. Whether the mechanical historical-to-successor mapping and 24 exact full-result equivalence cases genuinely establish no formula drift.
6. Whether the accepted checkpoint-free corpus still covers seeds 18101-18112, three processes, 44 decoder cases, all 11 formats, 16 mutations, and frozen thresholds.
7. Whether the synthetic target-adapter qualification uses the real parser, role validation, target sources, and pure cores while structurally excluding the original checkpoint.
8. Whether all current-tree v1/v2/v3 live-mint, coordinator, wrapper, and combined target entry points fail closed and historical reconstruction remains possible only through immutable Git bytes.
9. Whether CI cleanly separates historical immutable validation from current active authority and avoids treating tombstone bytes as historical numerical authority.
10. Whether the numerical contract v2 changes only authority paths and SHAs while preserving formulas, context, thresholds, top-N, stability rules, and failure classes.

Rerun relevant validators and tests from the exact head. Classify findings as `MATERIAL`, `NON_MATERIAL_REQUIRED`, or `DEFENSE_IN_DEPTH`. State whether any finding requires numerical-formula changes or original-checkpoint access.

Required advisory verdict, exactly one:

- `ACCEPT`
- `REJECT`

Confirm: original checkpoint access zero; Event 04 authorization absent; Event 04 unexecuted; real primary/secondary executions zero; P1 attempt 2 absent; historical master ledger 175.
