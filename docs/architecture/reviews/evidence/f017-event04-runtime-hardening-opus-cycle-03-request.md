# F017 Event-04 execution-readiness final review — Opus cycle 03

Use a fresh `claude-opus-5` session at high effort. Review exact committed
implementation bytes at `49628c1166b04f4f03ef3a5b0e0aff65ca711fec`, tree
`d6f24e5b7871a1fa398c5a2bd1c182a4b5bbe7bc`. FULL_NATIVE run
`32814639688` passed the Apple Silicon workspace baseline, pinned native MLX
qualification, and aggregate gate with required native skips zero. Exact-head
qualification, rehearsal, and measurement are the append-only `v3` evidence
artifacts. Repository evidence outranks this request. Work read-only. Do not
access original checkpoint payloads, mint or execute Event 04, or execute P1
attempt 2.

Reconstruct cycle-02 `B-01`, `N-01`, `N-02`, and `D-01` through `D-05`, then
attack the repairs rather than trusting their disposition artifact:

1. Supply noncanonical future authority, receipt, emergency, terminal, and
   checkpoint paths. Prove rendering rejects every spelling before any
   irreversible installation. Include `/tmp` versus `/private/tmp`, symlinked
   ancestors, dot segments, and alternate spellings.
2. Corrupt installed authorization and its receipt separately. Make the
   primary emergency evidence root unwritable, preoccupied, symlinked, or
   malformed. Prove the separately bound terminal fallback root either banks
   the maximal constructible failure or returns the stable controlled runtime
   classification without a raw exception. Prove neither root can redirect to
   an unapproved path and terminal writes cannot follow a rejected symlink.
3. Independently recompute the implementation head from the complete set of
   implementation paths bound by the runtime authority manifest. Mutate the
   readiness head, authority-manifest SHA, acceptance timestamp, and final
   operator-approval binding. Prove a readiness declaration for an evidence
   descendant cannot authorize different implementation bytes.
4. Mutate memory-gate threshold, sample age, page size, and counter types,
   including booleans. Require controlled failure and exact production gate
   semantics.
5. Inspect the qualification source and derive, rather than trust, the census
   of 47 modeled outcomes, split into 41 coordinator causal-bank injections and
   6 authorizer-phase direct capsules, 201 executions, zero accounting
   mismatches, and zero uncontrolled failures. Confirm CI asserts all derived
   values.
6. Replace one inherited graph shard with a symlink and inject a raw `os.open`
   error. Require controlled rejection before payload use. Recheck all root,
   catalog-SHA, hardlink, production-name, and production-root isolation.

Then rerun the complete execution-readiness attack: DID-01 through DID-12;
idempotent descriptor release and close-evidence recovery; dynamic
durable-start accounting; every modeled outcome with exact required and
forbidden artifacts; both independent descriptor-backed consumers using
inherited FDs from shards 2 through 6 with zero graph path reopen; the 1,410
graph-tensor plan and 399 non-access denials; all 11 formats; generator
`--check`; deterministic evidence; enforced mint and package-start memory
gates; independent C7-N2 checks; historical-worktree cleanup on pass and fail;
numerical contract V3; and the production-shaped no-access rehearsal.

Recompute the unchanged numerical core and decoder SHAs. Verify original
checkpoint access zero, Event-04 authorization absent, Event-04 execution
absent, P1 attempt 2 absent, attempts 1, retries 0, resume false, and historical
ledger 175. List every finding exactly once as `BLOCKING`,
`NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`, with stable IDs. Both blocking
and non-blocking-required prevent acceptance. State material disagreement.
End with exactly one unconditional verdict:
`ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION`
or `REJECT`.
