# F017 lifecycle V6 semantic design — Opus cycle 01

Review exact committed bytes at `a2168bbbfbb9b699af1095f929a5ded69412e94d` in a detached read-only worktree.

Independently attack Event 03 history, package-attempt representation, all lifecycle states/transitions/outcomes, conditional unstarted-consumer evidence, package/primary/secondary accounting, path timing and root relations, canonical serialization/readback SHA domains, candidate/install semantics, installation receipt, exact schema censuses, complete identity propagation, implementation measurement, truthful v1-v5 retirement, numerical-contract-v3 binding, and Event-04 isolation.

Specifically attempt coordinated model and generated-view changes, byte drift in every generated authority, semantic-column values such as `SUBSTRING_MATCH_OK` and `WARN_ONLY`, attacker validator paths, forged authority status, extra row/cell keys, unused states/actors, and a new reachable P1 or live-authority transition. Confirm the independent validator imports neither the generator nor its semantic derivation helper.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both BLOCKING and NON_BLOCKING_REQUIRED prevent acceptance. Return exactly one verdict: `ACCEPT_LIFECYCLE_V6_SEMANTIC_DESIGN_FOR_IMPLEMENTATION` or `REJECT`. No conditional acceptance. Do not modify repository files.
