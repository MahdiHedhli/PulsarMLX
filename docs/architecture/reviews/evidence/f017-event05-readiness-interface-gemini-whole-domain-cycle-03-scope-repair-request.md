# F017 Event 05 readiness interface whole-domain cycle 03 scope-repair confirmation

This is a repair confirmation within whole-domain cycle 03, not a fourth challenge cycle. Use `gemini-3.1-pro-high`, high effort, a fresh detached read-only checkout, and committed bytes only. Begin with `git rev-parse HEAD`; every challenge row and the global verdict must bind that exact detached head.

Read `docs/architecture/reviews/evidence/f017-event05-readiness-interface-scope-repair-review-packet-v1.json` and verify every listed SHA. Reconstruct Opus cycle-02 findings F-D and F-E from its exact response.

Directly attack F-D: prepared and final readiness are separate exact predicate sets; prepared declarations explicitly say validation-only, readiness false, and prepared reviewer fixtures are non-final. A live approval combined with prepared readiness must fail in the shared candidate builder and again at the live authorizer boundary. Final readiness must require the final manifest/reviewer schemas, exact reviewer models, reviewed heads, response SHAs, and zero open blocking/non-blocking-required/unresolved findings.

Directly attack F-E: the qualifier itself must generate 231 cases, including five production-boundary cases; CI must compare the generated output to qualification v3; repository tests must read the actually bound prepared declaration v4, manifest v4, instantiability v4, FULL_NATIVE v6, and exact Git head/tree objects. Verify FULL_NATIVE run 33044253965, zero required native skips, zero numerical/result drift, and zero checkpoint access.

Attack all twelve readiness-critical claims and issue structured challenge rows with challenge ID, claim ID, severity, attack, observed evidence, status, and required repair. Do not modify files, access checkpoint shards, mint or execute Event 05, or execute P1 attempt 2.

Finish with counts for open blocking, open non-blocking-required, defense-in-depth, and unresolved findings, followed by exactly `NO_UNRESOLVED_MATERIAL_CHALLENGE` or `UNRESOLVED_MATERIAL_CHALLENGES`.
