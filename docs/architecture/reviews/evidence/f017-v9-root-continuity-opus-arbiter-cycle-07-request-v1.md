# F017 V9 Root-Continuity Graph — Opus Arbiter Cycle 07

Review exact committed bytes at head `3100714e9b89593add5f0d54dfd340900614ee66` in a fresh detached read-only worktree.

Act as `claude-opus-5`, high effort, claim-by-claim ARBITER. Return one verdict for every one of the 25 readiness-critical claims: `ACCEPT`, `REJECT`, or `UNRESOLVED`, plus a global verdict of exactly `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION` or `REJECT`. No conditional acceptance.

Cycle 06 retained 23 claims and rejected `C-JSON-005` and dependent `C-IMPL-002` after deferred annotation evaluation through approved `typing` surfaces returned genuine `json.loads`. Reconstruct that cycle from the exact response, arbiter ledger v6, challenge ledger v11, support ledger v10, claim ledger v12, authority manifest v13, measurement v17, qualification v16, runtime qualification v16, rehearsal v17, and FULL_NATIVE v11.

Required attacks:

1. Re-run every exact cycle-06 bypass: return, parameter, class, and dataclass annotations; `typing.get_type_hints`; `from typing import get_type_hints`; `ForwardRef._evaluate`; `evaluate_forward_ref`; and the f-string annotation with no single `json` literal.
2. Attack the shape-based acquired-callable analysis through direct Call/Subscript/Attribute assignments, chained aliases, tuple/list/dict/set containers, destructuring, annotated assignments, named expressions, conditional expressions, boolean expressions, closures, defaults, comprehensions, class storage, instance storage, return-and-receive flows, and callable decorators.
3. Attempt to confuse propagation with unrelated names in conditions or sibling expressions, and verify legitimate accepted callbacks remain permitted without weakening acquired-capability rejection.
4. Attack `typing` under all import aliases, from-import aliases, first-party re-exports, nested modules, and member transport. Verify the exact allowed from-import vocabulary is complete for current bytes and no runtime `typing` module capability is permitted.
5. Attack deferred annotations through implicit string concatenation, bytes, formatted values, nested Subscript/Tuple annotations, `__annotations__`, `inspect.get_annotations`, `annotationlib`, `typing_extensions`, dataclass field metadata, PEP 649-style surfaces available in the pinned runtime, and first-party transport.
6. Re-run every cycle-03 through cycle-05 parser-policy bypass: direct/dotted JSON, decoder/scanner/encoder/tool, loader machinery, introspection dunders, string-keyed resolvers, alternate structural decoders, and import-root substitution.
7. Independently census the exact current closure and all active decode sites. Verify the canonical bounded parser and one documented offline allowance remain the only two sites.
8. Verify 110 permanent parser mutations, 158 focused tests, 203 combined active tests, 204 decode attacks, 252 root attacks, all 47 outcomes, 201 runtime failure executions, 50 runtime packages, FULL_NATIVE run `32886210156`, and EVIDENCE_ONLY run `32887888524` with zero native jobs.
9. Re-run root substitution, journal corruption, fallback loss, retained-handle, deep-artifact, and cross-product attacks supporting the retained root and parser claims.
10. Verify implementation head `fa3daeaf37425cc7aeeab05bcc5fb7b74c7a7e28`, tree `6c0fffdb4ab57a36bcc6444a29594003795e0d55`, all 43 measurement bindings, numerical byte identity, original checkpoint access zero, Event-04 authorization/package-start/execution absent, and P1 attempt 2 absent.
11. Assess out-of-process inline decoder commands and measurement auxiliary literals separately as defense in depth unless an executable current-byte path violates a readiness-critical claim.

Use `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required findings prevent global acceptance. Every rejected or unresolved claim must name its earliest invalidation node. Preserve prior accepted claims only when current evidence and dependencies remain valid.

Return machine-readable JSON containing reviewed head/model, 25 claim verdicts with evidence and invalidation dispositions, findings, accepted/rejected/unresolved counts, safety observations, and the exact global verdict.
