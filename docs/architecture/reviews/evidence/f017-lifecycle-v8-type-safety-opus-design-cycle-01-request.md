# F017 V8 descriptor type-safety design closeout — Opus cycle 01

Use a fresh `claude-opus-5` high-effort session. Review exact committed design bytes at `73cc8653d9110c90e650a088f5c8a2b8694325b8` in a detached read-only worktree. Repository evidence outranks this request. Do not modify files, access original checkpoint shards, mint Event-04 authority, execute Event 04, or execute P1 attempt 2.

Independently reconstruct C7-N1 and C7-N2. Test `0o1100644`, `65536`, bool mode, all malformed descriptor element shapes, mapping subclasses, every unhashable lease-ID class, invalid grammar, and duplicate IDs. Verify every malformed value reaches a stable `ValueError`, not `OverflowError`, `AttributeError`, `TypeError`, `KeyError`, or raw OS/stat errors. Inspect the exact type/census/semantic ordering and confirm `stat.S_ISREG` is unreachable before the 16-bit bound and `set()` is unreachable before every lease ID is a valid string. Inspect and adversarially test the independent checker and import separation.

Rerun the 256-mutation suite, independent design validator, all 48 symbolic constructions, transitive closure, exact five-descriptor continuity, ordinals 2–6, path timing, safety invariants, and descriptor release closure. Recheck all prior cycle-05 through cycle-07 required findings for regression. Confirm numerical authority SHAs are unchanged, active generation remains NONE, original checkpoint access is zero, and V8 implementation may proceed without original checkpoint access.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required findings prevent acceptance. Return exactly one verdict: `ACCEPT_CHECKPOINT_IDENTITY_CAUSAL_DESIGN_V8_FOR_IMPLEMENTATION` or `REJECT`. No conditional acceptance.
