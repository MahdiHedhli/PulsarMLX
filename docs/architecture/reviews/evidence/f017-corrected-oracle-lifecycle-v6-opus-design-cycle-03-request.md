# F017 Lifecycle V6 Semantic Design Review — Cycle 03

Use a fresh `claude-opus-5` session at high effort. Review exact committed bytes at `3f71bac6384822569060ff9c042e4bca9d10ddeb` in a clean detached read-only worktree. Git and committed evidence outrank this request. Do not modify the repository, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

This is design checkpoint cycle 03. Independently reconstruct and attack the complete lifecycle V6 authority, not only the listed repairs. The required verdict is exactly `ACCEPT_LIFECYCLE_V6_SEMANTIC_DESIGN_FOR_IMPLEMENTATION` or `REJECT`. Use `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH`; either of the first two prevents acceptance.

Reproduce cycle-02 findings against the new head:

- authority scope must be exactly and consistently `PRODUCTION`;
- every pre-mint/load-bearing failure must have a realizable, terminal, durable route with identities introduced before use;
- unstarted consumer receipt/terminal hashes must be JSON null and fabricated evidence must fail;
- finite floats must have one cross-runtime byte representation;
- runtime artifacts, indices, bindings, schemas, and bank order must correspond to the semantic authority;
- qualification counts must arise from actual subprocess reports and file-backed trace executions, not multiplication;
- accounting classes, historical Git-object ledger bytes, path-parent timing, forbidden live-ID markers, and the load-bearing measurement census must be pinned.

Attack independently:

1. complete-model byte anchoring and coordinated model+regeneration drift;
2. exact registry/matrix semantic columns and schema/key censuses;
3. declared sibling bank order for every applicable outcome;
4. failure-route totality and outcome/artifact/ledger correspondence;
5. package receipt conditional fields for started and unstarted consumers;
6. path satisfiability and root relations;
7. canonical serialization and finite-float encoding;
8. historical ledger verification from the exact Git object;
9. runtime schema correspondence and required ledger indices;
10. exact authority/parser/coordinator/consumer separation and retired generations;
11. actual 10-package and 225-failure-trace synthetic qualification evidence;
12. production-shaped rehearsal with zero checkpoint opens/reads and no authority installation.

Confirm numerical contract v3 and both pure numerical-core SHAs remain unchanged. Confirm `ACTIVE_CORRECTED_ORACLE_GENERATION` remains `NONE`, Event 04 is not authorized or executed, original-checkpoint access is zero, P1 attempt 2 does not exist, and the historical ledger remains 175.
