# F017 corrected-oracle numerical-authority supersession — Opus cycle 5

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `efe2ae4908dd5e04c948ded37326bc95c40b119c` in a clean detached read-only worktree. This is the final numerical review cycle. Repository evidence outranks this request. Do not access original checkpoint shards, mint authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Reconstruct cycles 1–4. Independently verify:

1. Exact import-AST multisets reject all alias, from-import, star-import, submodule, and spelling variants while accepting the exact committed core imports.
2. Exact `numpy`/`mlx` attribute allowlists reject all file, mmap, load/save, export, and unreviewed capabilities.
3. Dangerous builtin and meta-loader names are rejected in any AST context, including tuple/list/dict/default/walrus/loop/decorator/attribute forms.
4. Recursive assigned-surface collection rejects class-body, annotated, lambda, dictionary-key, and nested assigned graph names.
5. The numerical contract binds the validator and qualifier themselves by exact path/SHA and the validator enforces those bindings.
6. All prior exact equivalence, independence, target separation, provenance, retirement, CI, geometry, and complete requalification properties remain closed.
7. Full requalification remains byte-identical and original checkpoint access remains zero.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required prevent acceptance.

Required terminal verdict, exactly one:

- `ACCEPT_CORRECTED_ORACLE_NUMERICAL_AUTHORITY_SUPERSESSION`
- `REJECT`

Confirm safety: Event 04 authorization absent; Event 04 unexecuted; real primary/secondary executions zero; original checkpoint access zero; P1 attempt 2 absent; historical ledger 175.
