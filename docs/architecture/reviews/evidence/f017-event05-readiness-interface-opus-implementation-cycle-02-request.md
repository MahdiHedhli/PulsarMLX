# F017 Event 05 readiness interface IMPLEMENTATION ARBITER cycle 02

Use `claude-opus-5`, high effort, a fresh detached read-only worktree, and committed bytes only. Begin with `git rev-parse HEAD` and bind all verdicts to the exact detached head.

This is a repair resubmission after cycle-01 REJECT. Independently verify all three cycle-01 findings:

- F-A: every transitively consumed readiness-bound artifact is canonical before the bounded decoder; noncanonical committed producer artifacts are rejected; canonical successors are append-only; and `f017-event05-readiness-interface-prepared-production-instantiability-v2.json` proves the real committed production-boundary artifacts instantiate through the measured validator and authorizer.
- F-B: the measured implementation tree is the exact existing 40-hex Git tree `d7a7d72ad1f7dfaefe0157b46b384e17e61f3505`, consistently bound in measurement v6, scientific access v11-v6, FULL_NATIVE v4, prepared manifest v2, and prepared declaration v2.
- F-C: Gemini’s cycle-03 repair confirmation must be evaluated for correctness. Verify the actual call graph and do not claim `render_rehearsal_candidate` uses the Event-05 validation/live shared builder; the load-bearing sharing requirement applies to `render_validation_only_operator_go_candidate` and `render_operator_go_candidate` through `_render_operator_candidate` and `build_operator_go_candidate`.

Re-run the exact prepared declaration through bounded decode, readiness validation, validation-only approval, shared candidate construction, primary validation, and secondary validation. Verify 20 deterministic repetitions, candidate SHA, zero state creation, zero live authorization, zero checkpoint access, zero numerical operations, and zero ID consumption. Inspect FULL_NATIVE run 33040808487 and 231 substantive mutations.

Arbitrate all twelve readiness-critical claims independently: C-SCHEMA-001, C-SCHEMA-002, C-VALIDATOR-001, C-AUTHORIZER-001, C-INSTANT-001, C-INSTANT-002, C-BIND-001, C-SAFETY-001, C-LEGACY-001, C-CI-001, C-REVIEW-001, C-GO-001. Return ACCEPT, REJECT, or UNRESOLVED with evidence and earliest invalidated node. Report blocking, non-blocking-required, defense-in-depth, and unresolved counts.

Finish with exactly `ACCEPT_F017_EVENT05_READINESS_INTERFACE_IMPLEMENTATION` or `REJECT`. No conditional acceptance.
