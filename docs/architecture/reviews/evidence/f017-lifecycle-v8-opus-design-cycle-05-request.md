# F017 lifecycle V8 causal design — Opus design review cycle 05

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `0084e7e0` in a detached read-only worktree. Do not modify files or access original checkpoint shards.

Reconstruct cycle 04 and independently attack all four `NON_BLOCKING_REQUIRED` findings. The descriptor identity rule now enforces exact nine-field records, strict integer fields, non-empty lease IDs, pairwise-distinct `(device,inode)` pairs, exact shard ordinals, and sizes derived from the pinned checkpoint metadata. Successful comparison classifications are derived from numerical-contract V3 and exclude every `ATTEMPT_2_BLOCKED` consequence. Failure-capsule live leases are derived from expected, successful, and duplicate closures; unknown leases cannot discharge the census. All 15 formerly type-only identifiers now bind to their envelope or upstream durable-start artifact, or use an exact non-empty-string rule. Coordinated addition of payload constants outside the independently enumerated census fails. The suite now asserts all 47 immutable durable prefixes and 205 rejected mutations.

Run the independent validator, ten tests, and all 48 symbolic outcomes. Re-run every cycle-04 exploit with dependency SHAs and payload digest restatements repaired: duplicate descriptor device/inode, wrong size, wrong field types, empty lease ID; blocked comparison classifications on complete success; duplicate and unknown lease closure combinations; candidate envelope divergence; both consumer event-chain divergence; package-ledger identity divergence; empty operator approval and owner nonce; coordinated payload-constant drift. Attempt new representation-independent variants and report `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH` separately. Required verdict is exactly `ACCEPT_CHECKPOINT_IDENTITY_CAUSAL_DESIGN_V8_FOR_IMPLEMENTATION` or `REJECT`; any required finding must produce `REJECT` regardless of directional acceptance.
