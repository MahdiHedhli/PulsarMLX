# F017 Event 06 Sequence 5 — Antigravity Final Design Challenge, Cycle 8

Review exact detached commit `06013ba58fc2118016626065b075174dc2fd75bb`,
tree `3064e800759d74d53ab6fc5ec2cced02b7ce27e8`. This is the final permitted
design cycle. Read only. Never resolve or access original checkpoint paths;
never create authority or run numerical/Event-06 work.

Independently run:

```text
python3 scripts/research/generate_f017_event06_sequence05_design_v8.py --check
python3 scripts/research/validate_f017_event06_sequence05_design_v5.py
python3 scripts/research/validate_f017_event06_sequence05_design_v5.py --inject-a2-failure
python3 -m py_compile scripts/research/generate_f017_event06_sequence05_design_v8.py scripts/research/validate_f017_event06_sequence05_design_v5.py
```

The injected command must exit 1 while emitting a complete `result: FAIL`
report. Reproduce all 13 isolated mutations and inspect all 15 AST-guarded
predicate functions.

Reconstruct Cycle-6's eight repairs and Cycle-7's Antigravity and Opus
rejections. Directly attack every Cycle-7 issue:

- historical final-declaration manifest membership is prohibited and replaced
  by a distinct canonical summary that SHA-binds the historical declaration
  and implementation measurement v2;
- FAIL reporting is reachable and banked;
- qualification has zero validation gaps and no contradictory false flag;
- the alias matrix is a disjoint 6 semantic-family × 3 structural-location
  axis and the 324 total is derived from the active readiness census;
- all three schemas come from the external SHA-bound schema authority;
- manifest `bindings_type`, exact census, declared-tree hashes, path traversal,
  symlink mode, forbidden final/self paths, and future-role posture are checked;
- all nine advisories have distinct cycle-qualified IDs, dispositions, evidence
  paths, evidence SHAs, and zero unresolved rows;
- all 16 outcomes equal the failure-matrix category census and match real edge
  write classes;
- Cycle-5 and Cycle-6 graph facts both reconcile without namespace collapse;
- all recovered review provenance has the exact 21-field contract census.

Do not accept generated PASS labels. Return unique findings with severity,
exact evidence, affected claim, and smallest repair. State blocking, required,
advisory/challenged, and unresolved counts. Acceptance requires every count to
be zero and exact verdict:

`ACCEPT_F017_EVENT06_SEQUENCE5_DESIGN_CYCLE8`

Otherwise return `REJECT`. No conditional acceptance.
