# F017 Event-04 Authority Reconciliation Gemini Review Cycle 02

Use a fresh `gemini-3.1-pro-high` AGY session at high effort. Review exact committed repository bytes at evidence head `b3892e6dbbbc02f32a32f7f9f798ab0c899dcce2`. The accepted implementation measurement head is `b6bf0af3731a56fb69b6e95ee7dad1b9f7252980`, tree `4ba0b4f1dcce691b2ac32fc4b8704a02c86933ef`, and the runtime-rebound authority bundle head is `6d0f65433ec7bc6ea389f842359aad934a3b9839`. Repository bytes outrank this request.

Do not modify repository files, access original checkpoint shard payloads, mint or execute Event 04, run a real oracle, or execute P1 attempt 2.

Independently reconstruct the original parser/coordinator mismatch and assess the cycle-01 repair disposition. Compute SHA-256 from exact `measurement_head:path` Git bytes, inspect Git blob identities, and compare implementation measurement V5, the original lifecycle declaration, the stale original scientific-access V6 authority, reconciliation discrepancy evidence, scientific-access reconciliation V3, inert fixture V3, authority manifest V3, correction declaration V2, production-shaped no-access rehearsal V2, CI census V2, and cycle-01 Opus findings and repair disposition.

Attack all of the following:

- whether the active runtime parser now loads and validates measurement V5 and scientific-access reconciliation V3 rather than the stale V3/original contract defaults;
- whether measurement V5 truthfully binds its V4 parent bytes and all 68 entries at the exact Git measurement head;
- whether the scientific contract's complete 20 implementation bindings equal measurement V5 and authority manifest V3 without omissions;
- whether path-timing, serialization, and lifecycle-manifest bindings are anchored to exact Git bytes;
- whether all 23 independent mutations fail closed, including fully coordinated mutations and manifest omissions;
- the implementation-identity source-of-truth hierarchy and attempts to excuse stale inner bindings using valid outer artifact SHAs;
- inert-fixture rebinding, candidate construction, installed-auth validation, and active generation V6;
- production-shaped rehearsal V2 with no state creation, checkpoint opens, checkpoint reads, or numerical operations;
- exact-head FULL_NATIVE run `32731485003` at `6d0f65433ec7bc6ea389f842359aad934a3b9839` and EVIDENCE_ONLY runs recorded in CI census V2, including `32732801169` at `b3892e6dbbbc02f32a32f7f9f798ab0c899dcce2`;
- required native skips zero and evidence-only native jobs zero;
- the append-only correction of the two inaccurate cycle-01 Gemini assertions;
- absence of Event-04 authority/execution, original checkpoint access, and P1 attempt 2.

Rerun the independent reconciliation validator with all 23 mutations and rerun the corrected rehearsal if safe. Original checkpoint payload access is prohibited; the rehearsal must remain metadata-only.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. State material disagreement explicitly. Return exactly `ACCEPT` or `REJECT` as the final verdict.
