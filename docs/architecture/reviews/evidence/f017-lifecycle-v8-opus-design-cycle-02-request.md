# F017 lifecycle V8 causal design — Opus design review cycle 02

Use a fresh `claude-opus-5` session at high effort. Review exact committed bytes at `c6563e80` in a detached read-only worktree. Repository evidence outranks this request. Do not modify files, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Reconstruct Opus cycle 01 and Gemini cycle 03. Independently attack every cycle-01 finding and verify the general repair, not only named examples. The design now enumerates every durable success prefix as one of 47 terminal failure variants plus complete success. Check that retained graph-descriptor leases are derived from the actual prefix; identity failures across all six shard ordinals release exactly retained leases; partial and completed release prefixes never double-close; all failure release reports bind duplicate and unknown counts; every failure closure ends in a final declaration; actual checkpoint metadata binds all shard filenames, sizes, and digests; processing semantics are source-resolved; the model exactly covers every DAG producer transition and actor; accounting ranks and outcome cuts are exact; and recursive validation independently enforces full schema, payload, outcome, root-authority, dependency, causal-rank, and package-identity conformance after consistent descendant rehashing.

Run the validator, five-test suite, and 48-outcome symbolic constructor. Attack unmodeled prefixes, partial-release states, lease-manifest correspondence, forbidden artifact presence, schema/outcome/root forgery, transition/model drift, actor drift, accounting drift, and safety-pointer drift. Report `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH` separately. Both blocking and non-blocking-required prevent implementation. Required verdict: `ACCEPT_CHECKPOINT_IDENTITY_CAUSAL_DESIGN_V8_FOR_IMPLEMENTATION` or `REJECT`; no conditional acceptance.
