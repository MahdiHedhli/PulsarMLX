# F017 Native Bounded-P1 Domain — Fable Ratification Request

Use Claude Fable 5 only. This request must remain parked until that exact reviewer is available. Do not substitute another model and do not let the Opus interim acceptance bias the review.

Review committed bytes in a clean detached worktree at exact implementation head `ab236b924f7dd7a95d226c5bb528315a4a68d72c` on `feat/017-rust-native-inference-runtime`. Recompute every load-bearing hash. Inspect exact-head CI run `32574711605` directly. The complete interim history is bound by `docs/architecture/reviews/evidence/f017-native-bounded-p1-domain-fable-ratification-packet-v1.json`, expected SHA-256 `7aca187f2ad4c7d1ee221fe38dbbc5e34df481df894cbc55e10241f08fbee6e6`.

Independently inspect all three Opus cycles, findings, repairs, and defense-in-depth dispositions. Treat Opus as evidence, never authority for your verdict. Explicitly reconcile the C3 request-revision label noted in the packet.

Rerun, at minimum:

1. traversal, absolute, separator, Unicode, NUL, overlength, empty, and safe-but-contract-wrong authority identifiers; prove rejection precedes checkpoint planning/opening, claim, state, and math;
2. source-built versus banked synthetic producer provenance and the ten-fresh-process receipt-v2 qualification;
3. D3.5 grants, 89-read census, 34-stage mapping/serialization, D0 tolerance epistemics, routing structure, and mixed-class PASS without numerical re-execution;
4. full bounded-P1 producer instantiability, layer/expert/quant format coverage, KV/RoPE position-zero scope, final norm/logits/top-k, one token, mandatory stop, and no Python/cross-runtime fallback;
5. six-shard/1,809-tensor plan, alternate-root/link/corruption/duplicate/missing/format mutations, with plan-only opening no original checkpoint bytes;
6. all 22 counter producers, liveness, exact receipt census, receipt/terminal accounting, RN1 owner-only terminalization, replay, race, symlink, retry, resume, continuation, and second-token attacks;
7. Apple M1 Ultra/arm64, MLX 0.31.2, MLX-C 0.6.0, dylib/executable/head, 16 GiB and 5-second freshness bindings;
8. historical ledger 175 uniqueness and separation from native event/residency accounting;
9. B1 missing-free, exact B2 source-first/no-eval/no-sync, oracle policy/parity, and residual-edge regressions;
10. all seven C3 defense-in-depth findings, promoting anything that weakens the live-authorization gate.

Do not run P1, open original checkpoint payload, create a live authorization, or perform full-model real-checkpoint inference. Put all mutations and build output outside the review worktree. Bank exact request/response/model/CLI/head/hashes/tests/findings/severities/dispositions.

`BLOCKING` and `NON_BLOCKING_REQUIRED` prevent acceptance.

Return exactly one terminal verdict:

`ACCEPT_FOR_SINGLE_BOUNDED_M1_ULTRA_P1`

or

`REJECT`

Always state:

`REAL_M1_ULTRA_P1_EXECUTED_DURING_REVIEW: NO`

`ORIGINAL_CHECKPOINT_READS_DURING_REVIEW: 0`

`LIVE_P1_AUTHORIZATION_CREATED_DURING_REVIEW: NO`
