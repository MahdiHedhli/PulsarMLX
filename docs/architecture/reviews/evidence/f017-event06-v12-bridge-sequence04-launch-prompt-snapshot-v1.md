# PulsarMLX F017 Execution Graph
## Event 06 V12-Bridge Corrected Full-Checkpoint Oracle One-Shot Execution

# Operator directive

**GO.**

Authorize and execute exactly one corrected full-checkpoint oracle Event 06 on
the **Mac Studio M1 Ultra** under the accepted V12 checkpoint-identity
authority, accepted V12-to-V11 numerical bridge authority, unchanged numerical
V4 authority, and unchanged V11 result authority.

The fresh human authorization text is exactly:

`GO — authorize exactly one corrected full-checkpoint oracle Event 06 on the Mac Studio M1 Ultra under the accepted V12 identity authority, V12-to-V11 bridge authority, numerical V4 authority, and V11 result authority.`

Its UTF-8 SHA-256, excluding Markdown delimiters and any trailing newline, is:

`d38b4766fb6f3e25b0813032df0f0941f332c3afeda4e6867624926ff6e929e5`

This GO is fresh authority after the prior Sequence 2 GO expired before
approval. It applies only to Event 06, only to the exact source and authority
bytes pinned below, and only to one package attempt. It is not permission to
retry or resume Event 04, Event 05, or the expired Event 06 launch; execute a
second package; change runtime bytes; mint P1 attempt-2 authority; or execute
P1 attempt 2.

This GO permits, only after every pre-mint guard passes:

1. one fresh operator approval and event-identity/execution-plan pair;
2. one exact V12 candidate and installed checkpoint-identity authority;
3. one package claim and durable package start;
4. six ordered checkpoint identity opens and complete identity hashes;
5. one primary binary64 full-model oracle execution;
6. one independent secondary binary32 full-model oracle execution;
7. six canonical numerical payloads;
8. one independent comparison, descriptor release, accounting closure, and
   package terminal through the accepted bridge coordinator;
9. append-only evidence, EVIDENCE_ONLY CI, challenge review, and arbitration;
10. one P1 attempt-2 preparation-readiness decision.

A failure is durable terminal evidence, never permission to retry. Repository
evidence is authoritative; Git outranks this prompt.

---

# 1. Prompt, GO, parent, and duplicate authority

Prompt repository:

`MahdiHedhli/PulsarMLX-Prompts`

Prompt path:

`Prompts/F017/Mac-Studio-M1-Ultra/004__F017__Mac-Studio-M1-Ultra__Event-06-V12-bridge-corrected-full-checkpoint-oracle-one-shot-execution-go__prompt.md`

Adjacent checksum path:

`Prompts/F017/Mac-Studio-M1-Ultra/004__F017__Mac-Studio-M1-Ultra__Event-06-V12-bridge-corrected-full-checkpoint-oracle-one-shot-execution-go__prompt.md.sha256`

Expected paired response path:

`Prompts/F017/Mac-Studio-M1-Ultra/004__F017__Mac-Studio-M1-Ultra__Event-06-V12-bridge-corrected-full-checkpoint-oracle-one-shot-execution-go__response.md`

Expected response checksum path:

`Prompts/F017/Mac-Studio-M1-Ultra/004__F017__Mac-Studio-M1-Ultra__Event-06-V12-bridge-corrected-full-checkpoint-oracle-one-shot-execution-go__response.md.sha256`

Expected parent response:

- path:
  `Prompts/F017/Mac-Studio-M1-Ultra/003__F017__Mac-Studio-M1-Ultra__Event-06-V12-to-V11-numerical-authority-bridge-repair__response.md`;
- commit: `ee5bc2a2ca7f8f55f028b61385c3b66a81101e9e`;
- SHA-256:
  `5f97be9d8e420c93a2244ff2e8e2a54010b28694d272a09fbac8ee542872fe39`;
- verdict:
  `F017_EVENT06_V12_TO_V11_NUMERICAL_AUTHORITY_BRIDGE: ACCEPTED`;
- readiness:
  `READY_FOR_FRESH_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06_GO: YES`.

Before source-repository write, local alias resolution, approval, state
creation, checkpoint access, or numerical operation:

1. fetch this prompt from the exact commit-pinned launcher;
2. calculate its SHA-256 and require equality with both launcher and adjacent
   basename-only sidecar;
3. require the prompt commit to be an ancestor of live prompt-control `main`;
4. verify the exact parent response bytes, checksum, commit, ancestry, accepted
   verdict, readiness, and zero-access safety state;
5. verify the Sequence 4 response/checksum/notification/handoff/terminal do not
   already exist and no duplicate worker has consumed this directive;
6. verify the prior Sequence 2 GO disposition remains
   `EXPIRED_BEFORE_APPROVAL`, its five `V12-1` identities remain unconsumed,
   and it is never resumed;
7. verify Event 04 and Event 05 remain terminal/no-retry and no other committed
   directive is launched;
8. verify Mac Studio latest sequence is `4` and preserve all concurrent prompt
   control by compare-and-swap.

The MacBook Pro M2 Max Feature Loop lane is concurrent and independent. Do not
modify `Prompts/F017/STATE.json`, any MacBook artifact, 42 Ultracode, the R001
branch, or Feature Loop policy state. Preserve MacBook sequence `13` or any
later fast-forward exactly as found. Prompt-control movement is acceptable
only when this prompt remains an ancestor and Sequence 4 remains unconsumed.

Any mismatch expires this GO before source mutation and checkpoint access.

---

# 2. Exact source and accepted readiness authority

Source repository:

`MahdiHedhli/PulsarMLX`

Branch:

`feat/017-rust-native-inference-runtime`

Required exact starting evidence head/tree:

- head: `58d3984686e829d7e434777c9bf3d7ac485ed459`;
- tree: `5e0802d69b2dd6a8061c5ea612d0cf0d18c05911`.

Required measured bridge implementation head/tree:

- head: `7fbc641339f65619f77cca78a86eabc4d19277b7`;
- tree: `4ea92265360b7130948e52b8497f2802a7526702`.

Require exact local/remote parity, a clean worktree, no merge/rebase, and
starting head exactly equal to the required evidence head. A descendant is not
a substitute. Any source movement expires this GO before approval.

Recompute and bind the accepted V12 identity authority:

- final authority declaration:
  `docs/architecture/reviews/evidence/f017-event06-v12-checkpoint-identity-authority-final-declaration-v1.json`
  SHA-256 `09e1a2b0eb35ffa45ea635ef4c245705b6589e377a77bf980d80dc0650aecfe6`;
- V12 authority manifest:
  `docs/architecture/reviews/evidence/f017-event06-v12-checkpoint-identity-authority-manifest-v10.json`
  SHA-256 `cd34c93987ad0f7dec1bbb8b922aaf084e4a187b20bcb92232ff54de22b51774`;
- checkpoint-identity contract:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-oracle-checkpoint-identity-authority-v12.json`
  SHA-256 `12f6d717edbdea385974d870445754b7163ce76dfef3ee936a72586307da9050`;
- exact instantiability:
  `docs/architecture/reviews/evidence/f017-event06-final-readiness-instantiability-v3.json`
  SHA-256 `57c44e0a201b89cd198581412cac50dcc2198b36c843204cfb757517ea64dd4b`;
- production-shaped no-access rehearsal:
  `docs/architecture/reviews/evidence/f017-event06-production-shaped-no-access-rehearsal-v12-v9.json`
  SHA-256 `93e87a4e6ce2e9e750656a41afbdde853eafc4e729e7ba9d1e201ca8457575f2`.

Recompute and bind the accepted bridge/readiness authority:

- bridge declaration:
  `docs/architecture/reviews/evidence/f017-event06-v12-to-v11-numerical-authority-bridge-final-declaration-v1.json`
  SHA-256 `8eba9b05604e13e70d71f00207ff4ee4c670ab1745129aaad5a80180b6167d47`;
- final readiness declaration:
  `docs/architecture/reviews/evidence/f017-corrected-oracle-event06-execution-readiness-final-declaration-v12-v3.json`
  SHA-256 `98586f23d395ba8b86c7896eb9d36300a04886fc88a30371c12c5f7aa883149d`;
- bridge authority manifest:
  `docs/architecture/reviews/evidence/f017-event06-v12-to-v11-bridge-authority-manifest-v5.json`
  SHA-256 `4b666425387d83c3a9cf207273a14e3323c4484297438bb09b46c8e60b6710db`;
- bridge implementation measurement:
  `docs/architecture/reviews/evidence/f017-event06-v12-to-v11-bridge-implementation-measurement-v2.json`
  SHA-256 `c706cf485c5c0f58e192d903e94523010fe0f690d1465a9bc38879d1b03801df`;
- bridge contract:
  `specs/017-rust-native-inference-runtime/contracts/f017-event06-v12-to-v11-numerical-authority-bridge-v4.json`
  SHA-256 `6b01b3a41ebe1c88c594c37acd76bff5a96ae33d6ebbdab0204ae48241c03b9c`;
- bridge lifecycle:
  `specs/017-rust-native-inference-runtime/contracts/f017-event06-v12-to-v11-bridge-lifecycle-v4.json`
  SHA-256 `4479d35a348b10c0f68122599e3e46e6f4a0a1bb4414ba81efcbdfeac5fa2343`;
- canonical bridge digest:
  `b7f473d7066a3420170cb6fde73bdfd4955f1912d3d8977d9ca0b003c83c06cd`.

Bind unchanged numerical/result authority:

- numerical V4 contract:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-full-checkpoint-oracle-numerical-contract-v4.json`
  SHA-256 `a555abe0ff2aff03a693ac7313d4af17061d01766e90971d92a7ba528f4995f2`;
- V11 result authority:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-oracle-result-authority-v11-v2.json`
  SHA-256 `4fd71e90f4184e5f2c7449eac6089f7392f1cc0d1961aecb0243f7ef723af101`;
- V12 scientific access:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-full-checkpoint-oracle-scientific-access-v12.json`
  SHA-256 `cad79cd9a87897347d096ad698e7dc2da3f0fe18b13aa124ef559dc9691a450d`.

Recompute every manifest binding. Require zero mismatch, Event 06 unexecuted,
no live Event 06 authority, no package start, no checkpoint root resolution or
access, no consumed Event 06 identity, no P1 authority/action, and historical
master ledger exactly `175`.

Query authoritative GitHub records and require:

- FULL_NATIVE run `33141124246` at `7fbc641339f65619f77cca78a86eabc4d19277b7`:
  PASS, both native jobs PASS, required/unexpected skips `0/0`;
- manifest-bound EVIDENCE_ONLY run `33144107149`: PASS;
- final EVIDENCE_ONLY run `33145999564` at
  `58d3984686e829d7e434777c9bf3d7ac485ed459`: PASS, native jobs `0`.

Do not trust copied CI prose.

---

# 3. Load-bearing measured bytes and call path

Before approval, reproduce the implementation measurement and at minimum these
exact bytes:

- V12 candidate builder:
  `scripts/research/f017_corrected_oracle_authorization_v12.py`
  SHA-256 `d62e4504817836a692d2b00f62d1d7889cb548e49f72e8c5364f9195ce11dcf3`;
- V12 installed-authority validator:
  `scripts/research/f017_checkpoint_identity_authority_v12.py`
  SHA-256 `448871049f275e9d506ce92344816695749839b424504802916f970861e846ee`;
- V12 capability validator:
  `scripts/research/f017_checkpoint_identity_capability_v12.py`
  SHA-256 `c809e13e7665a450639d1be6fbaab75201a9607c33f349d6d719bf0e612a3f15`;
- V12 identity producer:
  `scripts/research/f017_checkpoint_identity_producer_v12.py`
  SHA-256 `419a8f5395368b3a2066d2c3ef5b19d1f14ef2bf49cc4ca8a19e2031e7880dbe`;
- V12 package/identity coordinator:
  `scripts/research/execute_f017_corrected_oracle_event_v12.py`
  SHA-256 `ed7acb311eb6c537551b8b52554b6b12435de02bc397888ee7ae9553a8687d5e`;
- fixed bridge coordinator:
  `scripts/research/execute_f017_corrected_oracle_event_v12_bridge.py`
  SHA-256 `aa99e0071c1654eace328cabfb12c02c99858dd1ffaaa7c53d2bea601949b5fd`;
- sealed bridge authority:
  `scripts/research/f017_event06_numerical_bridge_v1.py`
  SHA-256 `b7d8eaedcd87d4d8363a14977750f59406b171677a87417aebe31d11ed04112e`;
- execution-plan validator:
  `scripts/research/f017_event06_execution_plan_v1.py`
  SHA-256 `e6bc9020eb0262bca9ce9bc39b526337f1068da4e77edf854ebfbcf6cc1fd499`;
- V12 primary bridge wrapper:
  `scripts/research/f017_corrected_oracle_primary_wrapper_v12.py`
  SHA-256 `a95f0720e0d6f476b934412beabc1e8823940416472ebd500b4deb5c9856c4b5`;
- V12 secondary bridge wrapper:
  `scripts/research/f017_corrected_oracle_secondary_wrapper_v12.py`
  SHA-256 `119bc41cfc4790ebf93435f112fef3a1c6e3c96d3488cd574782628134c71d95`;
- unchanged primary numerical core:
  `scripts/research/f017_corrected_oracle_primary_numerics_v2.py`
  SHA-256 `657cdff9ee833cb2b3a0b3fa71b6cbc3dd1e0fbc71b74b9bbff9dca6b5b76767`;
- unchanged secondary numerical core:
  `scripts/research/f017_corrected_oracle_secondary_numerics_v2.py`
  SHA-256 `e3670b22ac71bad7523efe1e47b00f2345d1f103d2af8f7592e2f3f8c793a791`;
- V11 result artifact closure:
  `scripts/research/f017_result_artifacts_v11.py`
  SHA-256 `d82af8629c8ad989baa760c13cea94a7e42bd3628aa6a5e24a6795dc33fa4ae4`;
- V11 comparison authority:
  `scripts/research/f017_binary_comparison_authority_v11.py`
  SHA-256 `10235d8482aa66a318d7e97b7d3b9fbf27859a732cf67bf29d9cbcd19596e352`.

Require the production call path exactly:

```text
VALIDATED_READINESS
 -> V12_CANDIDATE_TRIPLE_VALIDATED
 -> V12_INSTALLED_AUTHORITY_AND_RECEIPT_VALIDATED
 -> PACKAGE_START_GATE_VALIDATED
 -> DURABLE_PACKAGE_START
 -> V12_IDENTITY_TERMINAL_AND_FIVE_LEASES_VALIDATED
 -> EXECUTION_PLAN_VALIDATED
 -> BRIDGE_DERIVED_AND_VALIDATED
 -> PRIMARY_DURABLE_START
 -> PRIMARY_SINGLE_CALL
 -> PRIMARY_RESULT_TERMINAL
 -> SECONDARY_DURABLE_START
 -> SECONDARY_SINGLE_CALL
 -> SECONDARY_RESULT_TERMINAL
 -> INDEPENDENT_COMPARISON
 -> DESCRIPTOR_RELEASE
 -> ACCOUNTING_CLOSURE
 -> PACKAGE_TERMINAL
```

The live execution entry point is exactly `execute_event06_bridge`. Do not
manually splice the old V12 coordinator to V11 wrappers, call the identity
producer outside the fixed path, pass callbacks, or supply caller-created
durable-start or closure digests. Invoke the fixed entry point exactly once.

No load-bearing implementation change is permitted under this GO. If any
runtime, authorizer, bridge, wrapper, numerical, result, comparison, release,
accounting, or lifecycle byte must change, expire the GO before approval and
bank a truthful pre-mint terminal failure with zero checkpoint access.

---

# 4. Pre-mint ownership, memory, and no-access guard

Before operator approval, execution-plan rendering, or candidate creation:

1. verify machine model `Mac Studio M1 Ultra` and architecture `arm64`;
2. verify no competing F017 oracle process, lease owner, package lock, or
   unresolved runtime root exists;
3. verify every fresh identity and all authority/state/evidence leaves below
   are absent;
4. verify Event 04 retry false and Event 05 disposition
   `TERMINAL_FAILURE_NO_RETRY`;
5. verify the prior Event 06 GO is `EXPIRED_BEFORE_APPROVAL`, Event 06
   executed/package-start/live-authorization counters are zero, and all five
   `V12-1` identities remain unconsumed and prohibited;
6. verify primary and secondary Event 06 execution counts are zero;
7. verify historical master ledger exactly `175`;
8. verify P1 attempt 2 and live P1 authorization absent;
9. resolve machine-local aliases privately and prohibit serialization;
10. verify checkpoint root/shard opens, identity hashes, payload reads, mmaps,
    tensor reads, and numerical operations are zero in this graph;
11. obtain fresh memory through the accepted parser and require at least
    `17,179,869,184` available bytes;
12. verify checkpoint-root ancestry, six exact shard basenames, metadata sizes,
    and geometry without opening or reading shard payloads;
13. rerun the accepted production-shaped no-access call-path proof and require
    the exact bridge digest and zero side effects.

`CHECKPOINT_ROOT`, `SOURCE_REPO_ROOT`, `STATE_ROOT`, and notification topic are
machine-local capabilities. Never print or serialize their resolved values.
Do not use shell globbing to select checkpoint, state, evidence, or execution
roots.

Any guard failure expires this GO before approval. Do not repair and continue
under the same GO.

---

# 5. Fresh one-shot identities and plan binding

Use exactly these new identities:

- operator approval: `F017-CORRECTED-ORACLE-EVENT-06-OPERATOR-GO-V12-2`;
- live authorization: `F017-CORRECTED-ORACLE-LIVE-AUTHORIZATION-06-V12-2`;
- package attempt: `F017-CORRECTED-ORACLE-PACKAGE-ATTEMPT-06-V12-2`;
- primary event: `F017-CORRECTED-ORACLE-PRIMARY-EVENT-06-V12-2`;
- secondary event: `F017-CORRECTED-ORACLE-SECONDARY-EVENT-06-V12-2`.

Require all five pairwise distinct, grammar-valid, globally unused, absent
from machine-local state/evidence roots, and absent from repository history as
live identities. The five `V12-1` identities are permanently ineligible for
this fresh GO even though they were never minted.

Render and validate one canonical 25-field execution plan through the measured
validator. It must bind the three execution identities, measured implementation
head/tree, measurement SHA, six exact shards/roles, tensor catalog, numerical
V4 files, V11 result/comparison/release/accounting bytes, attempts `1`, retries
`0`, and resume `false`.

Render one canonical event-identity plan that additionally binds:

- this prompt path, exact prompt commit, prompt SHA-256, and human-GO digest;
- decision `GO_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06`;
- Event 06, identity generation V12, bridge digest above, numerical V4, result
  authority V11, scope `PRODUCTION`, and operation
  `CORRECTED_FULL_CHECKPOINT_ORACLE`;
- starting evidence head/tree and measured implementation head/tree;
- final authority/readiness/bridge declarations and exact hashes;
- all five fresh identities;
- exact execution-plan SHA-256;
- attempts `1`, retries `0`, resume `false`;
- maximum one primary and one secondary execution;
- Event 04/05 retry false, prior Event 06 resume false, and P1 prohibited;
- fresh bounded approval interval and complete failure policy.

Canonicalize the execution plan first; bind its digest into the event plan;
bind the event-plan digest into the V12 candidate. Bank both using exclusive
no-replace creation, file and directory fsync, descriptor-relative readback,
and exact SHA verification. Reconstruct both from trusted inputs immediately
before installation and byte-compare them.

The inert Event 06 template remains inert. Never edit it into live authority.

---

# 6. One-shot execution graph

```text
E0  PROMPT_GO_PARENT_DUPLICATE_GUARD
 -> E1 SOURCE_CI_MANIFEST_AND_MEASURED_BYTES_RECONCILIATION
 -> E2 PRE_MINT_OWNER_MEMORY_NO_ACCESS_AND_FIXED_CALL_PATH_PROOF
 -> E3 FRESH_EXECUTION_PLAN_EVENT_PLAN_APPROVAL_AND_V12_CANDIDATE
 -> E4 CANDIDATE_TRIPLE_EXACT_INSTALL_RECEIPT_AND_INSTALLED_TRIPLE
 -> E5 EXECUTE_FIXED_V12_BRIDGE_ENTRYPOINT_EXACTLY_ONCE
      -> PACKAGE_START_GATE_AND_DURABLE_PACKAGE_START
      -> V12_SIX_SHARD_IDENTITY_AND_FIVE_DESCRIPTOR_LEASES
      -> DERIVE_EXACT_VALIDATED_BRIDGE
      -> PRIMARY_BINARY64_EXACTLY_ONCE_AND_TERMINAL
      -> SECONDARY_BINARY32_EXACTLY_ONCE_AND_TERMINAL
      -> INDEPENDENT_COMPARISON
      -> FIVE_DESCRIPTOR_RELEASE
      -> ACCOUNTING_AND_PACKAGE_TERMINAL
 -> E6 APPEND_ONLY_EVIDENCE_AND_EVIDENCE_ONLY_CI
 -> E7 GEMINI_CHALLENGE_AND_BOUNDED_NONRUNTIME_EVIDENCE_REPAIR
 -> E8 OPUS_ARBITRATION_AND_FINAL_EVENT06_DECLARATION
 -> E9 RESPONSE_CHECKSUM_NOTIFICATION_AND_PRIVATE_HANDOFF
 -> STOP_BEFORE_P1_ATTEMPT_2
```

Persist durable graph state after every completed transition. A restarted
worker reconstructs from committed/durable evidence and never repeats a
completed execution node. Exactly one owner may hold the package lease.

---

# 7. Candidate, exact installation, and fixed entry point

Validate final readiness through the measured readiness validator. Build the
V12 candidate only through the accepted builder using:

- authority scope `PRODUCTION`;
- operation `CORRECTED_FULL_CHECKPOINT_ORACLE`;
- fresh V12-2 authorization/package identities;
- privately resolved `CHECKPOINT_ROOT`;
- exact event-identity-plan SHA-256.

Require candidate schema 28 fields and installed schema 30 fields. Reject
aliases, coercions, unknown fields, substituted execution plan, substituted
event plan, substituted bridge digest, or any V11 field injected into the V12
candidate.

Run primary, secondary, and identity-producer candidate validators. Require
all three PASS with zero durable state, installation, checkpoint access,
numerical operation, or consumed ID. Reconstruct and byte-compare the candidate
immediately before install.

Install once through exclusive no-replace creation with file/directory fsync,
descriptor-relative readback, exact shared-field/candidate-SHA binding, and
installation receipt binding. Run all three installed-authority validators
against the expected candidate. Direct lower-level validation without the
expected candidate is prohibited.

Invoke `execute_event06_bridge` exactly once with the validated execution plan,
canonical event plan, candidate, installed authority, receipt, fresh package
ID, and separately owned identity/primary/secondary/package/terminal paths.
Do not separately create a package start or separately call the identity or
consumer stages.

Any failure before installation expires the GO. Any failure after installation
preserves its exact durable prefix and is terminal with no retry.

---

# 8. Package identity, numerical bundles, and bridge closure

The fixed entry point must validate the package-start gate, then bank exactly
one package durable start before original-checkpoint access.

Identity requirements:

- shards opened in exact ordinal order 1 through 6;
- exactly six complete identity hashes;
- total bytes hashed `238,458,632,928`;
- shard 1 `IDENTITY_ONLY`, fully hashed and closed before inference;
- shards 2-6 `GRAPH_PAYLOAD`;
- exactly five validated descriptor leases;
- no path reopen after descriptor acquisition;
- canonical identity evidence and one complete identity terminal before
  primary durable start.

Primary requirements:

- one durable start and exactly one binary64 core call;
- 79 layers, 11 formats, five graph shards materially consumed;
- 1,410 graph tensors resolvable and 399 non-access tensors denied;
- prompt token `9703`, position `0`, empty KV, causal mask, greedy argmax,
  top-N `32`;
- final hidden `f64le[6144]`, 49,152 bytes;
- final normalized `f64le[6144]`, 49,152 bytes;
- full logits `f64le[154880]`, 1,239,040 bytes;
- immutable distinct payload leaves plus routing, top-32, manifest, receipt,
  result terminal, consumer terminal, index, and bridge bundle binding.

Secondary is ineligible until primary terminal binding. Then require:

- one durable start and exactly one independent binary32 core call;
- no primary payload used as input or target;
- final hidden `f32le[6144]`, 24,576 bytes;
- final normalized `f32le[6144]`, 24,576 bytes;
- full logits `f32le[154880]`, 619,520 bytes;
- the same immutable closure classes and exact bridge bundle binding.

If primary fails, secondary never starts. If secondary fails, comparison never
starts. No numerical stage may be rerun for evidence, notification, CI, or
review failures.

Validate comparison directly from raw committed payloads and bundle closures.
Frozen thresholds:

- maximum absolute error `0.0065169706285814755`;
- RMSE `0.003463567697419031`;
- cosine minimum `0.9999999985448085`;
- top-N `32`.

Return exactly one classification:

- `EXACT_EXPECTED_TOKEN_STABLE`;
- `NUMERICALLY_STABLE_TOP_K_ONLY`;
- `TOP1_UNSTABLE_WITHIN_FROZEN_UNCERTAINTY`;
- `ORACLE_DISAGREEMENT`;
- `ORACLE_EXECUTION_FAILURE` only when execution/evidence closure fails before
  complete comparison.

Only after classification is durable may historical diagnostics for tokens
`21615`, `17351`, and `154820` run. They are prohibited as targets/success
predicates and cannot alter classification.

Require exactly five successful descriptor closures, no duplicate/unknown
close, zero live leases, coordinator-derived comparison/release/accounting
bindings, ten adjacent transition records, validated chain head, V11 closure
root, and one V12 bridge package terminal. Callers may not supply closure
digests.

---

# 9. Failure, mutation, and evidence policy

Before approval: create no approval, candidate, authority, state, checkpoint
access, or numerical operation.

After approval but before install: preserve approval and plans; install
nothing; checkpoint access remains zero.

After install but before package start: preserve installed authority/receipt;
package delta and checkpoint access remain zero.

After package start: preserve the exact durable prefix, derive the modeled
phase-specific outcome, release every acquired descriptor exactly once, bank a
terminal failure, and stop.

After primary/secondary start: preserve every produced numerical byte and
terminalize from the durable prefix. Never rerun inference.

No load-bearing implementation mutation is permitted. After terminalization,
tracked changes are limited to append-only execution evidence, validators and
indexes for those new evidence bytes, CI-census files, review packets, and
declarations. A change to measured runtime/authority/bridge/numerical/result
bytes or any change requiring re-execution invalidates readiness and cannot
authorize a rerun.

Never amend a pushed commit, force-push, rewrite history, delete evidence, or
replace a terminal artifact.

After terminalization:

1. validate schemas, canonical bytes, hashes, causal closure, access census,
   geometry, leases, and exact execution counts;
2. commit/push append-only source evidence and verify parity/clean worktree;
3. require EVIDENCE_ONLY CI PASS with native jobs `0`;
4. require Gemini CHALLENGE review of committed bytes;
5. permit only bounded nonruntime evidence repair;
6. require Opus ARBITER review from a detached read-only checkout;
7. bank one final Event 06 declaration/manifest with every load-bearing result;
8. require zero blocking, required, rejected, or unresolved readiness-critical
   finding.

Completed execution nodes are never rerun during review. P1 preparation
readiness may be `YES` only if the complete corrected classification and
independent arbitration support it. Do not mint P1 authority or execute P1.

---

# 10. Privacy, publication, and final response

Apply `Prompts/F017/PRIVACY-POLICY.yaml` before every prompt-repository commit.
Retain only machine model/generic specs, public names, repository-relative
paths, branches, commits, trees, CI runs, and artifact hashes.

Use only typed aliases for local roots/topic. Never serialize usernames,
absolute paths, hostnames, private addresses, internal DNS, mount/share names,
notification topics, credentials, tokens, or unrelated topology.

After final source evidence is committed and pushed:

1. render the paired Markdown response from committed evidence;
2. identify this exact prompt commit/path/SHA, source starting/final
   head/tree, measured implementation, bridge digest, CI, verdict,
   classification, P1 readiness, and safety state;
3. sanitize and scan, write adjacent checksum, fetch live prompt control, and
   require this prompt remains an ancestor and Sequence 4 remains unconsumed;
4. preserve all concurrent MacBook artifacts, `Prompts/F017/STATE.json`, and
   Feature Loop policy state;
5. stage only the paired Mac Studio response and checksum;
6. commit/push by normal fast-forward compare-and-swap and verify remote bytes;
7. deliver the established local notification without publishing its topic;
8. leave chat handoff pending and print the same substantive response with
   commit-pinned URL and SHA-256.

Report:

1. prompt/GO/source/measured/bridge authority reconciliation;
2. pre-mint ownership, memory, fixed call path, and zero-access result;
3. plan SHAs, approval SHA, and all five fresh identities;
4. candidate/installed triples, receipt, and package gate;
5. six-shard identity accounting;
6. primary/secondary executions and six payload identities;
7. independent comparison and exact classification;
8. descriptor release, transition chain, accounting, and package terminal;
9. final evidence head/tree, EVIDENCE_ONLY CI, Gemini, and Opus;
10. exact safety state, P1 readiness, and next safe action;
11. commit-pinned response URL and response SHA-256.

Always state:

```text
TARGET_MACHINE: MAC_STUDIO_M1_ULTRA
EVENT_04_RETRY: NO
EVENT_05_RETRY: NO
PRIOR_EVENT_06_RETRY_OR_RESUME: NO
EVENT_06_EXECUTED: YES/NO
EVENT_06_PACKAGE_STARTED: YES/NO
LIVE_EVENT_06_AUTHORIZATION_CREATED: YES/NO
PRIMARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0/1
SECONDARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0/1
ORIGINAL_CHECKPOINT_SHARD_OPENS: <exact count>
ORIGINAL_CHECKPOINT_IDENTITY_HASH_READS: <exact count>
ORIGINAL_CHECKPOINT_PAYLOAD_READS: <exact count>
ORIGINAL_CHECKPOINT_MMAPS_OR_TENSOR_READS: <exact count>
P1_ATTEMPT_2_EXECUTED: NO
LIVE_P1_ATTEMPT_2_AUTHORIZATION_CREATED: NO
HISTORICAL_MASTER_LEDGER: 175
```

Return exactly one oracle classification and exactly one P1 readiness value:

- `READY_TO_PREPARE_P1_ATTEMPT_2_AUTHORIZATION: YES`; or
- `READY_TO_PREPARE_P1_ATTEMPT_2_AUTHORIZATION: NO`.

Then stop. Do not mint P1 authority and do not execute P1 attempt 2.
