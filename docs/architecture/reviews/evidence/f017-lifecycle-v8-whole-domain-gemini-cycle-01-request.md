# F017 lifecycle V8 whole-domain review — Gemini cycle 01

Use a fresh `gemini-3.1-pro-high` AGY session at high effort. Review the exact committed bytes containing this request and the implementation measured at `a740687e61e9632ea6a957f52789acfb513de5ae`, tree `b9fd57f0d82761b8ee7eba2cec75b0fdb0b9daa8`. Repository evidence outranks this request. Do not modify files, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Independently attack the completed V8 domain, not merely the declared evidence. Reconstruct C7-N1 and C7-N2 and verify their controlled `ValueError` repairs in the production implementation. Probe strict integer/non-bool descriptor modes, the portable `[0, 2**16)` domain, check ordering before `stat.S_ISREG`, exact descriptor dictionary and key censuses, typed and grammar-validated lease IDs before set construction, and normalization of malformed runtime evidence. Inspect the independent type-safety checker and its import separation.

Recompute the 95-artifact/94-edge causal DAG, all 48 legal outcomes, 1,223 symbolic artifacts per complete repetition, zero self/future references, transitive SHA closure, exact five-descriptor continuity for ordinals 2 through 6, zero path reopens, descriptor inheritance and release, terminal obligations, path timing, and all safety invariants. Inspect the production V8 parser, authorizer, coordinator, identity producer, lease manager, target sources, wrappers, comparison, accounting, serializer, artifact banker, active-generation registry, scientific-access authority, capabilities, inert fixture, and measurement manifest. Confirm active V8 does not itself imply an Event-04 operator GO or live authorization.

Re-run or inspect the 304 rejected design/runtime mutations, the 20 synthetic success packages (10 minimal and 10 mixed-format authority packages), the fresh-process identity/continuity qualifications, all failure-outcome constructions, runtime malformed-data probes, the production-shaped no-access rehearsal, numerical V3 regression/capability gates, tombstones, and exact-head FULL_NATIVE CI. Determine whether the evidence is sufficient for one future Event-04 authorization preparation under a separate human GO. Report all material findings, defense-in-depth observations, material disagreement, and an unconditional `ACCEPT` or `REJECT` verdict.
