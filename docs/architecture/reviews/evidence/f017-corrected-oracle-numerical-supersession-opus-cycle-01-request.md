# F017 corrected-oracle numerical-authority supersession — Opus cycle 1

Use a fresh `claude-opus-5` session at high effort. Review exact committed bytes at `b69117800eb3123cc61ef7d2a5537cea750184a9` in a clean detached read-only worktree. Repository evidence outranks this request. Do not modify files, access any original checkpoint shard, mint authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Independently reconstruct the starting authority at `84f0d1dc3e60a4151329ed82773880951ee3e618` and attack:

1. The complete symbol inventory and whether every old symbol is uniquely classified.
2. Whether the new primary and secondary pure cores are checkpoint-, authority-, access-event-, lifecycle-, file-I/O-, and target-CLI-free.
3. Whether target sources contain only authorization-bound data access/decoder dispatch and no graph, routing, normalization, attention, expert, or result arithmetic.
4. Whether primary and secondary numerical independence is preserved.
5. The exact historical Git-byte manifest, symbol mapping, AST/extraction scripts, and 24 full-result old/new equivalence comparisons.
6. The complete checkpoint-free qualification: seeds 18101-18112, three processes, 44 decoder cases, all 11 formats, 16 mutations, and fresh-process runs.
7. The real v6 parser/wrapper/target-source/pure-core chain exercised against six-shard file-backed synthetic packages, and whether the original checkpoint is structurally excluded.
8. Numerical contract v2 for any formula, arithmetic-class, route, context, threshold, top-N, failure-class, or target-quarantine drift.
9. Current-tree retirement of both old combined target scripts and every v1/v2/v3 mint/coordinator/wrapper surface. Directly invoke them and verify nonzero `HISTORICAL_ONLY` behavior with zero side effects.
10. Whether historical reconstruction uses exact immutable Git bytes rather than current tombstone bytes.
11. The CI historical/current split and whether any workflow still treats a superseded source as current executable authority.
12. Whether any new active wrapper imports the retired combined scripts or duplicates graph code.

Rerun validators, unit tests, and representative equivalence/target-adapter cases. Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required findings prevent acceptance. State whether any finding requires an intended formula change, threshold change, original-checkpoint access, or loss of primary/secondary independence.

Required terminal verdict, exactly one:

- `ACCEPT_CORRECTED_ORACLE_NUMERICAL_AUTHORITY_SUPERSESSION`
- `REJECT`

Confirm: Event 02 retry no; Event 03 retry/resume no; Event 04 authorization absent; Event 04 unexecuted; primary/secondary real executions zero; original checkpoint access zero; P1 attempt 2 absent; historical master ledger 175.
