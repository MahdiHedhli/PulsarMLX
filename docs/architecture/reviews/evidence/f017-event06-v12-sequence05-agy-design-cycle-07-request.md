# F017 Event 06 Sequence 5 — Antigravity Design Challenge, Cycle 7

Review the exact detached, read-only repository commit
`235ce844278f21df84c076e87868848a4544c1d7`, tree
`039c0405cc155b3346b313f4faa5200523d7b626`.

This is design-only review. Do not modify repository state. Do not resolve,
stat, enumerate, open, hash, read, or mmap any original checkpoint path. Do
not create live authority, execute Event 06, or execute numerical operations.

Independently run these no-access commands in the supplied detached worktree:

```text
python3 scripts/research/generate_f017_event06_sequence05_design_v7.py --check
python3 scripts/research/validate_f017_event06_sequence05_design_v4.py
python3 -m py_compile scripts/research/generate_f017_event06_sequence05_design_v7.py scripts/research/validate_f017_event06_sequence05_design_v4.py
```

Read the exact recovered Cycle-6 Opus request, exact response, provider
envelope, provenance, normalized result, and the eight-row Cycle-7 repair
ledger. Challenge every one of the eight minimum repairs in the source Opus
response rather than relying on generated PASS labels:

1. Derive B2, R6, A1, A2, and A3 from allowlisted repository facts. Inspect
   the AST guard and prove no literal, tautological, constant-foldable,
   swallowed-error, or reviewer-result-echo predicate remains. Execute all
   five isolated negative mutations and confirm each fails only its intended
   predicate.
2. Confirm the implementation measurement is v2 and its bridge digest,
   implementation head, and tree equal the bridge declaration fields.
3. Resolve every current prepared-manifest binding with `git show` against the
   manifest's declared head/tree, not the working tree. Confirm 12 current and
   9 future roles, exact key/role/binding censuses, no traversal, no self/final
   or future dependency, and no false final-readiness claim.
4. Confirm both recovered Cycle-6 review provenance documents have exactly the
   contract's 21 fields and bind exact request, response, normalized result,
   provider envelope/session facts, timestamps, exit status, and model
   availability truthfully.
5. Recompute 86 + 86 + 34 + 18 + 100 = 324 and confirm the qualification role
   floor is also 324. Attack the named alias and race factorization.
6. Confirm all 16 failure outcomes map to real state-machine transitions and
   every `requires_write` value equals that edge's write class.
7. Confirm Cycle-04 A1–A6 and Cycle-05 A1–A3 remain distinct, each has named
   evidence, and graph/claim counters reconcile to the Cycle-6 source review.
8. Confirm the qualification schema is supplied by a distinct SHA-bound
   external authority and the prepared-manifest validator implements the exact
   census, declared-tree binding, path safety, partition, and self-exclusion
   checks demanded by Cycle 6.

Also inspect the generator's `--check` reconstruction, supersession links,
repair-validation index, mechanical report, zero-access census, and the full
path diff from `9ac074e595be354618af9524b436f3cecf9474d9`.

Return structured findings with unique IDs, severity, exact path/line or JSON
field evidence, affected claim, and smallest repair. Report counts for
blocking, required, challenged/advisory, and unresolved findings. Acceptance
requires all four counts to be zero and exact final verdict:

`ACCEPT_F017_EVENT06_SEQUENCE5_DESIGN_CYCLE7`

Otherwise return exactly `REJECT` after the findings. No conditional
acceptance and no implementation recommendation that bypasses an open design
finding.
