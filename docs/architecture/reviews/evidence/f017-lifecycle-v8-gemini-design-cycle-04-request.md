# F017 lifecycle V8 causal design — Gemini design review cycle 04

Use a fresh `gemini-3.1-pro-high` session at high effort. Review exact committed bytes at `af782a4d` in a detached read-only worktree. Repository evidence outranks this request. Do not modify files or access original checkpoint shards.

This is the final budgeted Gemini design cycle. Reconstruct Opus cycle 02 and attack its repairs. Every failure transition now banks one atomic terminal capsule, so it creates no durable partial tail; the interface pins the atomic filesystem protocol and recursion terminator. Lease inception is the successful graph-payload access-event open and retained leases are derived from actual DAG access-event ranks. Every payload field has an exact constant, equality, nonnegative-integer, or strict type rule; critical constants are independently pinned; shard receipts derive expected size/digest from the committed checkpoint metadata and require observed equality; continuity and release reports equal the lease manifest; root authorities, actors, payload key censuses, accounting ranks, result fields, and safety declarations are independently enforced. Cleanup duplicate/unknown observations remain recordable in a conforming atomic capsule.

Run all design validation, six tests, and the 48-outcome symbolic constructor. Attempt consistently rehashed payload forgeries, coordinated generator/schema drift, actor/root drift, access-event rank changes, failure-capsule safety changes, partial-tail insertion, and cleanup anomaly evidence. Return exactly `ACCEPT` or `REJECT` with material findings.
