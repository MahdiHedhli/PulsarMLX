# F017 Event-04 execution-readiness final review — Opus cycle 02

Use a fresh `claude-opus-5` session at high effort. Review exact committed
implementation bytes at `313a4832d492c241cde738fc979d2b6890607906`,
tree `9a11ae968e9da26acd266243820c4e26d4e163c0`. FULL_NATIVE run
`32812068532` passed both the Apple Silicon workspace baseline and pinned
native MLX qualification, required native skips zero. Exact-head qualification
and rehearsal are the append-only `v2` evidence artifacts. Repository evidence
outranks this request. Work read-only. Do not access original checkpoint
payloads, mint or execute Event 04, or execute P1 attempt 2.

Reconstruct cycle-01 `B1`, `N1`, `N2`, and `D1` through `D8`, then attack the
repairs rather than trusting their disposition artifact:

1. Prove immutable mint evidence attests the threshold at mint without
   expiring installed bytes. Force a delayed parse and independently require a
   fresh, enforced package-start sample before package claim. Malformed gates
   must fail as controlled `ValueError`.
2. Prove live candidate shard records exactly equal the six committed
   checkpoint-metadata records, including names, sizes, digests, order, and
   roles; mutate one size and one digest.
3. Corrupt installed authorization before parsing and corrupt the receipt
   before handshake. Verify the installer-created, receipt/authorization-bound
   emergency root captures maximal durable failure evidence. No parse, scope,
   test-control, or root-preparation failure may escape uncapsulated after
   installation.
4. Mutate observed failure transition and last-completed artifact separately.
   The outcome comparison must reject either mismatch. Confirm qualification
   truthfully separates six authorizer-phase direct capsules from forty-one
   coordinator causal-bank injections while retaining 47 outcomes and 201
   executions.
5. Substitute synthetic roots into production and production names into
   synthetic execution; mutate the synthetic catalog SHA; create a hardlink to
   a synthetic shard. All must fail before unauthorized payload use.
6. Inspect readiness/operator-approval exact censuses, freshness, SHA binding,
   no-replace installation, emergency-root creation, and corrected authorizer
   documentation.
7. Inspect the workflow trap. Post-cleanup worktree census must execute on both
   success and failure paths.

Then rerun the complete execution-readiness attack from cycle 01: idempotent
release and close-evidence recovery; dynamic durable-start accounting; all 47
modeled outcomes with exact required/forbidden artifacts and zero generic
fallbacks; both independent descriptor-backed consumers using inherited FDs
from shards 2–6 with zero path reopen; 1,410 graph tensor bounds and 399
non-access denials; all 11 formats; deterministic evidence; enforced memory
gates; independent C7-N2 checks; generator `--check`; numerical contract V3;
and production-shaped no-access rehearsal.

Recompute unchanged numerical core/decoder SHAs. Verify original checkpoint
access zero, Event-04 authorization absent, Event-04 execution absent, P1
attempt 2 absent, attempts 1, retries 0, resume false, and historical ledger
175. List every finding exactly once as `BLOCKING`,
`NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`, with stable IDs. Both blocking
and non-blocking-required prevent acceptance. State material disagreement.
End with exactly one unconditional verdict:
`ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION`
or `REJECT`.
