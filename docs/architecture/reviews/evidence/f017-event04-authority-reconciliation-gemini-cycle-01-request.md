# F017 Event-04 Authority Reconciliation Gemini Review Cycle 01

Use a fresh `gemini-3.1-pro-high` AGY session at high effort. Review exact committed repository bytes at evidence head `c442e54e5004cf231e6571142dd60a8b3a141d78`. The accepted implementation measurement head is `418a4121ceeb396b56a83158ce2424c138ec7ffa`, tree `eb3374332e5b943a5d37379a3b16e0886af823a3`, and the reconciled authority bundle head is `d5d4c412180d7a32e25bafff6f25ef598438e885`. Repository bytes outrank this request.

Do not modify repository files, access original checkpoint shard payloads, mint or execute Event 04, run a real oracle, or execute P1 attempt 2.

Independently reconstruct the reported parser/coordinator mismatch. Compute SHA-256 from exact `measurement_head:path` Git bytes, inspect Git blob identities, compare implementation measurement V4, the old lifecycle declaration, the stale scientific-access V6 authority, reconciliation discrepancy evidence, corrected scientific-access V6 authority revision, corrected inert fixture, authority manifest, correction declaration, production-shaped no-access rehearsal, and CI census.

Attack all of the following:

- the implementation-identity authority hierarchy and source-of-truth rule;
- exact Git measurement and working-tree parity checks;
- scientific-access internal path/SHA bindings;
- measurement-manifest completeness and consistency;
- original lifecycle-declaration consistency and append-only correction treatment;
- attempts to excuse stale inner bindings with valid outer artifact SHAs;
- coordinated contract/manifest/declaration mutations while Git bytes remain unchanged;
- inert-fixture rebinding and refusal of live authority;
- candidate construction and installed-auth handshake binding;
- active generation V6;
- production-shaped rehearsal with zero checkpoint access;
- FULL_NATIVE run `32726480096` at `d5d4c412180d7a32e25bafff6f25ef598438e885` and EVIDENCE_ONLY run `32727677562` at `c442e54e5004cf231e6571142dd60a8b3a141d78`;
- required native skips zero and evidence-only native jobs zero;
- absence of Event-04 authority/execution and P1 attempt 2.

Run the independent reconciliation validator with all 18 mutations and rerun the corrected rehearsal if safe. Original checkpoint payload access is prohibited; the rehearsal must remain metadata-only.

Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. State material disagreement explicitly. Return exactly `ACCEPT` or `REJECT` as the final verdict.
