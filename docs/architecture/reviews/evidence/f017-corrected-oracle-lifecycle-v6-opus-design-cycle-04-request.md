# F017 Lifecycle V6 Opus Design Review Cycle 04

Use a fresh `claude-opus-5` session at high effort. Review exact committed bytes at `bb15fe27202410aed9842b6b18a8bdb974694949` in a clean detached read-only worktree. Git and committed evidence outrank this request. Do not modify the repository, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Cycle 03 rejected with NBR-1 (authority SHA fields were not verified against true bytes) and NBR-2 (finite-float/readback-SHA serialization did not cover raw numerical results). Reproduce both old defects and attack the repairs. Verify candidate validation now checks exact bytes for every declared authority path/SHA pair and both capability reports before install; synthetic qualification uses real bound SHAs and a structurally isolated synthetic root; unknown/substituted/missing authority bytes fail before install or checkpoint access.

Verify one canonical JSON byte domain applies recursively to all lifecycle authority and numerical-result evidence: finite binary64 values are lowercase `float.hex()` strings, nonfinite values fail, exact bytes include one newline, readback hashes cover complete artifacts, and no artifact embeds a self-SHA. Inspect the removal of the legacy pure-core `result_sha256` at the wrapper boundary and ensure complete logits/top-32/metrics remain validated and usable after decoding only at numerical edges.

Reconstruct the whole V6 semantic authority independently: all states/transitions/outcomes; event-accounting contract; required/forbidden evidence for unstarted consumers; path timing and root relations; installation receipt; candidate/install identity; authority activation; model/registry/matrix/schema/outcome/path/serialization byte anchors; exact semantic columns; independent-checker separation; numerical-v3 authority; historical tombstones; synthetic success/failure family; zero original checkpoint access; and absence of Event-04/P1 authority.

Attempt coordinated drift, forged authority-path substitution, wrong capability bytes, alternate serialization, float/hex confusion, fabricated receipt/terminal SHAs, and bypass of the independent checker. Use `BLOCKING`, `NON_BLOCKING_REQUIRED`, and `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required prevent acceptance. Return exactly `ACCEPT_LIFECYCLE_V6_SEMANTIC_DESIGN_FOR_IMPLEMENTATION` or `REJECT`; no conditional acceptance.
