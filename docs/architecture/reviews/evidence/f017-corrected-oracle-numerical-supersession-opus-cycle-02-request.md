# F017 corrected-oracle numerical-authority supersession — Opus cycle 2

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `7fff686ab2a576230dcf23c8e43ca8e4d74c35fa` in a clean detached read-only worktree. Repository evidence outranks this request. Do not access original checkpoint shards, mint authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Reconstruct cycle 1 from the committed request, response transcript, normalized result, and repair disposition. Independently verify:

1. `F1`: target-source policy enumerates all methods, rejects `matvec` and `transpose_mv` injected into either adapter, and cannot be bypassed by nesting arithmetic in a class.
2. `F2`: the validator exactly binds both pure cores, both target sources, both decoder authorities, the separation architecture, historical manifest, numerical requalification bytes, and all corresponding tree/evidence SHAs.
3. `F3`: both prior one-shot extractors are now non-mutating provenance audits that reconstruct exact historical symbol/method origins and fail on stale target arithmetic or constructor drift.
4. `F5`: pure-core import and dynamic-import policy fails closed for file/network/native escape surfaces.
5. `F7`: `FULL_NATIVE` now reruns the complete historical/successor equivalence and target-adapter qualifier and byte-compares its output with the banked qualification.
6. `F8`: secondary target matrices enforce catalog geometry and row bounds without changing numerical arithmetic.
7. Regenerated numerical contract v2 and requalification evidence bind the repaired target SHA while formulas and thresholds remain unchanged.
8. All ten tombstones remain fail-closed and historical Git-byte reconstruction remains exact.

Rerun the numerical validator, provenance audits, mutation tests, complete requalification, and representative retirement invocations. Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`. Both blocking and non-blocking-required prevent acceptance.

Required terminal verdict, exactly one:

- `ACCEPT_CORRECTED_ORACLE_NUMERICAL_AUTHORITY_SUPERSESSION`
- `REJECT`

Confirm safety: Event 04 authorization absent; Event 04 unexecuted; original checkpoint access zero; real primary/secondary executions zero; P1 attempt 2 absent; historical ledger 175.
