# F017 lifecycle V8 causal design — Gemini design review cycle 03

Use a fresh `gemini-3.1-pro-high` session at high effort. Review exact committed bytes at `82dcc4cf` in a detached read-only worktree. Repository evidence outranks this request. Do not modify files, open/hash/mmap/pread original checkpoint shards, mint Event-04 authority, execute a real oracle, or execute P1 attempt 2.

Cycle 01 rejected mutation-census ambiguity and cycle 02 accepted the repaired original V8 design. Opus cycle 01 then found two blocking and five required constructibility/validator defects. Independently verify their repair: 48 outcome variants cover every durable success prefix; checkpoint-identity failures at shard ordinals 1–6 close exactly the leases already retained; evidence-banking failure never duplicates a completed release; failure release reports record duplicate and unknown closures; every failure banks a final declaration; checkpoint processing and the exact six filenames/sizes/digests are bound to their real authority; every DAG producer transition is represented in the lifecycle model; actors, edge semantics, accounting start ranks, outcome cuts, and safety-source pointers are exact; and the recursive package validator independently rejects forged schema, outcome, payload, root-authority, dependency, rank, identity, and package bindings even after consistent descendant rehashing.

Run the independent validator, five-test suite, and symbolic construction. Attack the repaired design beyond the named cases. Report material findings and return exactly `ACCEPT` or `REJECT`.
