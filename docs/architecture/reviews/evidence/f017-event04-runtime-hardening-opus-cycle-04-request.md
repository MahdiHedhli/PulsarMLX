# F017 Event-04 execution-readiness final review — Opus cycle 04

Use a fresh `claude-opus-5` session at high effort. Review exact committed
implementation bytes at `74159013e968dcd592a1b2a099c4976606c1aa0e`, tree
`a77e0fcb2a0c622a8c3ce96abce4cbcebb619417`. FULL_NATIVE run
`32817885238` passed workspace, pinned native MLX, and aggregate gates with
required native skips zero. Exact-head measurement, qualification, and
rehearsal are the append-only `v4` artifacts. Repository evidence outranks
this request. Work read-only. Do not access original checkpoint payloads,
mint or execute Event 04, or execute P1 attempt 2.

Reconstruct cycle-03 `N-01-C03` and `D-01-C03` through `D-07-C03`. Attack the
repairs rather than trusting their disposition:

1. Independently derive the complete repository-local import closure of the
   authorizer, coordinator, and both live wrappers. Require all 30 runtime
   modules to be present among 38 measured implementation paths. Change an
   unbound-looking transitive module and a bound active file in the working
   tree; both must invalidate render and install. Change a Git object at the
   accepted head or substitute a different head; fail closed.
2. Confirm render and irreversible install independently replay exact operator
   approval, readiness, freshness, canonical path, path uniqueness, runtime
   manifest, Git-byte, working-byte, shard census, tensor-plan, and candidate
   digest gates. Reproduce the prior `/tmp` alias candidate bypass against the
   installer entry point.
3. Replace an ancestor of each authorized terminal root with a symlink after
   installation. No capsule may reach the redirected tree. Test leaf symlink,
   collision, unreadable, absent, file, and permission failures; require the
   separately bound fallback or an honest maximal-constructible result.
4. Make both roots unavailable while five leases exist. All five leases must
   still be attempted and closed exactly once. The returned envelope must
   distinguish unreadable authority, binding mismatch, and unusable roots.
5. Derive event accounting from durable runtime artifacts rather than outcome
   literals. Derive controlled/generic status from the actual terminalization
   result. Mutate each independently and require qualification/CI failure.
6. Exercise absent/symlinked/unopenable synthetic and production roots plus
   invalid Git context. Module boundaries must normalize failures without raw
   `OSError` or `CalledProcessError` leakage.
7. Inject partial evidence-root creation and receipt banking failures. Before
   no-replace authority consumption, created empty roots must be cleaned and
   the failure classified. After installation, receipt failure must bank the
   installation-failure capsule in one of the two bound roots.

Then rerun the entire execution-readiness attack: DID-01 through DID-12;
idempotent release; dynamic accounting; complete terminalization; 47/47
modeled failures over 201 executions with exact causal prefixes and no generic
fallback; both consumers materially using inherited descriptors for shards
2–6 with zero path reopen; 1,410 graph tensors and 399 non-access denials; all
11 formats; deterministic evidence; enforced memory gates; independent C7-N2
checks; generator `--check`; historical-worktree cleanup; numerical contract
V3; production-shaped no-access rehearsal; and exact-head CI.

Recompute unchanged numerical core and decoder SHAs. Verify original
checkpoint access zero, Event-04 authorization absent, Event-04 execution
absent, P1 attempt 2 absent, attempts 1, retries 0, resume false, and historical
ledger 175. List each finding exactly once as `BLOCKING`,
`NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH` with a stable ID. Both blocking
and non-blocking-required prevent acceptance. State material disagreement.
End with exactly one unconditional verdict:
`ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION`
or `REJECT`.
