# F017 Event-04 runtime hardening whole-domain review — Gemini cycle 03

Use a fresh `gemini-3.1-pro-high` session at high effort. The CLI has explicitly mounted `/Users/mhedhli/Documents/Coding/PulsarMLX`; begin by `cd`-ing to that exact directory and prove `git cat-file -e 05876a3bdfec625ec9c72d771e26679bf6a1af45^{commit}` and `git cat-file -e fd517315f3e289d7abceb9ef73a793074d879fdf^{commit}` before reviewing. If those checks fail, report the environment failure only. Review implementation head `fd517315f3e289d7abceb9ef73a793074d879fdf`, tree `afb208452ebe35eceba7100899454ec1354b9f36`, and evidence descendant `05876a3bdfec625ec9c72d771e26679bf6a1af45`. FULL_NATIVE run `32809028957` passed. Do not modify files or access original checkpoint payloads.

Retest all cycle-01 implementation findings: active generation must remain `NONE`; the 47-outcome realizer must drive actual authorizer/coordinator transitions rather than manufacture a required set; the future-live authorizer must bind operator approval/readiness, enforce fresh mint memory, dual-validate exact candidate bytes, and implement canonical no-replace install/readback/receipt without qualification creating live authority; `execute_event04` must be a production entry with installed-auth validation, enforced package memory, and no test fault selector; both target sources must stream row-bounded data from five inherited descriptors in independent subprocesses without graph-path opens or full-model materialization; release banking failure must recover without double-close; and modeled failures must use exact transition/outcome/accounting with zero generic fallbacks. Verify authorizer documentation agrees.

Attack descriptor `pass_fds`, per-read `fstat` identity, offset/length/shard bounds for all 1,410 graph tensors, 399 non-access denials, all-shard material consumption, 11 formats, synthetic/production isolation, identity acquisition progress faults, emergency terminalization, generator `--check`, deterministic core/envelope separation, memory failures, C7-N2 regressions, worktree cleanup, unchanged numerical SHAs, and original checkpoint access zero. Re-run targeted tests and validators from the mounted repository when useful.

Attempts remain exactly 1, retries 0, resume false; do not recommend retry scaling. List findings exactly once as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`, with stable IDs and smallest repairs. State material disagreement. End exactly with `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION` or `REJECT`.
