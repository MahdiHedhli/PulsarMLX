# F017 Lifecycle-v5 Interface Review — Cycle 2

Use a fresh `claude-opus-5` high-effort session. Review exact committed target `af77aace06f591770c07f85b29f369db9feb33b3` in a clean detached read-only worktree. Repository evidence outranks this request. Do not modify files, access original checkpoint shards, mint authorization, create Event 04 state, execute real oracles, or execute P1 attempt 2.

Reconstruct cycle 1 from its exact response, normalized result, and repair disposition. Independently verify each B-1..B-5 and N-1..N-4 repair rather than trusting the disposition.

Required re-attacks:

1. Repeat every interface forgery from cycle 1: P1 permission, expected-token permission, side-effect limits, candidate authority, install-receipt requirement, schema generation, retry/resume, historical ledger, context/limit census, and live requirements.
2. Forge registry/matrix/schema documents consistently: fabricate unstarted-secondary bindings, delete a required terminal identity from all generated views, allow derivation/ignore mismatch, alter JSON paths and types. Require independent rejection.
3. For every terminal outcome, compare required/forbidden artifacts to computed distinct artifact path states. Confirm terminals/receipts/ledger indices exist exactly when required and candidate temporary bytes are removed after install.
4. Verify candidate and installed authorization have one realizable flat layout and that no generic bindings/payload schema conflicts with it.
5. Inspect every failure variant, especially `F06_INSTALLATION_FAILURE`, `T13F_PRIMARY_FAILURE`, and `T17_CLOSE_PRE_PRIMARY_FAILURE`; confirm source state, prior artifacts, and ledgers come from that branch rather than a global success spine.
6. Verify exact payload censuses, typed ledger-index artifacts, reservation/execution semantics, nonfinite parse rejection, full 36-pair directory relation matrix, post-create no-symlink rules, and exact measurement-manifest entry census.
7. Search for new gaps in lifecycle identity introduction/propagation, outcome closure, path satisfiability, schema bidirectionality, canonical bytes, or readback SHA semantics.

This is still the interface-design checkpoint; v2/v3 mechanical retirement and v5 runtime implementation remain the next implementation phase and should be classified accordingly unless the compact model falsely claims they are already complete.

Use `BLOCKING`, `NON_BLOCKING_REQUIRED`, `DEFENSE_IN_DEPTH`. Blocking or non-blocking-required means REJECT. Return exactly `ACCEPT_LIFECYCLE_V5_INTERFACE_FOR_IMPLEMENTATION` or `REJECT`.
