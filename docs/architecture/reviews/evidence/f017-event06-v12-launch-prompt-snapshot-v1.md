# PulsarMLX F017 Execution Graph
## Event 06 V12 Corrected Full-Checkpoint Oracle One-Shot Execution

# Operator directive

**GO.**

Authorize and execute exactly one corrected full-checkpoint oracle Event 06 on
`Mac Studio M1 Ultra` under the accepted V12 checkpoint-identity authority,
unchanged numerical authority V4, and unchanged result authority V11.

This is the fresh human GO requested by the accepted V12 readiness declaration.
It applies only to Event 06, only to the authority pinned below, and only to one
package attempt. It is not permission to retry or resume Event 04 or Event 05,
to execute Event 06 twice, or to prepare or execute P1 attempt 2.

This GO permits, after every pre-mint guard passes:

1. one fresh Event 06 operator-approval and event-identity plan;
2. one V12 candidate and one exact installed checkpoint-identity authority;
3. one package claim and durable package start;
4. six ordered checkpoint identity opens and complete identity hashes;
5. one primary binary64 full-model oracle execution;
6. one independent secondary binary32 full-model oracle execution;
7. six canonical numerical result payloads;
8. one independent comparison and one package terminal;
9. append-only evidence, EVIDENCE_ONLY CI, challenge review, and arbitration;
10. one P1 attempt-2 preparation-readiness decision.

This GO does not permit source implementation repair, changed measured runtime
bytes, changed checkpoint authority, a second package, any retry or resume,
numerical rerun for evidence defects, P1 attempt-2 authorization minting, or P1
attempt 2.

A failure is durable evidence, never permission to retry.

Repository evidence is authoritative. Git outranks this prompt.

---

# 1. Prompt authority and paired response

Prompt repository:

`MahdiHedhli/PulsarMLX-Prompts`

Prompt path:

`Prompts/F017/Mac-Studio-M1-Ultra/002__F017__Mac-Studio-M1-Ultra__Event-06-V12-corrected-full-checkpoint-oracle-one-shot-execution-go__prompt.md`

Adjacent prompt checksum path:

`Prompts/F017/Mac-Studio-M1-Ultra/002__F017__Mac-Studio-M1-Ultra__Event-06-V12-corrected-full-checkpoint-oracle-one-shot-execution-go__prompt.md.sha256`

Expected paired response path:

`Prompts/F017/Mac-Studio-M1-Ultra/002__F017__Mac-Studio-M1-Ultra__Event-06-V12-corrected-full-checkpoint-oracle-one-shot-execution-go__response.md`

Expected response checksum path:

`Prompts/F017/Mac-Studio-M1-Ultra/002__F017__Mac-Studio-M1-Ultra__Event-06-V12-corrected-full-checkpoint-oracle-one-shot-execution-go__response.md.sha256`

Before any source-repository write, approval, state creation, checkpoint access,
or numerical operation:

1. fetch this prompt from the exact commit-pinned launcher;
2. verify its SHA-256 against both the launcher and adjacent sidecar;
3. require the prompt commit to be an ancestor of current prompt-control `main`;
4. verify the paired response and checksum do not already exist;
5. verify no Sequence 2 handoff, terminal, notification, or duplicate response
   has already consumed this one-shot directive;
6. verify the previous Mac Studio Sequence 1 response remains the terminal
   Event 05 failure and Event 05 retry remains prohibited;
7. do not execute or launch any other committed directive.

Any mismatch expires this GO before source mutation and checkpoint access.

The MacBook Pro M2 Max Feature Loop lane is concurrent and independent. Do not
modify `Prompts/F017/STATE.json`, any MacBook prompt/response/handoff, or its
42 Ultracode policy state while publishing the Mac Studio response. A
fast-forward prompt-control movement is acceptable only when this exact prompt
commit remains an ancestor and the Sequence 2 terminal artifacts remain absent.

---

# 2. Accepted starting source authority

Source repository:

`MahdiHedhli/PulsarMLX`

Branch:

`feat/017-rust-native-inference-runtime`

Required starting evidence head and tree:

- head: `eb85661ea3f1579f7566a9c23e469c7785557bd2`
- tree: `938ca67a872591852b60c14de75fb1c0a47a6ee6`

Required measured implementation:

- head: `5b98e53e6d9c4e2c2a8ae91bd71f55001517f5d0`
- tree: `e27590470ffb6ddf3885e8ae4e1a399e211aa27e`

Require exact local/remote parity, clean worktree, no merge/rebase in progress,
and starting head equal to the required head. Do not accept a descendant as a
substitute. Any pre-existing source movement expires this GO before approval.

Accepted V12 terminal authority:

- final authority declaration:
  `docs/architecture/reviews/evidence/f017-event06-v12-checkpoint-identity-authority-final-declaration-v1.json`
  SHA-256 `09e1a2b0eb35ffa45ea635ef4c245705b6589e377a77bf980d80dc0650aecfe6`;
- execution-readiness declaration:
  `docs/architecture/reviews/evidence/f017-corrected-oracle-event06-execution-readiness-final-declaration-v12-v2.json`
  SHA-256 `33bf3456a7557a4be66bf534d117b8efb0cb82fd09b615acbf8e5a34e5a17e3d`;
- V12 authority manifest:
  `docs/architecture/reviews/evidence/f017-event06-v12-checkpoint-identity-authority-manifest-v10.json`
  SHA-256 `cd34c93987ad0f7dec1bbb8b922aaf084e4a187b20bcb92232ff54de22b51774`;
- checkpoint-identity contract:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-oracle-checkpoint-identity-authority-v12.json`
  SHA-256 `12f6d717edbdea385974d870445754b7163ce76dfef3ee936a72586307da9050`;
- exact final instantiability:
  `docs/architecture/reviews/evidence/f017-event06-final-readiness-instantiability-v3.json`
  SHA-256 `57c44e0a201b89cd198581412cac50dcc2198b36c843204cfb757517ea64dd4b`;
- final reconciliation:
  `docs/architecture/reviews/evidence/f017-event06-v12-final-authority-reconciliation-v1.json`
  SHA-256 `36460264ec71e699624e32d4a2d04258dd616080cab55c5f60b9851dfea494c6`;
- production-shaped no-access rehearsal:
  `docs/architecture/reviews/evidence/f017-event06-production-shaped-no-access-rehearsal-v12-v9.json`
  SHA-256 `93e87a4e6ce2e9e750656a41afbdde853eafc4e729e7ba9d1e201ca8457575f2`.

Recompute all 43 manifest bindings. Require zero mismatch, zero blocking or
required findings, zero unresolved claims, active generation `V12`, historical
ledger `175`, Event 06 not executed, no live Event 06 authority, no package
start, no original-checkpoint access, and P1 attempt 2 not executed.

Accepted CI:

- FULL_NATIVE run `33124907328` at implementation head `5b98e53e...`: PASS;
- manifest-bound EVIDENCE_ONLY reconciliation run `33127339764`: PASS;
- final EVIDENCE_ONLY run `33128506289` at evidence head `eb85661...`: PASS;
- required/unexpected FULL_NATIVE skips: `0/0`;
- final EVIDENCE_ONLY native jobs launched: `0`.

Query authoritative GitHub run records. Do not trust copied prose.

---

# 3. Load-bearing measured bytes

Require the manifest and implementation measurement to reproduce these exact
bytes before approval:

- V12 candidate builder:
  `scripts/research/f017_corrected_oracle_authorization_v12.py`
  SHA-256 `d62e4504817836a692d2b00f62d1d7889cb548e49f72e8c5364f9195ce11dcf3`;
- V12 candidate/installed validator:
  `scripts/research/f017_checkpoint_identity_authority_v12.py`
  SHA-256 `448871049f275e9d506ce92344816695749839b424504802916f970861e846ee`;
- V12 capability validator:
  `scripts/research/f017_checkpoint_identity_capability_v12.py`
  SHA-256 `c809e13e7665a450639d1be6fbaab75201a9607c33f349d6d719bf0e612a3f15`;
- V12 identity producer:
  `scripts/research/f017_checkpoint_identity_producer_v12.py`
  SHA-256 `419a8f5395368b3a2066d2c3ef5b19d1f14ef2bf49cc4ca8a19e2031e7880dbe`;
- V12 package gate and identity coordinator:
  `scripts/research/execute_f017_corrected_oracle_event_v12.py`
  SHA-256 `ed7acb311eb6c537551b8b52554b6b12435de02bc397888ee7ae9553a8687d5e`;
- primary wrapper V12:
  `scripts/research/f017_corrected_oracle_primary_wrapper_v12.py`
  SHA-256 `6fdb6bfdd98859618cfba39859f95acfa97c75427c3d2063104fa4bbc1b00ff5`;
- secondary wrapper V12:
  `scripts/research/f017_corrected_oracle_secondary_wrapper_v12.py`
  SHA-256 `c476299cc3a7ab9baa9d81bde0da1ef6b9916de1bb7ccb58a4bbf63c90962ca8`;
- readiness validator:
  `scripts/research/f017_event06_readiness_authority_v1.py`
  SHA-256 `d1ee8b80f7a6f9e778a7faffa8962a8795415db08b0c60308d01ee6650ba4bc8`;
- installed triple validator:
  `scripts/research/validate_f017_corrected_oracle_access_v12.py`
  SHA-256 `49c88c47570d81e3e7bddd29e3d85e2514cbd0795534b0af5f6c03d0fbb27ce9`;
- scientific-access contract V12:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-full-checkpoint-oracle-scientific-access-v12.json`
  SHA-256 `cad79cd9a87897347d096ad698e7dc2da3f0fe18b13aa124ef559dc9691a450d`;
- lifecycle outcomes V12:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-oracle-checkpoint-identity-lifecycle-outcomes-v12.json`
  SHA-256 `0e510a90a32ee9a066c5295b5119445947821f152aac1d78d433e36dfb828c0c`;
- numerical contract V4:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-full-checkpoint-oracle-numerical-contract-v4.json`
  SHA-256 `a555abe0ff2aff03a693ac7313d4af17061d01766e90971d92a7ba528f4995f2`;
- result authority V11:
  `specs/017-rust-native-inference-runtime/contracts/f017-corrected-oracle-result-authority-v11-v2.json`
  SHA-256 `4fd71e90f4184e5f2c7449eac6089f7392f1cc0d1961aecb0243f7ef723af101`.

The V12 execution coordinator is the package gate and identity stage, not a
license to invent a different full-model executor. Before approval, prove the
complete existing call path from the V12 package gate and identity producer to
the exact V12 primary and secondary wrappers, inherited V4 numerical
implementation, V11 result bundles, independent comparison, accounting,
release, and terminalization. Require exact argument, descriptor, lease,
receipt, and durable-transition compatibility.

No load-bearing implementation change is permitted under this GO. If the exact
committed path cannot be instantiated without changing runtime, authorizer,
validator, wrapper, numerical, comparison, accounting, or lifecycle bytes,
expire the GO and bank a pre-mint terminal failure with zero checkpoint access.

---

# 4. Pre-mint ownership and safety guard

Before operator approval or candidate rendering:

1. verify the machine is `Mac Studio M1 Ultra`, architecture `arm64`;
2. verify no competing F017 oracle process, lease owner, package lock, or
   unresolved runtime root exists;
3. verify all fresh IDs and their authority/state/evidence leaves are absent;
4. verify Event 04 retry is false and Event 05 has terminal disposition
   `TERMINAL_FAILURE_NO_RETRY`;
5. verify Event 06 executed/package-start/live-authorization counters are zero;
6. verify primary and secondary Event 06 execution counts are zero;
7. verify historical master ledger is exactly `175`;
8. verify P1 attempt 2 and its live authorization remain absent;
9. resolve machine-local aliases privately and prohibit their serialization;
10. verify original checkpoint shard opens, hash reads, payload reads, mmaps,
    tensor reads, and numerical operations are all zero in this graph;
11. obtain fresh memory through the accepted parser and require at least
    `17,179,869,184` available bytes;
12. verify checkpoint root ancestry, shard names, metadata sizes, and six-shard
    geometry without opening or reading shard payloads.

The checkpoint root is machine-local `CHECKPOINT_ROOT`. Never publish its
absolute value. Do not use shell globbing to choose checkpoint, state, or
evidence roots.

Any guard failure expires this GO before approval. Do not repair and continue
under the same GO.

---

# 5. Fresh identities and human-GO binding

Use exactly these fresh identities:

- operator approval: `F017-CORRECTED-ORACLE-EVENT-06-OPERATOR-GO-V12-1`;
- live authorization: `F017-CORRECTED-ORACLE-LIVE-AUTHORIZATION-06-V12-1`;
- package attempt: `F017-CORRECTED-ORACLE-PACKAGE-ATTEMPT-06-V12-1`;
- primary event: `F017-CORRECTED-ORACLE-PRIMARY-EVENT-06-V12-1`;
- secondary event: `F017-CORRECTED-ORACLE-SECONDARY-EVENT-06-V12-1`.

Require all five to be pairwise distinct, grammar-valid, globally unused, and
absent from machine-local roots and repository history as live identities.

Render a canonical operator-approval/event-identity plan that binds at least:

- this exact prompt path, prompt commit from the launcher, and prompt SHA-256;
- decision `GO_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_06`;
- Event 06, generation V12, scope `PRODUCTION`, and operation
  `CORRECTED_FULL_CHECKPOINT_ORACLE`;
- required starting head/tree and measured implementation head/tree;
- final declaration, readiness declaration, authority manifest, and their
  exact SHA-256 values;
- all five fresh identities;
- attempts `1`, retries `0`, resume `false`;
- one primary and one secondary execution maximum;
- Event 04/05 retry false and P1 attempt 2 prohibited;
- a fresh bounded approval interval;
- package-start-before-checkpoint-access ordering;
- exact failure and terminal policy in this prompt.

Bank canonical bytes using exclusive no-replace creation, file fsync,
directory fsync, descriptor-relative readback, and SHA-256 verification.

Pass the plan SHA-256 as `event_identity_plan_sha256` to the measured V12
candidate builder. Reconstruct the expected plan from trusted inputs and
byte-compare it immediately before candidate install. Mutation of the prompt
identity, decision, source authority, fresh IDs, limits, order, or constraints
must invalidate the expected candidate before installation.

The inert Event 06 template remains inert and is not approval. Never edit it
into a live document.

---

# 6. Execution graph

```text
E0  PROMPT_BYTES_PARENT_DUPLICATE_GUARD
 -> E1 SOURCE_AUTHORITY_CI_MANIFEST_RECONCILIATION
 -> E2 PRE_MINT_OWNER_MEMORY_NO_ACCESS_AND_CALL_PATH_PROOF
 -> E3 FRESH_APPROVAL_EVENT_PLAN_AND_V12_CANDIDATE
 -> E4 TRIPLE_VALIDATION_EXACT_INSTALL_AND_RECEIPT
 -> E5 PACKAGE_START_GATE_CLAIM_AND_V12_SIX_SHARD_IDENTITY
 -> E6 PRIMARY_BINARY64_ONE_EXECUTION_AND_BUNDLE
 -> E7 SECONDARY_BINARY32_ONE_EXECUTION_AND_BUNDLE
 -> E8 INDEPENDENT_COMPARISON_RELEASE_AND_PACKAGE_TERMINAL
 -> E9 APPEND_ONLY_EVIDENCE_AND_EVIDENCE_ONLY_CI
 -> E10 GEMINI_CHALLENGE_AND_BOUNDED_EVIDENCE_REPAIR
 -> E11 OPUS_ARBITRATION_AND_FINAL_EVENT06_DECLARATION
 -> E12 RESPONSE_CHECKSUM_NOTIFICATION_AND_PRIVATE_HANDOFF
 -> STOP_BEFORE_P1_ATTEMPT_2
```

Persist a durable graph state after each completed transition. A restarted
worker reconstructs from committed/durable evidence and never repeats a
completed execution node. Only one owner may hold the package lease.

---

# 7. Candidate, exact installation, and package gate

Validate the exact readiness declaration through
`validate_execution_readiness`. Build the checkpoint-identity candidate only
through `build_identity_candidate_from_readiness` with:

- authority scope `PRODUCTION` as selected by the measured builder;
- the exact fresh authorization and package IDs;
- privately resolved `CHECKPOINT_ROOT`;
- the exact event-identity-plan SHA-256.

Require the exact candidate schema with 28 fields, installed schema with 30
fields, aliases/coercions/unknown fields rejected, and operation
`CORRECTED_FULL_CHECKPOINT_ORACLE`.

Run the primary, secondary, and identity-producer candidate validators. Require
all three PASS with zero state, authority installation, checkpoint access,
numerical operations, or consumed IDs.

Immediately before installation, reconstruct the candidate from the validated
readiness and trusted event plan. Require byte identity.

Install once with exclusive no-replace creation, file and directory fsync,
descriptor-relative readback, exact candidate/install shared-field binding,
candidate SHA binding, and installation receipt binding. Run the primary,
secondary, and producer installed-authority validators against the expected
candidate. Direct lower-level installed validation without the expected
candidate is prohibited.

Invoke `validate_package_start` from the measured V12 coordinator. Require:

- candidate triple PASS;
- installed triple PASS;
- capability PASS;
- candidate SHA equals installed authorization SHA;
- receipt authorization/package identity exact;
- package claim eligible;
- checkpoint opens and reads zero;
- state created false.

Do not call the identity producer directly before this gate. Do not pass a
progress callback to it; callback capability must remain structurally absent.

Any failure before installation expires the GO. Any failure after installation
preserves the exact durable prefix and is terminal with no retry.

---

# 8. Package start and six-shard identity

After the package-start gate passes, atomically bank exactly one package claim
and durable package start. Only then may original-checkpoint access begin.

Invoke the measured V12 identity stage with the immutable validated installed
authority, exact package ID, `package_durable_start=true`, and the accepted
evidence directory.

Require:

- shards opened in exact ordinal order 1 through 6;
- exactly six complete identity hashes;
- total bytes hashed `238,458,632,928`;
- shard 1 role `IDENTITY_ONLY`, fully hashed and closed before inference;
- shards 2 through 6 role `GRAPH_PAYLOAD`;
- exactly five validated graph descriptor leases;
- no path reopen after descriptor acquisition;
- identity evidence canonical, fsynced, read back, and self-consistent;
- one `COMPLETE` identity terminal before primary durable start.

Identity failure is a terminal package failure. Release any acquired
descriptors, bank the modeled V12 phase-specific outcome, and stop. Do not run
primary or secondary and do not retry.

---

# 9. Primary and secondary result bundles

After complete identity closure, revalidate the five graph descriptors and bank
one primary durable start. Invoke the exact V12 primary wrapper and unchanged
binary64 numerical implementation once.

Primary requirements:

- core execution count exactly 1;
- all 79 layers and all 11 formats;
- shards 2 through 6 materially consumed;
- 1,410 graph tensors resolvable and 399 non-access tensors denied;
- prompt token `9703`, position `0`, empty KV, causal mask, greedy argmax,
  top-N `32`;
- no repeated final normalization or output projection;
- final hidden `f64le[6144]`, exactly 49,152 bytes;
- final normalized `f64le[6144]`, exactly 49,152 bytes;
- full logits `f64le[154880]`, exactly 1,239,040 bytes.

Bank the three primary files as distinct immutable leaves and inodes with
exclusive creation, exact geometry, finite values, fsync, descriptor-relative
readback, and SHA-256. Then bank payload manifest, routing manifest,
payload-derived top-32, result receipt, result terminal, consumer terminal, and
bundle index in causal order.

Secondary is ineligible until primary terminal closure. Then revalidate the
same five graph descriptor identities, bank one secondary durable start, and
invoke the exact V12 secondary wrapper and independent binary32 implementation
once. Never use primary output as secondary input or target.

Secondary payloads:

- final hidden `f32le[6144]`, exactly 24,576 bytes;
- final normalized `f32le[6144]`, exactly 24,576 bytes;
- full logits `f32le[154880]`, exactly 619,520 bytes.

Apply the same immutable-leaf, geometry, finite-value, routing, top-32,
receipt, terminal, and bundle-index requirements.

If primary fails, secondary never starts. If secondary fails, comparison never
starts. No numerical stage may be rerun for any reason.

Use durable notifications at coarse execution milestones outside the identity
producer. Notification failure does not create execution authority and does not
permit rerun.

---

# 10. Independent comparison, release, and terminal

Validate both result bundles from raw committed payload bytes. Require exact
package/event/role bindings, receipt-bound routing, payload-derived top-32,
distinct leaves, and terminal closure.

Invoke the independent comparison authority and rederive all frozen fields.
Do not trust a supplied summary.

Frozen thresholds:

- maximum absolute error `0.0065169706285814755`;
- RMSE `0.003463567697419031`;
- cosine minimum `0.9999999985448085`;
- top-N `32`.

Return exactly one classification:

- `EXACT_EXPECTED_TOKEN_STABLE`;
- `NUMERICALLY_STABLE_TOP_K_ONLY`;
- `TOP1_UNSTABLE_WITHIN_FROZEN_UNCERTAINTY`;
- `ORACLE_DISAGREEMENT`;
- `ORACLE_EXECUTION_FAILURE` only when execution/evidence closure fails before
  complete comparison.

Only after the classification is durable, run historical diagnostics for
tokens `21615`, `17351`, and `154820`. These tokens are prohibited as runtime
targets or success predicates and diagnostics cannot alter classification.

Release all five descriptor leases. Require five successful closures, zero
duplicate or unknown closes, and zero live leases. Bank comparison, release,
package receipt, and package terminal. The package terminal must recursively
bind all six payloads and all manifests, routing, summaries, receipts,
terminals, comparison, and release evidence.

---

# 11. Failure policy and source mutation boundary

Before approval: no approval, candidate, authority, state, checkpoint access,
or numerical operation.

After approval but before install: preserve approval and event plan; install
nothing; checkpoint access remains zero.

After install but before package start: preserve installed authority and
receipt; package delta remains zero; checkpoint access remains zero.

After package start: preserve the exact durable prefix, derive truthful phase
accounting, close descriptors, bank a modeled terminal failure, and stop.

After primary or secondary start: preserve every produced numerical byte and
terminalize from the durable prefix. Never rerun inference to repair evidence.

No load-bearing source implementation mutation is permitted. After execution,
repairs are limited to append-only evidence, evidence validators, indexes,
CI-census files, review packets, and declarations. A repair that changes or
would require rerunning runtime bytes invalidates the execution review; it does
not authorize a rerun.

Never force-push, rewrite history, delete accepted evidence, or amend a pushed
commit.

---

# 12. Evidence, CI, and independent review

After terminalization:

1. validate all artifact schemas, canonical serialization, hashes, causal
   closure, accounting, access census, geometry, leases, and exact execution
   counts;
2. commit and push append-only source evidence;
3. verify branch parity and clean worktree;
4. require EVIDENCE_ONLY CI PASS with native jobs launched `0`;
5. require Gemini CHALLENGE review of committed bytes;
6. permit only bounded non-runtime evidence repairs;
7. require Opus ARBITER review from a detached read-only checkout;
8. bank one final Event 06 declaration and manifest binding every load-bearing
   result;
9. require zero blocking, non-blocking-required, rejected, or unresolved
   readiness-critical findings.

Completed execution nodes are never rerun during review.

P1 attempt-2 preparation readiness may be `YES` only if the complete corrected
classification and independent arbitration support it. Do not mint P1
authority and do not execute P1 attempt 2.

---

# 13. Privacy, response publication, and concurrent prompt control

Apply `Prompts/F017/PRIVACY-POLICY.yaml` before every prompt-repository commit.
Retain only machine model, generic specifications, public runtime/model names,
repository-relative paths, branches, commits, trees, CI runs, and artifact
hashes.

Replace machine-local roots with typed aliases:

- `SOURCE_REPO_ROOT`;
- `PROMPT_REPO_ROOT`;
- `CHECKPOINT_ROOT`;
- `STATE_ROOT`;
- `NTFY_TOPIC_ALIAS`.

Never serialize usernames, absolute home paths, hostnames, private addresses,
internal DNS, mount/share names, notification topics, credentials, secrets, or
unrelated topology. Fail closed if a credential or prohibited identifier is
detected.

After final source evidence is committed and pushed:

1. render the paired Markdown response from committed evidence;
2. identify this prompt path, exact prompt commit, and prompt SHA-256;
3. identify source branch, starting head/tree, measured implementation, final
   evidence head/tree, CI runs, verdict, readiness, safety state, and next action;
4. sanitize and scan the response;
5. compute its SHA-256 and write the adjacent sidecar;
6. fetch prompt-control `main` and require this prompt commit remains an
   ancestor and Sequence 2 terminal artifacts remain absent;
7. preserve all concurrent MacBook artifacts and `Prompts/F017/STATE.json`;
8. stage only the paired response and checksum;
9. commit and push by normal fast-forward only;
10. verify remote bytes, sidecar, parity, and clean worktree;
11. deliver the established local notification without publishing its topic;
12. print the same substantive response in chat with commit-pinned response URL
    and response SHA-256.

If prompt-control publication cannot complete, state that truthfully. Never
claim a complete handoff without the committed response and checksum.

---

# 14. Required final response

Report:

1. prompt, source, and measured authority reconciliation;
2. pre-mint ownership, memory, call-path, and zero-access result;
3. fresh approval/plan SHA and all five fresh identities;
4. candidate triple, exact install, receipt, and package-start gate;
5. six-shard identity accounting;
6. primary and secondary execution/bundle results;
7. independent comparison and exact classification;
8. descriptor release and package terminal;
9. source evidence head/tree and EVIDENCE_ONLY CI;
10. Gemini and Opus results;
11. exact safety state, P1 readiness, and next safe action;
12. commit-pinned response URL and response SHA-256.

Always state:

```text
EVENT_04_RETRY: NO
EVENT_05_RETRY: NO
EVENT_06_EXECUTED: YES/NO
EVENT_06_PACKAGE_STARTED: YES/NO
LIVE_EVENT_06_AUTHORIZATION_CREATED: YES/NO
PRIMARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0/1
SECONDARY_REAL_ORACLE_EVENT06_EXECUTIONS: 0/1
ORIGINAL_CHECKPOINT_SHARD_OPENS: <exact count>
ORIGINAL_CHECKPOINT_IDENTITY_HASH_READS: <exact count>
ORIGINAL_CHECKPOINT_PAYLOAD_READS: <exact count>
P1_ATTEMPT_2_EXECUTED: NO
LIVE_P1_ATTEMPT_2_AUTHORIZATION_CREATED: NO
HISTORICAL_MASTER_LEDGER: 175
```

Return exactly one oracle classification and exactly one P1 readiness value:

- `READY_TO_PREPARE_P1_ATTEMPT_2_AUTHORIZATION: YES`; or
- `READY_TO_PREPARE_P1_ATTEMPT_2_AUTHORIZATION: NO`.

Then stop. Do not mint P1 authority and do not execute P1 attempt 2.
