# F017 Event-04 execution-readiness final review — Opus cycle 01

Use a fresh `claude-opus-5` session at high effort. Review exact committed implementation bytes at `fd517315f3e289d7abceb9ef73a793074d879fdf`, tree `afb208452ebe35eceba7100899454ec1354b9f36`, with exact-head qualification/rehearsal evidence at descendant `05876a3bdfec625ec9c72d771e26679bf6a1af45`. FULL_NATIVE run `32809028957` passed workspace and pinned native MLX jobs with required skips 0. Gemini cycle 03 independently returned `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION` with no findings. Repository evidence outranks this request. Work read-only. Do not access original checkpoint payloads, mint or execute Event 04, or execute P1 attempt 2.

Independently reconstruct the accepted V8 milestone and all twelve forward DID findings. Verify each closure from code, tests, exact Git objects, and runtime evidence—not counters alone:

1. Attack partial descriptor release, close-event banking failure after successful close, EBADF, retry policy, second release after partial failure, and double-close. Require exact pending/live lease census and zero live leases on success.
2. Derive package/primary/secondary deltas only from validated durable-start artifacts. Attack failures after primary and secondary start, wrong transition IDs, fabricated evidence, and historical-ledger drift.
3. Attack installed-auth revalidation, package root creation, package claim/start, identity progress, consumer subprocess, comparison, release, receipt, and terminal failures. Determine whether every modeled post-install failure reaches its maximal durable capsule and every post-package-start modeled capsule truthfully constitutes package terminal evidence.
4. Verify authorizer documentation and actual two-phase behavior agree.
5. Substitute production roots into synthetic mode and synthetic roots into production/shadow mode; inspect independently resolved synthetic manifest/root authority.
6. Inspect both descriptor-backed target sources. Prove they are memory bounded, use inherited FDs only, re-`fstat` on each bounded `pread`, never open graph paths, and run primary and secondary in separate subprocesses with exact `pass_fds`. Prove synthetic graph tensors materially exercise shards 2–6 and all 11 formats. Independently validate all 1,410 production graph tensor offsets/lengths against committed shard sizes and all 399 non-access denials.
7. Run generator `--check` and attack generated drift.
8. Verify deterministic core bytes versus volatile envelopes across fresh processes.
9. Force mint and package memory gates below threshold and stale. Production must fail closed; shadow observation must not claim production execution.
10. Directly run the import-separated C7-N2 malformed descriptor/lease regressions.
11. Inspect workflow PASS/FAIL cleanup and post-cleanup worktree census.
12. Inspect `f017_runtime_outcome_realizer_v9.py`: it must drive actual authorizer/install/coordinator transitions and inject at real causal banks. Re-run all 47 modeled outcomes, ensure 201 executions, exact required/forbidden artifacts and deltas, and zero generic fallbacks.

Also reconstruct Gemini cycle-01 findings and confirm their closure: active registry remains `NONE` before final declaration; qualification creates no live authority; future live candidate binds a fresh operator GO and accepted readiness; canonical no-replace install/readback/receipt exists; `execute_event04` is reachable only via installed production authority and excludes fault controls; production identity acquisition exists but was never invoked; eager full-model materialization is absent; release evidence recovery cannot double-close; failure classification is exact.

Recompute unchanged numerical core/decoder SHAs, numerical contract V3, original checkpoint access zero, Event-04 authorization absent, Event-04 execution absent, P1 attempt 2 absent, attempts 1, retries 0, resume false, and historical ledger 175. Do not recommend retries or resume.

List every finding exactly once as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`, with stable ID and smallest correct repair. Both blocking and non-blocking-required prevent acceptance. State material disagreement. End with exactly one unconditional verdict: `ACCEPT_FOR_CORRECTED_FULL_CHECKPOINT_ORACLE_EVENT_04_EXECUTION_AUTHORIZATION` or `REJECT`.
