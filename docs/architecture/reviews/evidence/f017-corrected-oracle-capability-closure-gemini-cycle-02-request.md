# F017 numerical capability closure — Gemini cycle 2

Use a fresh `gemini-3.1-pro-high` high-effort session. Review exact committed bytes at `3ee593d7cc3bef6c06a04360696179bb81ae2f59` in a clean detached read-only worktree. Repository evidence outranks this request. Do not modify evidence, access original checkpoint shards, mint Event 04 authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Cycle 1 was advisory ACCEPT, while independent Opus cycle 1 found N1–N5. Verify their repairs:

1. Reproduce F5-R4 and representation-independent module/member aliases.
2. Attack dotted descendants including `numpy.lib`, `numpy.ctypeslib`, and `mlx.core.*`; confirm semantic identity is prefix-resolved and exact import census rejects them.
3. Attack function-parameter shadowing and class-scope leakage; confirm unknown parameters shadow outer bindings and class bindings are not method lexical bindings.
4. Inspect all 181 mutations. Confirm each case binds intended rejection classes and at least one substantial census reaches transport controls rather than shallow token checks.
5. Attack declared binding forms: decorators, AugAssign, exception targets, match captures, defaults, closures, containers, comprehensions, and receiver aliases.
6. Verify capability manifests and bytecode evidence are repository-relative and deterministic across cwd.
7. Verify the capability authority generator is contract-bound and rerun in CI with exact generated-byte drift detection.
8. Verify runtime subset assertions are computed, and distinguish load-bearing static checks from defense-in-depth runtime/bytecode corroboration.
9. Verify contract v3 exact bindings, unchanged pure-core bytes, 24 exact historical-equivalence cases, complete requalification, and zero original-checkpoint access.
10. Verify exact-head FULL_NATIVE run `32674829986` passed both required jobs with zero required native skips.

State any unresolved material disagreement and whether any repair requires formula, methodology, threshold, original-checkpoint, or independence changes.

Required advisory verdict exactly one: `ACCEPT` or `REJECT`.

Confirm safety: Event 04 authorization absent; Event 04 unexecuted; real primary/secondary executions zero; original checkpoint access zero; P1 attempt 2 absent; historical ledger 175.
