# F017 Event 06 Sequence 5 — Opus Design Arbitration, Cycle 7

Arbitrate exact detached read-only commit
`235ce844278f21df84c076e87868848a4544c1d7`, tree
`039c0405cc155b3346b313f4faa5200523d7b626`.

This is design only. Do not write repository state and do not resolve, stat,
enumerate, open, hash, read, or mmap any original checkpoint path. Do not
create authority, execute Event 06, or execute numerical operations.

Independently execute:

```text
python3 scripts/research/generate_f017_event06_sequence05_design_v7.py --check
python3 scripts/research/validate_f017_event06_sequence05_design_v4.py
python3 -m py_compile scripts/research/generate_f017_event06_sequence05_design_v7.py scripts/research/validate_f017_event06_sequence05_design_v4.py
```

Reconstruct the exact Cycle-6 Opus rejection and all eight minimum repairs
from its exact response and provider/provenance bindings. Attack all five
formerly literal predicates and their isolated mutations, implementation
measurement v2 rebinding, declared-tree manifest instantiability, exact
21-field provenance, 324-case arithmetic, 16 outcome/transition mappings,
distinct Cycle-04/Cycle-05 advisory namespaces, graph/claim reconciliation,
external qualification-schema authority, and all prepared-manifest validation
steps. Do not accept generated PASS labels without reproducing them.

The independent Antigravity Cycle-7 challenge against this same commit ran the
validator and returned `REJECT` with two blocking findings:

1. The prepared-manifest validator excludes only its own prepared manifest,
   while `bridge_declaration` binds a historical bridge final declaration; it
   judged the demanded final-declaration exclusion incomplete.
2. The 6×3 alias factorization still uses structural variants that overlap the
   six semantic alias families; it judged the axis mathematically ungrounded.

Independently accept, reject, or refine those findings from repository bytes.
Search for any additional blocker. Return a claim-by-claim verdict for:

- CANONICAL_READINESS_DESIGN
- MANIFEST_ACYCLICITY
- PRODUCTION_INSTALLATION_DESIGN
- POSTURE_SEPARATION
- FUTURE_GO_CAPABILITY_GATE
- DRY_INSTANTIABILITY_DESIGN
- NO_ACCESS_DESIGN
- IMPLEMENTATION_QUALIFICATION_PLAN

For every finding provide unique ID, severity, exact path/line or JSON field,
affected claim, and smallest repair. State counts for blocking, required,
advisory/challenged, and unresolved findings.

Acceptance requires all four counts zero and exact global verdict:

`ACCEPT_F017_EVENT06_SEQUENCE5_DESIGN_CYCLE7`

Otherwise return `REJECT`. No conditional acceptance. Independently execute
the validator and mutation suite rather than deferring to Antigravity.
