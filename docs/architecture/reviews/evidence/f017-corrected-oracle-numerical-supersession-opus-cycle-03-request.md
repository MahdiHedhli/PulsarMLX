# F017 corrected-oracle numerical-authority supersession — Opus cycle 3

Use a fresh `claude-opus-5` high-effort session. Review exact committed bytes at `80f44b458824903f5dca101bc8f1d590f6845422` in a clean detached read-only worktree. Repository evidence outranks this request. Do not access original checkpoint shards, mint authority, create Event 04 state, execute a real oracle, or execute P1 attempt 2.

Reconstruct cycles 1 and 2. Independently verify the cycle-2 required repair:

1. Primary pure-core imports are restricted exactly to `__future__`, `dataclasses`, `hashlib`, `json`, `math`, `struct`, and `typing`.
2. Secondary pure-core imports are restricted exactly to `__future__`, `hashlib`, `json`, `math`, `mlx`, `numpy`, `struct`, and `typing`.
3. Relative imports, dynamic imports, builtins/importlib escapes, file/network/native/archive modules, and unreviewed modules fail closed.
4. The target-source function/method census is exact per role, so renamed or additional graph methods fail in addition to the explicit arithmetic-name guard.
5. All previously closed F1/F2/F3/F7/F8 properties remain closed.
6. Full requalification remains byte-identical, formulas and thresholds remain unchanged, and original checkpoint access remains zero.

Run adversarial import and target-method mutations plus the full validator and requalification. Classify findings as `BLOCKING`, `NON_BLOCKING_REQUIRED`, or `DEFENSE_IN_DEPTH`.

Required terminal verdict, exactly one:

- `ACCEPT_CORRECTED_ORACLE_NUMERICAL_AUTHORITY_SUPERSESSION`
- `REJECT`

Confirm: Event 04 authorization absent; Event 04 unexecuted; original checkpoint access zero; real primary/secondary executions zero; P1 attempt 2 absent; historical ledger 175.
