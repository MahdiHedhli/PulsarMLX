# F017 M1-E Attempt 2 Authorization

**Status: PREPARED / NOT AUTHORIZED / NOT EXECUTED**

The previous packet is superseded because execution-config v2 conflated
repository HEAD with the compiled runtime SHA. Attempt 2 did not begin and
remains unconsumed: checkpoint access, oracle creation, tensor decode, MLX
dispatch, and attempt evidence are all zero/absent.

The repaired authorization will be issued only after a private immutable v3
config binds the exact reviewed authorization checkout, compiled runtime,
tooling identity, executable identity, ancestry proof, permitted-delta hash,
handoff hash, decoder-v2 artifacts, expert boundary, and all prior evidence;
and after the canonical config-only preflight returns exactly
`READY_TO_EXECUTE_M1_E`.

No execution is authorized by this prepared packet. M1-F remains blocked.
