# PulsarMLX F017 Accelerated Post-v3 Checkpoint-Free Report

## Outcome

`BLOCKED — REPRESENTATIVE FIXTURE STRATEGY`

Per-track dispositions:

- Track 1: `NEW_FAMILY_REQUIRED`
- Track 2: `ROUTE_INDEPENDENT_M1F_READY`
- Track 3: `DOWNSTREAM_PREP_READY`

The package is complete for independent adversarial review. The blocker is a
scientific real-access decision, not a checkpoint-free implementation defect:
both precommitted synthetic families are too underpowered under v3 membership
H=2, and the next credible representative boundary executes a real dense
prefix and therefore requires separate preparation, review, and authorization.

## Identities and phase boundary

- Starting SHA: `b9db0c5fcff03f1e19e6344d9173f134dad87059`
- Final load-bearing SHA: the implementation/evidence commit preceding this
  report; the exact final documentation head and CI run-to-head binding are
  supplied in the final operator handoff.
- Original routing-contract v3: `befbf30f85e12b779e7d5c778f337a5f7d6019a15805e04805a24e4903ea3969`
- Clarified v3 semantic target: `c5662a611abc000703606d799a7214ee27e39c556bc6595f217c86498e944a85`
- Real checkpoint payload access: `0`
- Real-payload ledger: `57` (unchanged)
- Frozen ladder executed: `false`
- Correlated family real discovery: `false`
- Q6_K real qualification: not executed
- M1-F real execution: `false`
- M1-G real execution: `false`
- P1: `false`

## V3 clarification closure

The clarification-only v3.0.1 successor binds predecessor `befbf30…` and
records no formula, threshold, or classification change.

1. Binary64 reduction-order variation in `sum_selected(probability)` is
   explicitly enclosed by the already frozen four-ULP probability endpoint
   guard. All eight-term permutations pass the regression.
2. The accumulation-order bound permutes exactly the eight routed expert terms.
   Shared-expert and residual placement is fixed; attempting to include them
   in the routed permutation surface fails.

## Track 1 — v3 H=2 fixture planning

Estimator contract SHA-256:
`5d54d1532df4c7b3ce442295f76f43f4ff6fade6c94c531d01d374aa9ae869b7`.
Each family used 1,000,000 PCG64 samples. The methodology targets the complete
selected-vs-unselected membership surface, excludes rank order, and reports
mathematical/H=2 rates plus p01/p10/p50/p90/p95/p99/p99.9 and maxima. Candidate
per-expert weight qualification is correctly marked unestimable without a
candidate execution.

The decision rule was frozen first: viable only when the conservative Wilson
lower `P(any of 8) >= 0.90`; non-viable when even the optimistic retained-
tightening Wilson upper is below 0.90. This threshold is justified against the
naive 96-payload cost; decoded-tensor reuse remains separately reviewed.

### Frozen random-normal ladder

- conservative H=2 rate: `0.000211`
- central H=2 rate: `0.007933`
- optimistic H=2 rate: `0.037541`
- optimistic central `P(any of 8)`: `0.2636947387004095`
- optimistic Wilson upper `P(any of 8)`: `0.26598263695757707`
- decision: `EXISTING_FROZEN_LADDER_NOT_VIABLE`

The complete fixture descriptors are retained with no route labels. Ladder
bytes, seed order `17017007..17017014`, and generator identity are unchanged.

### Correlated checkpoint-independent family

A spectral-16 plus AR(1) family was frozen before evaluation:

- family SHA-256: `a5c248e52ac576e2a9a332f748bc26be0aa068688d82af743bab814d1bbbfe03`
- generator SHA-256: `559b351256db68d348712ec7bb0e0b26092c3e2b5c88263376e6a31e12a34ac1`
- seeds: `17017201..17017208`
- selection: first qualifier in precommitted order
- stopping/banking: evaluate and retain all eight if separately authorized
- conservative/central/optimistic H=2 rates: `0.000151 / 0.006987 / 0.036005`
- optimistic Wilson upper `P(any of 8)`: `0.2565093182072469`
- decision: `CORRELATED_FAMILY_NOT_VIABLE`

This surrogate predicts no actual route and uses no checkpoint router rows.
The representative recommendation is therefore the separately reviewed real
dense-prefix layer-3 entry-state boundary, with fixture 1 retained as
`ADVERSARIAL_STRESS_ONLY`.

## Track 2 — route-independent M1-F preparation

Readiness artifact SHA-256:
`39622158f77f6158a91869e23cb3258408541845432d8996d0ba1b1dc4855662`.

- Expert slices: all 256 gate/up/down triplets derive independently from the
  banked catalog; IDs 0, 1, 15, 166, and 255 plus layout/index/alignment/
  truncation/overflow negatives pass.
- Dispatch instrumentation: conceptual roles and unique native launch events
  are separate. Constant and per-selected-expert scaling reconcile
  mechanically; fusion is preserved and no route-specific total is frozen.
- M1-F evidence/config: closed schemas bind runtime/tooling/executable,
  checkpoint/catalog/map, clarified v3, exact eight atomic pairs, allowlist,
  decoders, oracle, numerics, attempt state, and evidence destination. Loose
  command overrides fail closed.
- Decoder harness: at least two independent implementations, optional third,
  canonical LE f32 exact equality, malformed/truncated/non-finite negatives,
  and public-safe evidence.
- Q6_K: template prepared, target deliberately null until a representative
  route fixes the inventory.
- Numerical scaffold: complete layer stages and stress families are defined;
  route-observed threshold setting is forbidden.
- Repeat/lifecycle: ten complete equal stage-hash sets, mechanically reconciled
  dispatch, teardown, zero in-flight/stale work, and zero fallback/reference/
  scaffold/backend errors.

## Track 3 — post-M1-F to P1 acceleration

The exact roadmap is:

`M1-F accepted → M1-G final norm/logits/top-k → T017-141 command publication → M1-H review/one-P1 authorization → P1`.

M1-G still needs accepted final-hidden provenance, real-byte Q4_K output-head
qualification, full `output.weight` payload/memory admission, and an R11-
derived real-shape numerical contract. Route-independent execution/evidence,
attempt, repeat, retention, lifecycle, PASS, and privacy scaffolds are prepared
but unauthorized. The literal P1 command remains unpublished.

Known P1 fields are binary `f017-glm52-runner`, token `[9703]`, `n_new=1`,
expected token `21615`, `golden-strict`, and `production-mlx-tier-b`. Runtime,
authorization, manifests, memory floor, stream mode, evidence destination, and
accepted M1-F/M1-G hashes remain unresolved. Feature 018 is not a first-P1
dependency.

## Dense-prefix fallback inventory

Provisional honest gate:
`F017 M1-FPREP REAL DENSE-PREFIX LAYER-3 ENTRY-STATE BOUNDARY`.

It uses pre-existing P-MIN token 9703 at position 0, executes embedding plus
complete dense layers 0–2, and captures the layer-3 entry state. Metadata-only
catalog derivation gives:

| Measure | Value |
|---|---:|
| Payloads / positional reads | 40 / 40 |
| Shards | 1 |
| Packed bytes | 1,431,263,232 |
| Decoded-f32 upper bound | 8,504,653,824 |
| Largest decoded tensor | 3,806,330,880 |
| Projection-call planning count | 28 (not a native budget) |

Quantization counts are F32 12, Q8_0 12, Q5_K 12, Q6_K 3, and Q4_K 1. Q4_K
and Q6_K are unqualified real gates. Streaming peak and real native dispatch
budget remain unresolved until a residency plan and production path are
separately prepared.

## Review and validation

Internal review verdict:
`GO FOR ACCELERATED POST-V3 ADVERSARIAL REVIEW`.

Combined packet:
`docs/architecture/reviews/f017-accelerated-post-v3-adversarial-packet.md`.
SHA-256:
`0ba113024d1509bf5e55c82f0dd527258a6c3c12d73f3832a442e3e85eec777d`.

The final validation surface includes the full Rust workspace, full Python
research/evidence suite, routing-v3 and clarification regressions, both
million-sample estimator regenerations, fixture regeneration, slice/dispatch/
decoder/schema/numerical/repeat/lifecycle/downstream tests, historical hashes,
ledger, duplicate-key, privacy/path, generated-artifact, Spec Kit, and
`git diff --check`. Exact counts and the final Apple run-to-head binding are
reported in the operator handoff after the final-head CI is green.

## Exact next real-access decision

Independent adversarial review must decide whether to prepare the 40-payload
dense-prefix layer-3 entry-state boundary, including separate Q4_K and Q6_K
real-byte decoder gates and a bounded residency/oracle plan. No real access is
authorized by this package.
