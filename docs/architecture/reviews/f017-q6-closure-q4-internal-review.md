# F017 Q6 Closure + Q4 Preparation Internal Review

Verdict: `GO FOR Q4_K AUTHORIZATION DELTA REVIEW`

The minimized Q6_K fixture reproduces the lane-order defect and the corrected mapping agrees exactly across two independently structured Python implementations and a separate Rust implementation, with pinned upstream provenance. The accepted F017 surface contains no real Q6_K decoded dependency. F016 is annotated without rewriting historical claims.

The active dense-prefix decoder lineage now points at the pinned upstream C function; its prompt, inventory, byte budgets, 27 GiB admission floor, numerical thresholds, oracle, routing v3 contract, and attempt semantics are unchanged.

The Q6_K future target remains `blk.0.ffn_down.weight` under the frozen mechanical rule and is staged `PREPARED_NOT_AUTHORIZED_NOT_EXECUTED`. The Q4_K package binds the only inventory target, three independent decoder paths, exact-byte acceptance, a one-payload budget, an empty attempt ledger, and separate-review/operator authority. No model compute or automatic continuation is possible.

No false-PASS or historical retraction path was found. Real checkpoint access is `0`; ledger remains `57`.
