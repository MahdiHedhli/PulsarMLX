# Source-composed router caller contract

This phase preserves the 48 accepted predecessor files. It executes only three complete selected definitions: PipeNetwork Glm5NextMoEGate, mlx-vlm MoEGate and group_expert_select. Both full upstream modules, every other upstream constructor, model/processor/tokenizer construction, weights, Safetensors headers, inference, KDA, convolution cache and storage-engine integration remain unexecuted. Public source push/CI and shared-method acceptance remain deferred.

PipeNetwork a61a7c7d2fbdf3d218a9909365a24bd794f3a247 language.py SHA 6de479b6eafc0731e5e965f01f28797a58eecb606e7d5d5657db13642c230196 binds `_dsv32` and `MoEGate` explicitly to mlx_vlm.models.deepseek_v32.language (lines13–14). The complete caller class at54–62 casts both operands to FP32, projects against transposed weights and calls the selected selector with stored router attributes. Its node SHA is ff08475c6fc93a110abda7f6efc3c5f1f3b2da5e8a48404608f159569b51921f. The upstream Apache-2.0 LICENSE body and attribution are retained separately; copied upstream text is not relicensed as the research code.

mlx-vlm 8d79dbcf82a7885ddfc56fb4a55440fa9c5dfb90 deepseek_v32/language.py SHA cd210c73ce3e569ab5270a47a92941cd5ec1450c6ff7b55b8be57c51263f4cb6 supplies the complete MoEGate class at268–291 (SHA7ffeb78d43c856bdd389cbaa5bdf51d33c600873beeaab78dee23c562472771a) and decorated selector at233–265 (SHA4efab08404da68c50faa1f2a4ebe3235436a1b2aa09bac6e77e20dc0fba35ad9). Its MIT notice is retained. The base constructor creates FP32 zero weight/bias arrays and copies router fields from the config; the GLM class inherits that actual constructor.

Whole-file/node hashes, AST node/decorator/base checks, symtable global inventory and exact import statements precede selected-definition compilation. Actual admitted mlx.core and mlx.nn are the only external numeric bindings. Research seams are an inert exact-field ResearchConfig for annotation/attribute access, a namespace containing only the selected selector, minimal class-construction builtins and module names. The selected definitions do not require functools.partial or typing names, so none are enabled. Named control derivatives are explicitly different source bodies, never passed off as upstream.

Every normal/mutant/restored step builds fresh selector function and base/caller class objects and retains them alive so identity reuse cannot fake a reset. No compiled node is mutated by merely rebinding a cached global. Native kernel/driver caches may still be reused; cold-cache behavior is not claimed. A-B-A-B calls test independent instances/configs, repeated outputs, weights/biases and router attributes. This is known numerical/config state, not an OS or universal cache census.

Fixtures are synthetic finite matrices, H<=16, E<=288, B<=2, T<=5, each case's arrays<=1MiB. The artifact-shaped case carries only the verified config's E288, top_k8, n_group=topk_group=1, norm=true, scale2.5/noaux_tc router flags with hidden4 and handmade weights. Other cases are generic, including top_k1/top_k2, scaling, norm on/off, leading axes, distinct instances and positive grouping. A separate group-sensitive fixture distinguishes the sum of a group's two scores from the largest individual score in another group. Generic negative grouped exclusion remains FLSH-SELECTOR-MASK OPEN.

The scalar oracle imports only math/struct. It freezes exact represented FP32/BF16 words, f64 dot products and sigmoid scores, selection/group margins and per-ID weights before caller outputs. IDs compare as sets; returned partition order is recorded but unspecified. Routing tolerances remain atol4e-5/rtol4e-6. The separate projection diagnostic uses atol2e-6/rtol2e-6; it is explicitly a research expression on the same arrays, not a capture of caller locals. Its raw results and actual complete-caller outputs are separately identified. The unchanged selector also runs on that diagnostic projection with the original frozen config; both paths compare to the independent scalar reference.

Projection error uses gamma_H*sum(abs(x*w)), with u=2^-24. A predecessor engineering allowance16u plus one quarter of the projection bound covers sigmoid; bias rounding is added to ranking error. The sum/normalization bound uses K*sigmoid_error+gamma_K*(S+K*sigmoid_error), a positive S-dS denominator, division and scaling roundoff. The scalar reference slack is2e-12. Each prospective case stores these bounds and margins. These are bounded engineering allowances, not universal library ULP promises.

The BF16 domain is represented input/weight conversion followed by FP32 router computation only. A predeclared precision case compares against scalar BF16 rounding of the projection, with a predicted output difference greater than the preserved routing tolerance. Omitting just one cast may be masked by mixed-operand promotion. Such controls must be reported as numerically nondistinguishing, never counted as detected. Unsupported native BF16 operations are explicit gaps with exact exceptions. Arbitrary errors remain harness failures. No BF16 model or quantized-kernel parity is qualified.

The control table identifies source/global/selector binding guards; missing/swapped bias; transpose and row association; top_k, scaling, normalization, group count and cross-instance configuration; wrong expert-ID score comparison; and individual/both cast omissions. Normal -> mutant -> restored observations assert intended exception class/code and actual caller evaluation boundaries. Wrong-type, wrong-message and no-exception classifier probes reject false detections. Mandatory FP32 controls cannot pass by survival; cast-control gaps remain explicit.

The first complete CPU case must produce results, receipt, checkpoint and a portable parts archive that is read back in a separate archive-only child before the matrix. The reader never executes archived source or imports a numeric backend. Parts are<=512KiB, with size/SHA-256 and source/portable identity mappings; complete raw originals stay external. Personal path substitution is declared, not misrepresented as original raw-byte equality. Later publication uses strict Base64 Contents decoding and Git-blob identity. The runtime itself remains separately hash-locked and retained; this evidence archive is not a standalone runtime installer.

Changed edges and assertions: source/import bindings -> selected class hierarchy (`load_nodes`, constructor identity and guard controls); config -> actual base fields/zero arrays (`Runtime.make`); represented input/weight -> FP32 projection (`Runtime.invoke` and independent scalar bounds); caller projection/attributes/bias -> exact selector (all cases and real propagation controls); selected IDs -> per-ID score interpretation (association controls); distinct instances/functions -> repeated and restored observations (A-B-A-B and per-control resets); producer -> portable archive -> archive-only readback (single-case preflight). Full upstream modules, other constructors and production source consumers are explicitly excluded from each edge.

The reused CPython3.14.6 arm64 interpreter and MLX/MLX-metal0.32.2 environment are reverified by content and origins. NumPy2.5.3, Jinja2 3.1.6 and MarkupSafe3.0.3 remain in the same admitted wheel generation. WHEEL_SOURCE_CORRESPONDENCE is RELEASE_DECLARED_NOT_BUILD_VERIFIED. Isolated Python excludes user-site/PYTHONPATH and bytecode writes; the admitted package file set is checked for additions/drift. Selected namespaces expose no loader, provider, network or model tool. This is not an OS network sandbox. Direct-child RSS is sampled under1GiB, MLX memory/cache controls are set where supported, captures<=16MiB, deadline175s within the180s budget, and owned process-group teardown must complete before further starts. Descendants are not prevented or included in RSS. Core dumps are disabled. All project-controlled writes stay on the bound external NVMe; OS caches are not claimed isolated. FLSH-PROTECTION-01 stays OPEN (unencrypted/noowners), with no drive setting changes.

The shared source-map-tiny-parity method stays DRAFT. This task stops after its separately reviewed private handoff; the parent owns acceptance and cross-track coordination. No successor phase is launched.
