# Review-directed successor generation

Completed Fable5.1 REJECT is retained; its RV-MUT-01 blocker invalidates the initial seven-mutant acceptance claim. The missing-mx control previously forced rejection with a boolean flag. The successor passes an actual empty exec namespace to the same byte-exact capsule entry; Python must raise NameError at the original @mx.compile decorator. No upstream snapshot or decorator is changed.

A supplemental eighth distinct mutant changes mx.sigmoid to np.sigmoid in an in-memory copy, passed to the same production AST/symtable validator. It must reject external globals {mx,np}. This control isolates the global-closure guard without weakening the capsule hash gate and does not execute modified upstream text. All eight controls require normal → mutated → restored outcomes. The original seven IDs/fixtures/tolerances remain immutable; this new control is declared before its first run.

RV-FIX-01: the normalization-disabled top_k=2 fixture selects symmetric logits whose sigmoid sum is one. Keep that observation but mark normalization-off branch discrimination NOT_QUALIFIED. Normalization-on, single-selected behavior and configured norm=true remain separately exercised. No fixture is silently replaced.

RV-DOM-01: reject topk_group==n_group>1 to match the declared domain before calling upstream. RV-MUT-02: swapped axes is a real MLX channel-shape rejection, not an oracle numeric mismatch; reversed taps is a semantic mismatch. RV-REC-01: underflow rejection now asserts the actual finite-output error and an evaluation-count increment, and records the exception. RV-PRE-01: only the successor source generation qualifies revised code; prior prospective criteria and failed review stay intact. RV-CONV-01: k=1 is not qualified; all exercised kernels have width four. RV-OBS-01: CPU/Metal observations remain device-labelled.

A wording correction says that no full model module is executed; the explicitly named source-extracted selector function is executed. No scope, tolerance, wheel or upstream capsule change is made.
