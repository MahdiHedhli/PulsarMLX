# Prospective inert template boundary

Exact retained template SHA 34d5ee66b12fa6446cdae131c352b8f68cd85369e0e6fda115583805fada3891 is parsed only after same-buffer custody checks. Standard Jinja SandboxedEnvironment and StrictUndefined have no loader. Installed loopcontrols enables source break statements; reviewed standard namespace and sandbox range support only bounded macro loops. Other default globals are removed. No custom callable/filter is introduced. Inert JSON inputs are at most 64KiB and 16 messages; captured output at most 16MiB under the separate ten-second child cap.

Text uses an exact prospective expected string. An assistant tool-call-shaped fixture has string arguments and executes no tool. Missing messages, role or user content must reject with UndefinedError. A malformed messages container must reject before rendering. The exact template's tool-declaration macro calls tojson(ensure_ascii=False); installed Jinja's standard tojson accepts only indent. Preserve that TypeError as TEMPLATE_TOOL_DECLARATION_NOT_QUALIFIED, without substituting a filter. This test qualifies neither arbitrary template safety nor comprehensive malformed-message validation. Tokenization, media paths and model behavior remain unexecuted.
