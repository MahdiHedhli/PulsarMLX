# Source-bound cache lifecycle

The named successor operation `cache` composes the original `ArraysCache(size=2)` caller expression with exact `_BaseCache` and `ArraysCache` source from `Blaizzy/mlx-vlm@8d79dbcf82a7885ddfc56fb4a55440fa9c5dfb90`. This is a prospective source selection for the retained dependency contract, not proof of the original checkpoint producer's installed version. The installed runtime remains the admitted MLX 0.32.2 environment; the full mlx-vlm module is retained as data and never imported.

The capsule preserves both complete classes. The loader verifies source bytes, original byte spans and class ASTs, and compares the factory expression with the unique call in the retained PipeNetwork language source. Only `mx`, `Optional`, `List` and ordinary Python builtins are needed by the cache closure. The accepted convolution source slice, scalar oracle and earlier fixtures are unchanged. Research wrapper functions and instrumentation are identified separately from copied source; the two original licenses accompany the capsule.

The finite FP32 fixtures cover kernel sizes 2, 3 and 4, batch at most two, three or six channels and at most seven tokens. Inputs and states are signed quarter multiples bounded by 1.5; asymmetric, channel-distinct weights are eighth multiples bounded by 1. Four products bound each raw sum by 6. The existing scalar oracle supplies independent convolution, SiLU and split expectations; cache expectations come from declared Python row/list rules, not candidate methods. Raw absolute/relative tolerances are 2e-5/2e-6, SiLU 4e-5/4e-6; retained state is exact. The old validator's T8 and K1 admission is not a measured cache domain.

The finite edges include whole/chunked and prefill/decode execution, supplied and fresh state, interleaved streams, fresh-cache replacement, intentional snapshot/state reference sharing, and actual filter/extract/extend/merge followed by convolution consumption. Bookkeeping checks cover padding, lengths, deferred advance, masks and finalization. Slot1 uses synthetic values only and must remain untouched by convolution; this is not KDA numerical equivalence. No clear/reset API is invented: reset means constructing a distinct new cache.

Seven named controls cover wrong slot, wrong retained suffix, update order, absent fresh cache, cross-stream alias, stale prefill state and incorrect advance. Reset omission and cross-stream alias use the same stale-cache mechanism and are not independent mechanisms. Each classifier requires the exact assertion type/message and a per-target convolution evaluation delta of one (zero for bookkeeping advance). Normal/mutant/restored trials bind the same inputs, weights, original source and interpreter. Wrong type, wrong message and zero/extra numerical deltas are rejected by classifier checks.

Original source mutation/alias effects remain observable. Research refusal of nonfinite or out-of-domain input precedes source execution and is not an upstream guarantee. The controlled convolution failure observes the actual preceding cache write; it does not claim rollback. Source `extract` with an empty slot raises its original TypeError. The finite checks make no claim about arbitrary batches, every method branch, model construction, KDA, BF16, quantization, weights, physical I/O or performance.

Run through the existing admitted successor entry with operation `cache`. It first verifies the exact input manifest and source-free doctor/fences, then imports the tiny closure. A passing producer must report the exact eight named checks with zero skips/errors/failures and complete stop/capture receipts. The archive has the distinct `successor-cache-lifecycle-layout-v1` label and includes every admitted body. Current CPU/Metal, fresh-root, review and CI status belongs to the exact external evidence receipt; this document alone is not an execution claim.
