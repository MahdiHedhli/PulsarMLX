# F017 D0 v2 append-only review repair

D0 v1 remains immutable. V2 loads and validates its exact SHA before applying
one stage override: `router_normalized` is restored to the accepted retained
numeric R10-intermediate grade against SHA `687a692a...`. No tolerance changes.

Every JSON authority now carries branch, commit, path, SHA-256, schema,
schema-version, and semantic role, and the v2 validator resolves all seven
fields from committed bytes. Every effective stage oracle label resolves
through a closed registry to either a pinned independent fixture JSON path or
the exact historical comparison ordinal and expected SHA.

The misderived-tolerance repair string is mutation-locked. D3.5 must also bank
the executable, MLX/MLX-C libraries, Metal/macOS/hardware environment, and each
stage backend/dispatch symbol so a repeat divergence can be attributed. Numeric
tolerance can never conceal that divergence.

These changes repair Fable cycle-1 F1 and F3 and close F2, F4, and F5 as
defense in depth. Retained qualification and real P1 remain unexecuted.
