[package]
name = "backend"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Backend-neutral contracts for PulsarMLX inference runtimes."

[dependencies]
